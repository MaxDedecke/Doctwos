# Doctwos — Offene Entwicklungspunkte

**Stand:** 06.09.2026 (O-114 bis O-116 neu — Link-Manager: er ist das einzige Kurationswerkzeug für den Wissensgraphen, kann seine eigene Frage („stimmt diese Verknüpfung?") aber nicht beantworten, weil er als einziges Panel keine Navigation hat; dabei entdeckt: `backend/api/link_chat.py` wird von keinem Client aufgerufen und legt Verknüpfungen am pending/approve-Workflow vorbei an, und `LinkManagerView` umgeht mit direkten `fetch`-Aufrufen die zentrale API-Schicht samt 401-Interceptor. O-092 weitgehend erledigt — Soll-Matrix der Panel-Synchronisation als `docs/PANEL_SYNCHRONISATION.md` angelegt (30 Zellen `PS-01`…`PS-30`, zwei Navigationspfade, Auslöser-Inventar), Entscheidungen D-1 bis D-3 getroffen und umgesetzt (Einfrieren schützt in beiden Pfaden, Hinweis statt stillem Abbruch an der 4-Panel-Grenze, Call-Graph behält seinen Fokus), automatisiert in `frontend/hooks/panelSyncMatrix.test.tsx` (29 von 30 Fällen); D-4 bestätigt den Ist-Zustand (Historie bewegt alle Live-Panels, eingefrorene nicht); offen bleibt nur die nicht blockierende Festlegung D-5. O-091 dabei verifiziert und die zuerst notierte Ursache korrigiert: es ist der Dokument-Zweig der Graph-Aktion, nicht der Entity-Zweig. O-093 bis O-113 neu — Testabdeckungs-Lücken außerhalb des Zuschnitts der Reihe O-055–O-062, beim Nachprüfen der Frage „sind damit alle Tests nachgezogen?" gefunden: der gesamte Einstellungs-/Administrationsbaum des Frontends (`components/settings/`, elf Tabs, 5.259 Zeilen, kein einziger Test), vier weitere ungetestete Bestandskomponenten (u. a. der selbstgeschriebene Markdown-Renderer) und sieben Backend-API-Module ohne gezielten Test, geschnitten als ein Punkt pro Tab bzw. pro API-Modul; O-056 bis O-062 sowie O-076 erledigt — damit ist die Regressionstest-Reihe abgeschlossen; O-090 bis O-092 neu — Nutzerbeobachtungen zu Chat-Antwortumfang und zur Panel-Navigation/-Synchronisation — Component-Tests für die in `Sidebar.tsx` verbliebene Orchestrierung, für `WorkspaceShell.tsx`, `CallGraphView.tsx`, `JobCenter.tsx`, `LinkManagerView.tsx` sowie `PanelContentRenderer.tsx`/`SplitPaneWorkspace.tsx`; 05.09.2026: O-044/O-036/O-053/O-075/O-055 erledigt, O-052/O-054 neu, O-055–O-062 neu — Regressionstest-Aufbau in Häppchen, O-063–O-076 neu — Codequalitäts-/Abhängigkeits-/Onboarding-/Performance-/Auditierbarkeits-/Ingestion-Qualitäts-/Fortschrittsanzeige-Audit für die Repo-Übergabe, O-077–O-081 neu — Mehrsprachen-Skalierbarkeit: aktueller Stand geprüft (Git-/Confluence-Sync-Pipeline als Mermaid-Diagramme in `docs/GIT_SYNC_PIPELINE.md`/`docs/CONFLUENCE_SYNC_PIPELINE.md` dokumentiert), daraus scope-bewusste Refactoring-Punkte abgeleitet, die die Aussage "skalierbar auf andere Sprachen, wenn nötig" nachprüfbar machen sollen, ohne den bereits einmal gescheiterten Fehler einer ungenutzten `parser/languages/`-Vorrats-Abstraktion zu wiederholen, O-082–O-085 neu — Confluence-Chunking-Qualität: generisches `CodeParser.chunk_file()` fehlt Abschnitts-Metadaten, section-bewusste Chunk-Grenzen, Winzling-Merge und eine Innerhalb-der-Zeile-Aufteilung für übergroße Einzelzeilen, alle vier abgeleitet aus dem Vergleich mit COBOLs strukturbewusstem Chunking in `docs/GIT_SYNC_PIPELINE.md`, O-086–O-089 neu — Chat-Feedback zur Produktverbesserung: `ChatMessage.feedback` existiert seit je her, wird aber nirgends ausgewertet (0/28 Nachrichten je bewertet) — Sichtbarkeit schaffen, Downvotes an den bestehenden Link-Review-Workflow koppeln, als Eval-Set für Prompt-/Retrieval-Tuning nutzen; echtes RLHF/DPO-Fine-Tuning bewusst als spätere, von Kundenentscheidung und Datenvolumen abhängige Option zurückgestellt statt sofort umgesetzt; O-077 erledigt — Struktur-Parser-Dispatch in `GitConnector` auf eine kleine `STRUCTURE_PARSERS`-Registry umgebaut, Dispatch-Mechanismus per Fake-Sprache-Testdouble abgesichert, bewusst ohne neues `parser/languages/`-Paket; O-078 erledigt — generische Parse-Typen von `cobol/model.py` nach `parser/core/model.py` verschoben (Re-Export für Bestandscode), `spaces["cobol_extensions"]` auf `language_extensions` umbenannt mit Fallback auf den alten Schlüssel)
**Zweck:** Zentrale Liste für noch offene Änderungen, Abnahmen und Entscheidungen.

O-029 bis O-036 stammen aus einem Abgleich des [Anforderungskatalogs](ANFORDERUNGSKATALOG.md)
gegen den tatsächlichen Codestand (03.09.2026, Ground Truth `ff4c864`): für jede dort als
TEILWEISE geführte Anforderung wurde am Code geprüft, ob die Lücke noch besteht.

Neue offene Punkte werden ausschließlich hier aufgenommen. Erledigte Punkte bleiben
zur Nachvollziehbarkeit mit Erledigungsdatum und Verweis dokumentiert. Die
Detailplanung einzelner Arbeiten kann weiterhin in den verlinkten Quelldokumenten
stehen.

**Ergänzung 06.09.2026:** O-117–O-156 neu — Mainframe-/COBOL-Kompatibilität:
40 priorisierte Aufgaben für unterschiedliche Kundenumgebungen. Befunde,
Herstellerquellen, gemeinsame Abnahmeregeln und Reihenfolge stehen in
[MAINFRAME_KOMPATIBILITAET.md](MAINFRAME_KOMPATIBILITAET.md).

**Ergänzung 06.09.2026 (2):** O-082 erledigt — Confluence-Chunks tragen jetzt
`meta["section"]` in `metadata_json`, analog zu COBOLs `section`/`paragraph`.
Details bei O-082 selbst.

**Ergänzung 06.09.2026 (3):** O-083 erledigt — `CodeParser.chunk_file()`
schneidet für Confluence jetzt bevorzugt an Section-Grenzen statt nur nach
Zeichenzahl (`boundary_lines`-Parameter, gespeist aus O-082s `line_sections`).
Details bei O-083 selbst; O-084 (Winzling-Merge) bleibt offen und ist nach
O-083 der nächste sinnvolle Schritt, weil dichte Überschriftenfolgen jetzt
kleine Chunks erzeugen können. O-085 bleibt ebenfalls offen.

**Ergänzung O-063, 06.09.2026:** Typsicherheits-Nachzug abgeschlossen; auch Test- und E2E-Dateien enthalten keine expliziten `any`-Typen mehr. ESLint verhindert deren Wiedereinführung.

## Aktuell offen

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-001 | Performance / Abnahme | Formale Lasttest-Abnahme mit einem echten oder repräsentativen DRV-COBOL-Bestand durchführen. | Bestand bereitstellen, anschließend Persistenz- und Embedding-Durchsatz messen und abnehmen. Der synthetische Lauf ist nur ein Vorab-Signal. | Fujitsu/DRV: Bestand und Abnahmekriterien |
| O-002 | Barrierefreiheit | Formale BITV-Abnahme durchführen. | Verbindlichen Prüfumfang festlegen und manuellen Prüfschritt organisieren. Der automatisierte axe-core-Basischeck ist vorhanden. | Auftraggeber / Prüfstelle |
| O-003 | Design | Farbkontrast der Fujitsu-Farbtöne prüfen und gegebenenfalls nachbessern. | Markenfreigabe für zulässige Farbabweichungen einholen; danach zentrale Design-Tokens anpassen. | Markenverantwortliche |
| O-004 | Frontend-Architektur | Domänen-State aus `frontend/app/page.tsx` in eigene Hooks bzw. Context-Provider auslagern. | **Erledigt 02.09.2026:** `useProjects`, `useChatSessions`, `useKnowledgeSources` und `useWorkspaceLayout` extrahieren jeweils ihren State sowie die zugehörigen Lade-/Navigationslogiken; Typecheck, Lint, 18 Tests und Produktions-Build grün. | Keine externe Abhängigkeit |
| O-005 | Frontend-Performance | Nach O-004 `useCallback`/`memo` gezielt ergänzen. | **Erledigt 02.09.2026:** weitergereichte Navigations-, Session-, Chat-, Projekt- und Layout-Handler stabilisiert; Settings-Context sowie Header-Suche und Sidebar memoisiert. `tsc`, ESLint, 18 Tests und Produktions-Build grün. | O-004 |
| O-007 | Fachliche Anforderungen | Klären, ob CSV-Unterstützung aus Plan §13 / F-018 tatsächlich erforderlich ist. | Anforderung bestätigen oder als veraltet markieren; bei Bestätigung Umfang und betroffene Upload-/Connector-Flächen festlegen. | Fachliche Entscheidung |
| O-008 | Test-/Entwicklungsumgebung | Entscheidung treffen, ob Ollama in CI verbindlich getestet werden soll. | CI-Strategie festlegen: echter Ollama-Service, dedizierter optionaler Job oder bewusstes Ausnehmen mit dokumentierter Begründung. | Teamentscheidung / CI-Ressourcen |
| O-029 | Frontend-Layout | 3-Spalten-Layout (`3-col`) hat keinen ziehbaren Teiler. O-021 hat nur `split` (2 Panels, bestand schon vorher) und `4-grid` (neuer Kreuzgriff) resizable gemacht; `3-col` rendert in `WorkspaceShell.tsx` weiterhin starr zu gleichen Dritteln — keine der beiden Resize-Branches (`handleDividerMouseDown`/`handleGridResizePointerDown`) greift für diesen Layoutmodus. | **Erledigt 03.09.2026:** zwei ziehbare Teiler für `3-col` ergänzt. Neuer Zustand `threeColLeftPercent`/`threeColRightPercent` (Grenzen zwischen Panel 0/1 bzw. 1/2) in `useWorkspaceLayout`, neue Helper `pointerToPercent`/`clampPercentBetween` in `lib/workspaceResize.ts` (Mindestbreite je Spalte `MIN_THREE_COL_PANEL_PERCENT = 15`, da drei statt zwei Spalten dieselben 100% teilen). `WorkspaceShell.tsx` rendert die Teiler über eine gemeinsame `WorkspaceDivider`-Komponente (auch vom bestehenden `split`-Teiler genutzt). Snapshot-Speichern/-Wiederherstellen sowie `resetWorkspace` ziehen die neuen Werte mit. Neue Tests `useWorkspaceLayout.test.ts` (Drag-Verhalten inkl. Grenzfälle) und ergänzte Fälle in `workspaceResize.test.ts`; `tsc --noEmit`, ESLint, volle Vitest-Suite (43 Tests) und Produktions-Build grün. | O-021 (technische Basis vorhanden) |
| O-039 | Frontend / Workspace | Der Chat-Panel-Einklapp-Mechanismus (Chevron-Button "Chat einklappen", Platzhalter-Leiste "Chat ausklappen") ist ein Relikt aus der Zeit vor der vollständigen Panel-Schließen-/Wiedereröffnen-Funktion; seit ein Panel komplett geschlossen und über die Header-Bar ("Ansicht hinzufügen") wieder geöffnet werden kann, ist Einklappen redundant. | **Erledigt 03.09.2026:** Einklapp-Mechanismus vollständig entfernt — `collapsedPanels`-State, `togglePanelCollapse`, `isPanelCollapsed` aus `useWorkspaceLayout.ts` gestrichen; `PanelRenderer.tsx` rendert Panel-Inhalte jetzt immer direkt (keine Platzhalter-Darstellung, kein Collapse-Button mehr, `collapsed`/`onExpand`/`onCollapse`-Props entfernt); `WorkspaceShell.tsx` wendet Panel-Breiten jetzt bedingungslos an (kein `flex-none w-12`-Sonderfall mehr); Übersetzungsschlüssel `collapseChat`/`expandChat` aus `de.json`/`en.json` entfernt. Neuer Test `PanelRenderer.test.tsx` sichert ab, dass kein Collapse-Button mehr existiert und `collapsed` als Prop nicht mehr akzeptiert wird. `tsc --noEmit`, ESLint, volle Vitest-Suite (46 Tests) und Produktions-Build grün. | Keine |
| O-040 | Frontend / Branding | Kein Browser-Tab-Favicon gesetzt — `app/layout.tsx` hatte kein `<link rel="icon">`, kein `app/icon.*`-Next.js-Konventionsfile vorhanden (nur `public/condo-c.svg`/`condo-schriftzug.svg`, Relikte des ursprünglichen Condo/BIM-Templates, aus dem Doctus hervorging). | **Erledigt 03.09.2026:** neues `app/icon.svg` — dieselben Pfade wie `components/Logo.tsx::DoctusIcon` ("strukturelles D"), Farben aus den Theme-CSS-Vars (`--ds-accent`/`--ds-background`/`--ds-on-accent`, Dark-Theme-Werte, da das App-Standard-Theme) als feste Hex-Werte übernommen — eine statische Favicon-Datei hat keinen Zugriff auf die CSS Custom Properties der Seite. Next.js erkennt die Datei automatisch über die `app/icon.*`-Konvention und setzt `<link rel="icon">` selbst; keine Änderung an `layout.tsx` nötig. Verifiziert: `next build` listet `/icon.svg` als eigene statische Route, `curl` gegen den ausgerollten Container liefert `200 image/svg+xml` und das gerenderte HTML enthält den `<link rel="icon" ...>`-Tag. **Folgefix noch am selben Tag:** trotz korrektem Server-Response blieb das Icon in Chrome unsichtbar (auch im Inkognito-Modus, schloss Browser-Caching aus) — Ursache war ungültiges XML im erklärenden SVG-Kommentar (doppelte Bindestriche wie `--ds-accent`, laut XML-Spezifikation innerhalb von Kommentaren verboten); Chrome parst ein direkt als Favicon geladenes SVG als striktes XML statt mit dem nachsichtigen HTML-Tokenizer und verwirft die ganze Datei bei einem solchen Fehler. Kommentar umformuliert (Gedankenstrich statt doppeltem Bindestrich); mit `xml.etree.ElementTree` gegen die ausgelieferte Datei verifiziert. | Keine |
| O-038 | Frontend / Chat-Sessions | Eine Chat-Session sollte nicht mehr nur mit der ersten Chat-Nachricht beginnen können, sondern auch, sobald eine zweite View geöffnet wird (z. B. Graph-View + Code-View), selbst wenn der Chat nie benutzt wurde. Vorher war ein Befund, der nur über mehrere Views ohne Chat-Nutzung entsteht, nicht teilbar oder konservierbar, weil ohne Chat-Nachricht keine Session existierte. | **Erledigt 03.09.2026:** neuer Endpunkt `POST /chat/sessions` (`ChatSessionCreate`-Schema: `title`, optional `project_id`/`source_id`/`snapshot`) legt eine Sitzung ohne Chat-Nachricht an — Gegenstück zur impliziten Erzeugung in `POST /chat`, die den Titel aus der ersten Nachricht ableitet. Frontend: neues Speicher-Icon in der Header-Bar (`GlobalSearch.tsx`), sichtbar nur wenn `chatMessages.length === 0 && panelConfigs.length >= 2`; Klick öffnet `SaveSessionDialog.tsx` (Namenseingabe, eigene schlanke Modal-Komponente statt Dialog-Library). Neuer Handler `useChatController.ts::handleSaveSessionWithoutChat` ruft den Endpunkt auf, reiht die neue Session vorne in die Sidebar-Liste ein und setzt `activeSessionId` — der bereits bestehende debounced Workspace-Snapshot-Autosave-Effekt (`useWorkspaceLayout.ts`, reagiert auf `activeSessionId`-Änderungen) übernimmt danach automatisch das Schreiben des Snapshots über den bestehenden `PATCH .../snapshot`-Pfad, ohne eine zweite Snapshot-Bau-Stelle zu brauchen. Neue Tests: 8 Backend-Fälle in `test_chat_sessions.py` (Erzeugung, leerer Titel → 400, unbekanntes Projekt/Quelle → 404, Sichtbarkeit, Auth-Pflicht), 3 Frontend-Fälle in `useChatController.test.tsx` sowie 3 in `GlobalSearch.test.tsx` (Sichtbarkeit des Icons, Dialog-Öffnen, getrimmter Titel wird übergeben). Backend-Suite 143/146 grün (die 3 Fehlschläge vorbestehend, siehe O-037), volle Vitest-Suite 52 Tests grün, `tsc --noEmit`, ESLint und Produktions-Build grün. **Folgefix noch am selben Tag** (Nutzer-Feedback nach erstem Test): zwei Konsistenzlücken behoben — (1) erneutes Klicken auf das Speicher-Icon bei bereits aktiver chat-loser Sitzung legte eine zweite Sitzung an, statt die bestehende zu aktualisieren; Button wechselt jetzt auf einen `onUpdateSessionSnapshot`-Pfad ohne Dialog, sobald `activeSessionId` schon gesetzt ist (Tooltip wechselt entsprechend auf „Sitzung aktualisieren"). (2) Der lokale Sidebar-Cache (`sessions`-State) wurde nach einem erfolgreichen `PATCH .../snapshot` nie mit dem tatsächlich gespeicherten `snapshot_json` synchronisiert — dieselbe Sitzung erneut anzuklicken stellte dadurch einen veralteten oder leeren Panel-Zustand wieder her (z. B. Graph-View verschwand, nur Chat blieb offen). `useWorkspaceLayout.ts`s debounced Autosave-Effekt sowie beide neuen Handler (`handleSaveSessionWithoutChat`, `handleUpdateSessionSnapshot`) schreiben den gespeicherten Snapshot jetzt zusätzlich in den lokalen Cache zurück; die Erstspeicherung wartet zudem nicht mehr auf den 1,2s-Debounce, sondern schreibt sofort. Neue Tests: 2 in `useWorkspaceLayout.test.ts` (Cache-Sync nach Debounce, kein Fehler ohne `setSessions`), 4 weitere in `useChatController.test.tsx`, 1 weiterer in `GlobalSearch.test.tsx` — volle Vitest-Suite jetzt 59 Tests grün, `tsc --noEmit`, ESLint und Produktions-Build grün. | Bestehender Chat-Session-/Sidebar-Mechanismus |
| O-030 | Backend / Ingestion | `POST /knowledge-sources/upload` (`backend/api/knowledge_sources.py`) hat keine serverseitige Extension-Allowlist — nur die UI begrenzt die Dateiauswahl auf `.pdf`/`.md`/`.txt`; ein direkter API-Call nimmt jede Datei entgegen und schickt sie in die Verarbeitungs-Pipeline. | **Erledigt 03.09.2026:** serverseitige Allowlist `_ALLOWED_UPLOAD_EXTENSIONS` (`.pdf`/`.md`/`.txt`, synchron zur UI-`accept`-Liste) prüft die Dateiendung vor dem Anlegen der Knowledge-Source und lehnt Abweichungen mit HTTP 400 ab. Neuer Testfall `test_upload_local_document_rejects_disallowed_extension`; bestehender Traversal-Test auf eine erlaubte Endung (`passwd.txt`) umgestellt, da er zuvor ohne Endung lief. | Keine |
| O-031 | Backend / Ingestion | Lokaler Upload-Pfad (`parser/tasks/document.py::process_local_document_async`) hat keinen OCR-Fallback für Bild-PDFs. Folder-/WebDAV-Connectoren teilen sich dafür bereits `connectors/folder.py::_extract_text` (inkl. OCR); `document.py` extrahiert PDF-Text separat und dupliziert die Logik ohne OCR. | **Erledigt 03.09.2026:** neue geteilte Funktion `connectors/folder.py::extract_pdf_pages` (seitenweise Extraktion inkl. OCR-Fallback bei fehlendem Text-Layer, Seitenzuordnung geht dabei auf `page_no=None` zurück). `_extract_text` (Folder-/WebDAV-Pfad) baut jetzt darauf auf; `document.py` ruft dieselbe Funktion statt eigener `pypdf`-Logik auf und erhält damit den OCR-Fallback für lokale Uploads. Neuer Test `test_pdf_extraction.py` (Seitenextraktion, OCR-Fallback bei fehlendem Text-Layer über `_extract_text` und `extract_pdf_pages`, sowie Integrationstest für `process_local_document_async` mit `page=None`-Metadaten). Volle Parser-Suite 179 von 180 grün, der eine Fehlschlag in `test_ap4_persistence.py` ist vorbestehend und auf unverändertem Code reproduzierbar. Parser-Worker neu gebaut und ausgerollt. | Keine |
| O-032 | Backend / Sicherheit | Chat-Share-Mechanik war schwächer geschützt als beabsichtigt: `GET /chat/sessions/by-uuid/{uuid}` und `.../messages` prüften `ChatSession.is_public` nicht und verlangten keine Authentifizierung — jede Session war über ihre UUID lesbar, auch wenn sie nie geteilt wurde. `PATCH /chat/sessions/{id}/snapshot` und `PATCH /chat/messages/{id}/feedback` waren zusätzlich über eine fortlaufende Integer-`session_id`/`message_id` statt UUID adressiert und (Snapshot komplett, Feedback ohne Owner-/Public-Prüfung) ohne echte Zugriffskontrolle erreichbar. | **Erledigt 03.09.2026 (`de2c9c1`):** neue Zugriffsregel `owner_id == user.id or is_public` (`_session_accessible` in `backend/api/chat.py`) jetzt einheitlich durchgesetzt auf den `by-uuid`-Leserouten, dem Snapshot-/Feedback-Update, dem `/chat`-Fortsetzen einer fremden `session_id` sowie zusätzlich auf `GET /chat/sessions/{id}/messages` (dieselbe Lücke — sequentielle ID, bislang komplett ohne Auth/Owner-Check, war im Audit nicht extra erfasst, aber derselbe Fall). Da nichts `is_public` je auf `True` setzte, war die Freigabe-Funktion faktisch nie benutzbar (siehe DOC-F-070); neuer Endpunkt `POST /chat/sessions/{id}/share` (Owner-only) setzt es jetzt explizit beim Klick auf „Chat teilen" (`useChatController.ts::handleShareChat`). 8 neue Testfälle in `backend/tests/test_chat_sessions.py` (15 gesamt grün), `tsc --noEmit` grün, Backend+Frontend neu gebaut und ausgerollt. | Keine |
| O-033 | Backend / Verschlüsselung | `ChatMessage.metadata_json` (u. a. `refs`) und `feedback` liegen unverschlüsselt in der DB; nur `content` ist als `EncryptedString` modelliert. | Fachlich klären, ob Metadaten/Refs schutzwürdige Auszüge enthalten können; falls ja, Verschlüsselung erweitern. | Fachliche Einschätzung nötig |
| O-034 | Backend / Knowledge Graph | Der Knowledge-Graph-Endpunkt (`backend/api/graph.py`) bietet nur `/export/neo4j` (Cypher); anders als der Callgraph (JSON/CSV/GraphML) gibt es keinen neutralen CSV-/GraphML-Export. | **Erledigt 03.09.2026:** neuer Endpunkt `GET /graph/export?format=csv\|graphml` (analog zu `/callgraph/export`) — ruft intern dieselbe `get_graph()` auf wie `GET /graph`, erbt damit dieselbe Sichtbarkeitsprüfung. CSV mit Spalten `source,target,link_type,score,context`; GraphML mit Knoten-Label/Typ und Kanten-`link_type`/`score`. Frontend (`LayoutSettingsTab.tsx`) bekommt zwei weitere Export-Buttons neben dem bestehenden Neo4j-Cypher-Export. Neue Backend-Tests `test_graph_export.py` (4 Fälle: CSV-Inhalt, GraphML-Inhalt, unbekanntes Format → 422, Sichtbarkeitsprüfung → 404). Volle Backend-Suite 135/138 grün (die 3 Fehlschläge in `test_chat_llm_provider_guard.py` sind vorbestehend, `ALLOW_CLOUD_LLM=true` in dieser lokalen Compose-Umgebung, siehe O-037). `tsc --noEmit`, ESLint, volle Vitest-Suite (46 Tests) und Produktions-Build grün. Legacy-Neo4j-Endpunkt bewusst nicht entfernt (weiterhin in Nutzung, keine Migrationsentscheidung getroffen). | Keine |
| O-035 | Backend / LLM-Konfiguration | `POST /model-info` (`backend/api/system.py`) verändert weiterhin `cfg.OLLAMA_LLM_MODEL` global im laufenden Prozess statt request-/profilgebunden zu bleiben. | Modellwahl request- bzw. profilgebunden umsetzen, falls Mehrmandantenbetrieb mit unterschiedlichen lokalen Modellen relevant wird. | Fachliche Priorität klären |
| O-036 | Frontend / Performance | Datei-/Entitätslisten in der Seitenleiste sind bei sehr großen Beständen nicht virtualisiert — die volle Liste wird gerendert, keine Virtualisierungsbibliothek im Frontend vorhanden. | **Erledigt 05.09.2026:** `@tanstack/react-virtual` (MIT) ergänzt. Die beiden potenziell unbeschränkt großen Listen der Seitenleiste sind jetzt gefenstert: Chat-Verlauf (neue Komponente `components/sidebar/VirtualizedSessionList.tsx`) und der Datei-Baum einer aufgeklappten Wissensquelle (`components/sidebar/FileTreeList.tsx`). Der rekursive Baum lässt sich nicht direkt fenstern — neue reine Hilfsfunktion `lib/sidebarFileTree.ts::flattenVisibleFileTree` flacht ihn vorher auf die aktuell sichtbaren Zeilen ab (Kinder eingeklappter Ordner fehlen dort), danach rendert der Virtualizer nur noch die paar Zeilen im sichtbaren Ausschnitt statt aller. Die gepinnten Quellen selbst bleiben unvirtualisiert (`.slice(0, 4)`-gedeckelt, kein Skalierungsproblem). `renderSourceTreeNode`/`buildFileTree` aus `Sidebar.tsx` entfernt (in die neuen Module verschoben), Sidebar.tsx dadurch schlanker. Neue Tests: `lib/sidebarFileTree.test.ts` (6 reine Fälle für Baumaufbau/Abflachung/Kollaps), `VirtualizedSessionList.test.tsx` und `FileTreeList.test.tsx` (je 5-6 Fälle inkl. Nachweis, dass bei 500 Sitzungen bzw. 2000 Dateien nur ein Bruchteil davon im DOM landet). Volle Vitest-Suite (80 Tests), `tsc --noEmit`, ESLint und Produktions-Build grün, Frontend-Container neu gebaut und ausgerollt. | Keine |
| O-037 | Backend / Ingestion | Bei O-030 entdeckt: `POST /knowledge-sources/upload` liefert bei Erfolg einen leeren Response-Body (`{}`) statt der neuen Knowledge-Source. Ursache: `send_tracked_task()` committet nach dem `return`-relevanten letzten Attributzugriff erneut (`record.celery_task_id = task_id; db.commit()`), SQLAlchemy expired danach alle Instanzattribute (`expire_on_commit=True`, Default), und der Endpunkt gibt das ORM-Objekt direkt zurück statt es zu serialisieren — `jsonable_encoder` sieht dadurch ein leeres `__dict__`. Das Frontend (`SourcesSetupTab.tsx::handleFileUpload`) hängt dieses leere Objekt direkt an die Quellenliste an. Zusätzlich rot in der CI (unabhängig davon): `PermissionError: [Errno 13] Permission denied: '/repos'` beim lokalen Datei-Upload-Test — der CI-Runner hat keinen Schreibzugriff auf `UPLOADS_DIR`. Beide Fehlschläge sind vorbestehend (reproduzierbar mit unverändertem `ff4c864`, nicht durch O-030 verursacht) und lassen `test_upload_local_document_creates_source_and_saves_file` sowie `test_upload_local_document_sanitizes_path_traversal_in_filename` weiterhin fehlschlagen. | **Erledigt 03.09.2026 (`d798e3d`):** `upload_local_document` gibt jetzt `serialize_source(db_source)` zurück statt des rohen ORM-Objekts — identisches Muster zu `create_knowledge_source`/`create_git_source`/`create_folder_watch_source`. CI-Workflow (`backend`-Job) legt `/repos/uploads` vor dem Testlauf an und übergibt es dem Runner-User, da dieser Job bare auf `ubuntu-latest` läuft (kein Docker-Compose-Mount für `/repos`). Beide Upload-Tests lokal im neu gebauten `backend-api`-Container grün (`131 passed`; die 3 unabhängig fehlschlagenden `test_chat_llm_provider_guard.py`-Fälle liegen an `ALLOW_CLOUD_LLM=true` in dieser lokalen Compose-Umgebung, nicht an dieser Änderung). Backend neu gebaut und ausgerollt. | Keine |

O-041 bis O-051 stammen aus einer Produktreife-/Vermarktungs-Analyse vom 05.09.2026
(„was fehlt, um Doctus als Produkt zu vermarkten, insbesondere Konnektoren"), gegen
den tatsächlichen Codestand geprüft (nicht nur gegen Doku).

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-041 | Sicherheit / Authentifizierung | Login war ein reines lokales Passwort-Verfahren (`backend/api/auth.py`, Argon2id) — im Backend existierte kein OIDC-/SAML-/LDAP-Code, obwohl `docs/deployment-customer.md` bereits eine Anleitung beschrieb, beim Kunden Issuer-URL/Client-ID/Client-Secret für einen OIDC-Login abzufragen. | **Erledigt 05.09.2026 (`103bf62`):** OpenID-Connect-Authorization-Code-Flow als zweiter Anmeldeweg neben dem lokalen (`backend/core/oidc.py`), aktiv sobald `OIDC_ISSUER`/`OIDC_CLIENT_ID`/`OIDC_CLIENT_SECRET` gesetzt sind (`core/config.py::oidc_enabled()`). ID-Token-Prüfung (Signatur über JWKS, Issuer, Audience, Ablauf, Nonce, explizit `algorithms=["RS256"]` gegen Alg-Confusion) über `joserfc`. JIT-Provisioning ausschließlich über den `sub`-Claim, kein automatisches Verknüpfen über die E-Mail-Adresse (Design-Entscheidungen im Detail: `docs/ENTSCHEIDUNGEN.md` E-12). Migration `0007_user_oidc_subject`: `password_hash` nullable für SSO-Konten, neue Spalte `oidc_subject`. `POST /users/{id}/reset-password` lehnt SSO-Konten ab. `GET /config/features` liefert `auth.ssoEnabled`, `LoginView.tsx` zeigt darauf gestützt einen SSO-Button. Getestet ohne echten Kunden-IdP: `backend/tests/test_oidc.py` signiert eigene ID-Tokens mit einem generierten RSA-Schlüsselpaar und verifiziert sie über ein eigenes JWKS (20 Fälle), `test_auth.py`/`test_users_admin.py` decken die Router-Verdrahtung und die Reset-Password-Sperre ab, `LoginView.test.tsx` die Button-Sichtbarkeit und Fehleranzeige. Backend-Suite 164 grün (+12 vorbestehende, unabhängige Fehlschläge), Parser-Suite 179 grün (+1 vorbestehender Fehlschlag), Frontend 64 Tests grün, alle sieben Docker-Services neu gebaut und ausgerollt. `docs/deployment-customer.md` beschrieb den Kunden-Ablauf konzeptionell bereits richtig. **Nachtrag 06.09.2026:** auf Ebene der Variablennamen, der Redirect-URI und des Callback-Pfads stimmte die Doku aber nicht — nachgezogen unter [O-159]. | Keine |
| O-042 | Konnektoren | Kein Konnektor für mainframe-native Quellcode-Verwaltung (Endevor, ChangeMan, Micro Focus/PVCS, IBM RTC/CMVC) oder direkten Zugriff auf z/OS-PDS-Datasets. `parser/connectors/git.py` deckt nur Git-Bestände ab (providerneutraler Bare-Mirror-Clone, funktioniert daher zwar mit GitHub/GitLab/Azure Repos, nicht nur Bitbucket — aber eben nur mit Git). COBOL-Bestände, die noch nicht nach Git migriert sind, lassen sich damit gar nicht anbinden. **05.09.2026 verfeinert:** Am weitesten verbreitet sind laut allgemeinem Branchenwissen (keine für Doctus verifizierte Marktstudie) CA/Broadcom Endevor (Marktführer, v. a. Großkonzerne) und ChangeMan ZMF (OpenText, zweitgrößter Player, stark in Banken/Versicherungen); daneben verwalten ältere Umgebungen COBOL-Code teils direkt als z/OS-PDS-Member ohne kommerzielles SCM. **Vor jedem Codieren zu klären:** Setzt der Zielkunde (Endevor-Fall) zusätzlich "**Endevor Bridge for Git**" ein? Falls ja, synchronisiert Broadcom die Endevor-Elemente bereits automatisch in ein Git-Repo — dafür würde der **bestehende** `GitConnector` vermutlich ausreichen, kein neuer Konnektor nötig. Falls nein (klassischer Endevor-Zugriff über ISPF/Batch-SCL, kein Git-Bridge), bräuchte ein nativer Konnektor Zugriff auf Endevors REST-Schnittstelle ("Endevor Web Services") — technisch anschlussfähig über das bestehende Konnektor-Muster (`BaseConnector`, nur `fetch_documents()` zu implementieren, Parsing/Graph/Embedding bleiben unverändert), aber ohne echten Endevor-Testzugang nicht verifizierbar; der naheliegende Zowe-CLI-Weg scheidet ohnehin aus (EPL-2.0-lizenziert, verstößt gegen CLAUDE.md Prinzip 2). | Mit Vertrieb/Kunden zuerst klären, ob "Endevor Bridge for Git" im Einsatz ist (s. o.) — je nach Antwort ist entweder gar kein neuer Konnektor nötig, oder ein nativer Endevor-REST-Konnektor mit dediziertem Testzugang. Erst danach Konnektor-Architektur umsetzen. | Antwort von Vertrieb/Kunde zu Endevor Bridge for Git; bei nativer Anbindung zusätzlich ein Endevor-Testzugang für Verifikation |
| O-043 | Konnektoren / Fachlichkeit | Kein DB2-Katalog-/Schema-Import. Eingebettetes SQL in COBOL-Programmen bleibt dadurch teilweise unaufgelöst (laut Bestandsauswertung ca. 24 von 473 USES-Kanten „unresolved", vermutlich SQL-Restfälle). Ein Konnektor, der Tabellen-/Spaltenschema aus dem DB2-Katalog zieht, würde diese Kanten auflösbar machen und wäre ein fachliches Alleinstellungsmerkmal gegenüber generischen Code-Assistenten. | Scope klären (nur Schema-Metadaten vs. auch Laufzeitdaten), dann Konnektor + Erweiterung des Edge-Resolvers umsetzen. | Fachliche Priorisierung nötig |
| O-044 | Backend / Ingestion | Direkter Datei-Upload (`POST /knowledge-sources/upload`, `_ALLOWED_UPLOAD_EXTENSIONS` in `backend/api/knowledge_sources.py`) erlaubt serverseitig nur `.pdf/.md/.txt`, während Ordner-Watch- und WebDAV-Connector (`SUPPORTED_EXTENSIONS` in `parser/connectors/folder.py`/`webdav.py`) zusätzlich `.docx/.doc` extrahieren können. Inkonsistentes Verhalten je nach Zugangsweg zur selben Datenquelle. | **Erledigt 05.09.2026:** `_ALLOWED_UPLOAD_EXTENSIONS` um `.docx`/`.doc` erweitert (deckungsgleich mit `parser/connectors/folder.py::SUPPORTED_EXTENSIONS`); UI-`accept`-Liste (`SourcesSetupTab.tsx`) und `dropzoneHint`-Übersetzung (`de.json`/`en.json`) mitgezogen. Zusätzlich Duplikat wie bei O-031 beseitigt: neue geteilte Funktion `connectors/folder.py::extract_docx_text` (Absätze + Tabellenzellen), `_extract_text` (Folder-/WebDAV-Pfad) und `tasks/document.py::process_local_document_async` (lokaler Upload) rufen jetzt dieselbe Funktion statt zweier unabhängiger python-docx-Schleifen. Neue Tests: `test_docx_extraction.py` (6 Unit-/Integrationsfälle, Muster analog zu `test_pdf_extraction.py`/O-031), 1 neuer Backend-Fall `test_upload_local_document_accepts_docx`. Backend-Suite 174/177 grün (3 vorbestehende Fehlschläge in `test_chat_llm_provider_guard.py`, siehe O-037), Parser-Suite 201/213 grün (12 vorbestehende, umgebungsbedingte `[trio]`-Fehlschläge, mit unverändertem `09a1f39` reproduziert). `tsc --noEmit`, ESLint, volle Vitest-Suite (64 Tests) und Produktions-Build grün. | Keine |
| O-045 | Konnektoren | Kein SharePoint-/OneDrive-Konnektor. In vielen Unternehmen liegt Fachdokumentation dort statt in Confluence. | Bedarf beim Zielkunden klären, dann Konnektor analog Confluence/WebDAV umsetzen (Microsoft Graph API). | Fachliche Priorisierung nötig |
| O-046 | Konnektoren | Kein generischer S3-kompatibler Objektspeicher-Konnektor (AWS S3/MinIO). | Bedarf klären — relevant, falls Kunden Doku/Exports bereits in einem S3-Bucket statt Dateisystem/WebDAV halten. | Fachliche Priorisierung nötig |
| O-047 | Konnektoren | Kein Postfach-/E-Mail-Konnektor (IMAP/Exchange) für Fachkorrespondenz oder Anforderungsmails als Wissensquelle. | Bedarf klären, sonst nicht priorisieren. | Fachliche Priorisierung nötig |
| O-048 | Konnektoren | Außer Jira ist kein weiteres ITSM-/Ticket-System angebunden (z. B. ServiceNow, Azure DevOps Boards). | Bedarf beim Zielkunden klären. | Fachliche Priorisierung nötig |
| O-049 | Konnektoren / Chat | Keine Slack-/Teams-Integration für den Chat-Assistenten — Nutzung ist auf die Doctus-Weboberfläche beschränkt. | Bedarf klären (Assistent im Arbeitsalltag vs. reines Recherchewerkzeug), dann Bot-Integration umsetzen. | Fachliche Priorisierung nötig |
| O-050 | Vertrieb / Compliance | Keine Enterprise-Vertrauensnachweise vorhanden: kein Pentest-Bericht, kein Security-Whitepaper, keine ISO-27001-/BSI-Grundschutz-Aussage. Bei Konzern-/Behörden-Beschaffung häufig hartes Ausschlusskriterium in der Ausschreibung, unabhängig vom tatsächlichen Codezustand. | Mit Vertrieb klären, welche Nachweise für die anvisierten Ausschreibungen nötig sind; ggf. externen Pentest beauftragen und Whitepaper verfassen. | Auftraggeber-/Vertriebsentscheidung |
| O-051 | Betrieb / Paketierung | Installation ist „git clone + `.env` von Hand editieren" (siehe `docs/deployment-customer.md`) — kein Installer/Onboarding-Wizard, keine Trial-Version, kein dokumentiertes Update-/Migrationsverfahren zwischen Versionen für laufende Kundeninstallationen, kein Lizenz-/Preismodell. Skaliert aktuell nicht über eine Vor-Ort-Installation durch eine technisch versierte Person hinaus. | Priorisieren, sobald mehr als vereinzelte Pilotkunden geplant sind: Installer-Skript/Wizard, Upgrade-Pfad (Migrationen + Doku), Lizenzmodell festlegen. | Geschäftsentscheidung, wann Skalierung über Einzelinstallationen hinaus ansteht |
| O-052 | Frontend / CI | Bei O-044 entdeckt: `npx tsc --noEmit` schlug auf unverändertem `develop` (`09a1f39`) bereits fehl — `frontend/components/LoginView.test.tsx` reassignt `window.location` per `delete window.location; window.location = ...`, was mit dem aktuellen TypeScript/lib.dom-Typenstand nicht mehr durchgeht (`window.location` ist dort nur noch ein get-only Accessor ohne Setter). Der CI-Schritt „Type-check" auf `develop` war dadurch vermutlich bereits rot. | **Erledigt 05.09.2026:** beide Stellen auf `Object.defineProperty(window, 'location', { configurable: true, value: ... })` umgestellt statt delete+Reassignment; keine `@ts-expect-error`-Direktiven mehr nötig. `tsc --noEmit` und die 5 betroffenen Vitest-Fälle grün, volle Vitest-Suite (64 Tests) weiterhin grün. | Keine |
| O-053 | Backend+Frontend / Knowledge Graph | `GET /graph` (Knowledge-Graph-Übersicht, `backend/api/graph.py`) lud jede sichtbare Code-Entity und jeden Dokument-Chunk unbegrenzt — bei einem großen COBOL-Bestand (Ziel-Skalierung laut CLAUDE.md Prinzip 4) sowohl ein Server- (Antwortgröße/Ladezeit) als auch ein Client-Risiko (d3-force-Kraftsimulation im Browser-Hauptthread, am Ende nur ein unlesbarer "Wollknäuel"). Anders als der Call Graph (`/callgraph/focus`, immer auf eine begrenzte Ein-Hop-Nachbarschaft beschränkt) hatte die Knowledge-Graph-Übersicht kein Sicherheitsnetz. Zusätzlicher Fund dabei: der Endpunkt `GET /graph/focus` — strukturell exakt dasselbe Ein-Hop-Nachbarschaftsmuster wie beim Call Graph, offenbar genau für dieses Problem gebaut — wurde von keiner Stelle im Frontend aufgerufen (totes Code-Fragment). | **Erledigt 05.09.2026:** neuer Deckel `KNOWLEDGE_GRAPH_OVERVIEW_MAX_NODES` (Default 2000, env-konfigurierbar, `core/config.py`) in `get_graph()` — analog zu callgraph.py's `MAX_NODES` (F-066), aber mit höherem Wert, da die Übersicht bewusst auch unverlinkte Entities zeigt (Inventarcharakter). Beim Kappen bleiben die am dichtesten verlinkten Knoten zuerst erhalten (Degree-Sortierung), Kanten mit einem weggefallenen Endpunkt werden mitentfernt; Antwort trägt neu `truncated`/`total_nodes`/`total_edges`. `GET /graph/focus` jetzt tatsächlich ans Frontend angebunden: neuer Button „Nur Nachbarschaft laden" (`KnowledgeGraphView.tsx`) lädt bei einem Entity-Knoten dessen echte, ungekappte Nachbarschaft direkt aus der DB (anders als der bestehende, rein visuelle Soft-Focus, der nur das bereits geladene — ggf. gekappte — Übersichtsmaterial abdunkelt); Kapp-Hinweis-Banner in der Übersicht, „Zurück zur Übersicht"-Chip im Nachbarschafts-Modus. Bekannte Grenze: „Nur Nachbarschaft laden" braucht einen Projekt-Kontext (`/graph/focus` verlangt `project_id`), im „Allgemein"-Kontext für projektlose Entities daher nicht verfügbar. Nebenbei bei den neuen Frontend-Tests entdeckt und behoben (O-054, siehe dort). Neue Tests: 3 Backend-Fälle (`test_graph_overview_truncation.py`: unterhalb des Deckels unverändert, Kappen mit korrekten Gesamtzahlen, keine hängenden Kanten nach dem Kappen), 4 Frontend-Fälle (`KnowledgeGraphView.test.tsx`: Kapp-Banner erscheint/bleibt aus, `/graph/focus`-Aufruf mit korrekten Parametern, Rücksprung zur Übersicht). Backend-Suite 177/180 grün (3 vorbestehende Fehlschläge, siehe O-037), `tsc --noEmit`, ESLint, volle Vitest-Suite (84 Tests) und Produktions-Build grün. Backend+Frontend neu gebaut und ausgerollt. | Keine |
| O-054 | Frontend / Codequalität | Bei den O-053-Tests entdeckt: `KnowledgeGraphView`s Default-Parameter `projectEntities = []` erzeugt bei jedem Aufruf ohne diese Prop eine neue Array-Referenz — die Render-Zeit-State-Anpassung weiter unten (`prevFocusDeps.projectEntities !== projectEntities`, Zeile ~396) vergleicht genau darauf und wurde dadurch bei jedem einzelnen Render erneut ausgelöst: eine waschechte Endlosschleife. Bislang folgenlos, weil beide tatsächlichen Aufrufer (`app/page.tsx`, `PanelContentRenderer.tsx`) die Prop immer explizit mitgeben — ein künftiger Aufrufer ohne diese Prop hätte die Ansicht aber sofort eingefroren. | **Erledigt 05.09.2026:** modulweite stabile Konstante `EMPTY_PROJECT_ENTITIES` statt eines Inline-`[]`-Literals als Default. | Keine |

O-055 bis O-062 stammen aus einem Testabdeckungs-Audit vom 05.09.2026: Backend
(163 Testfälle/23 Dateien) und Parser (180/31) sind gut abgedeckt, insbesondere
der COBOL-Golden-Corpus (F-033) und sicherheitsrelevante Backend-Pfade. Beim
Frontend (84 Testfälle/18 Dateien) zeigt eine Stichprobe eine klare Lücke: die
größten, interaktionsreichsten Komponenten haben keinen einzigen Test —
`KnowledgeGraphView.tsx` bekam mit O-053 heute den ersten überhaupt (und
dabei sofort einen echten Endlosschleifen-Bug zutage gefördert, O-054). Kein
Nachlässigkeits-Problem — Tests werden seit 03.09.2026 bei jeder Änderung
mitgeschrieben, aber unberührter Bestandscode bleibt dadurch strukturell
ungetestet, solange ihn niemand aus anderem Anlass anfasst. Diese Punkte
bauen die Lücke gezielt, in Häppchen pro Komponente ab, statt auf den
nächsten zufälligen Berührungspunkt zu warten. Reihenfolge nach Produkt-
Kritikalität und Komplexität, nicht nach ID.

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-055 | Frontend / Tests | `ChatView.tsx` — die eigentliche Chat-Oberfläche, das Kernprodukt — hat keinen einzigen Test. | **Erledigt 05.09.2026:** neue `ChatView.test.tsx` (22 Fälle). Da `ChatView` selbst zustandslos ist (chatMessages/currentMessage/isLoading kommen als Props aus `useChatController`), deckt "Senden" hier die Handler-Verdrahtung ab (Klick/Enter → `handleSendChat`, Shift+Enter sendet nicht, Deaktivierung bei leerer Nachricht/während des Ladens), nicht einen echten Netzwerk-Request. Abgedeckt: leerer Zustand (Vorschlagskarten, Onboarding-Karte ohne Projekt deaktiviert, "clarify"-Vorschläge befüllen das Eingabefeld und setzen einen Assistenten-Hinweis), Fehlerzustand (fehlgeschlagenes Begrüßungs-Statement fällt auf einen der vier lokalen Fallback-Sätze zurück), Nachrichten-Rendering inkl. Markdown/Codeblock, Refs-Badges einer Nutzernachricht, referenzierte Quellen und Modell-Badge einer Antwort, Streaming-Anzeige (Ladeindikator ohne Inhalt, "Agent arbeitet..." statt Aktionsleiste während des Streams, Aktionsleiste nach Abschluss), Retry/Feedback-Buttons (inkl. Sperre ohne Nachrichten-ID bzw. während `isLoading`) sowie die Kontext-Chips (Projekt/Pin) samt Aufheben-Buttons. `api.getTypingStatement` gemockt, kein `next/image`-Mock nötig (rendert in jsdom unverändert). `tsc --noEmit`, ESLint, volle Vitest-Suite (106 Tests) und Produktions-Build grün. | Keine |
| O-056 | Frontend / Tests | `Sidebar.tsx` hat nach dem O-036-Umbau nur für die neuen Unterkomponenten (`VirtualizedSessionList`, `FileTreeList`, `sidebarFileTree`) Tests — die eigene Orchestrierung (Ordner-Akkordeon `expandedFolderId`, gepinnte-Quellen-Filterung nach Projektkontext, Sidebar-Resize, Mobile-Schließen-bei-Auswahl) ist ungetestet. | **Erledigt 06.09.2026:** neue `Sidebar.test.tsx` (31 Fälle) deckt genau die vier in O-056 genannten Bereiche ab: (1) Ordner-Akkordeon — Aufklappen lädt die Dateiliste über `api.getKnowledgeSourceFiles`, erneutes Aufklappen bedient sich am `sourceFiles`-Cache statt neu zu laden, Ladeindikator, Leer-Hinweis, immer nur eine Quelle gleichzeitig offen, lokale Quellen werden direkt geöffnet statt aufgeklappt, fehlgeschlagener Abruf stürzt nicht ab; (2) Filterung der gepinnten Quellen nach Projektkontext — nur angepinnte, ohne Projekt keine projektgebundenen, im Projekt die eigenen plus die globalen (aber keine fremden), „Global"-Badge nur im Projektkontext, Deckelung auf vier; (3) Sidebar-Resize — Standardbreite, Übernahme der Zeigerposition, Grenzen 180/500, Ende beim Loslassen, kein Ziehgriff bei geschlossener Sidebar; (4) Schließen bei Auswahl auf Mobilbreite (`window.innerWidth < 768`) für Neuer Chat, Sitzung, Baumdatei und lokale Quelle, inkl. Gegenprobe auf Desktopbreite. Dazu Verlaufs-Filterung/-Zähler je Projektkontext, Akkordeon-Einklappen und die Fußzeile (Nutzerdaten, Fallbacks, Abmelden). Beide Sidebar-Listen rendern gefenstert und messen `offsetHeight`, das jsdom immer als 0 meldet — wie in `VirtualizedSessionList.test.tsx` wird es deshalb per Spy auf einen festen Wert gesetzt, sonst rendert der Virtualizer keine Zeile. Gegenprobe: drei gezielte Mutationen am Bauteil (Projektfilter entfernt, `slice(0, 4)` aufgeweitet, Mobil-Schwelle auf 0) lassen 7 der neuen Tests fehlschlagen. `tsc --noEmit`, ESLint, volle Vitest-Suite (137 Tests) und Produktions-Build grün. Kein Container-Rebuild nötig — reine Testdatei, nicht Teil des Produktions-Bundles. | Keine |
| O-057 | Frontend / Tests | `WorkspaceShell.tsx` (Split-/3-col-/4-grid-Rendering, Panel-Breiten-Anwendung, Teiler-Sichtbarkeit je Layoutmodus) hat keinen Test — nur die zugrunde liegende Resize-Arithmetik (`workspaceResize.ts`, `useWorkspaceLayout`) ist getestet, nicht die Komponente selbst. | **Erledigt 06.09.2026:** neue `WorkspaceShell.test.tsx` (23 Fälle). `WorkspaceShell` ist zustandslos (Layoutmodus, Prozentwerte, Drag-Zustand, `t`, `cellCls`, `renderPanel` kommen aus `page.tsx`/`useWorkspaceLayout`), die Tests brauchen deshalb weder Provider noch gemockte API — `renderPanel` liefert je Index ein markiertes Div, an dem sich Reihenfolge und Breiten ablesen lassen. Abgedeckt: (1) Layout je `layoutMode` — `1-pane` ohne Teiler/Gap, `split` mit genau einem Teiler, `3-col` mit zweien, `4-grid` ohne Spaltenteiler, dafür mit Kreuzgriff, plus ein Panel je `panelConfigs`-Eintrag; (2) Teiler nur wo vorgesehen — die Position wird über `previousElementSibling`/`nextElementSibling` geprüft, damit ein Teiler nicht einfach irgendwo im Baum stehen darf, und jeder Teiler wird auf seinen eigenen Handler festgenagelt (Split, 3-col links/rechts, Raster); (3) Panel-Breiten aus dem Snapshot — `splitPercent` auf Panel 0, `threeColRightPercent - threeColLeftPercent` als Mittelspalte, letzte Spalte ohne feste Breite (`flex-1`), `gridColumnPercent`/`gridRowPercent` im Raster und in der Griffposition, Abschalten der Breitenanimation während `isDragging`; (4) Mobil — Sichtbarkeitsregel des Tab-Umschalters, Rückholen der Code- bzw. Dokumentansicht beim Wechsel vom Graph zum Editor, und die Reduktion auf genau ein Panel je aktivem Tab (inkl. `indexOf(...) === -1`, das die Shell unverändert durchreicht). Gegenprobe: vier gezielte Mutationen am Bauteil (rechter 3-col-Teiler am linken Handler, Mittelspalte mit voller statt differenzieller Breite, Split-Teiler vor statt zwischen den Panels, Animationsabschaltung ignoriert) lassen 4 der neuen Tests fehlschlagen. `tsc --noEmit`, ESLint, volle Vitest-Suite (160 Tests) und Produktions-Build grün. Kein Container-Rebuild nötig — reine Testdatei. | Keine |
| O-058 | Frontend / Tests | `CallGraphView.tsx` hat keinen Test — Fokus-Laden (`/callgraph/focus`), Hops-Auswahl, Export-Buttons (JSON/CSV/GraphML), Truncated-Banner sind ungetestet. | **Erledigt 06.09.2026:** neue `CallGraphView.test.tsx` (22 Fälle) nach dem `KnowledgeGraphView.test.tsx`-Muster — `react-force-graph-2d` durch einen Stub ersetzt, der je Knoten einen klickbaren Button rendert (treibt `onNodeClick` ohne Canvas an) und über `ref` die benutzten Kamera-Methoden anbietet, dazu ein `ResizeObserver`-Stub, ohne den `dimensions.width` bei 0 bleibt und der Graph gar nicht erst gerendert wird. Abgedeckt: (1) Fokus-Laden — Platzhalter und kein Abruf ohne Fokus, `/callgraph/focus` mit `entity_id`/`hops`, `project_id` nur im Projektkontext, HTTP-Fehler und Netzwerkfehler als Banner statt Absturz, Nachbau eines eigenen Knotens aus `target_name` bei unaufgelöster Kante, Neu-laden-Knopf, Knotenklick meldet Datei/Zeile/Quelle nach oben, Kantentyp-Filter blendet Kanten samt daran hängender Knoten aus; (2) Hops-Auswahl — Nachladen mit `hops=3`, Hervorhebung der aktiven Zahl, Singular/Plural in der Kopfzeile; (3) Export — alle drei Formate vorhanden, JSON-Download mit `callgraph-<Name>.json`, eigene Endung für GraphML, Export folgt der aktuellen Hop-Zahl und dem Projektkontext, gescheiterter Export zeigt `Export fehlgeschlagen (HTTP 500)` statt eines leeren Downloads; (4) Kapp-Banner bei `truncated: true`, still bei vollständiger Nachbarschaft, und Rücknahme der Warnung nach einem ungekappten Nachladen. Gegenprobe: fünf gezielte Mutationen am Bauteil, vier davon von den Tests gefangen (feste `hops=1` im Export, `truncated` wird nie zurückgesetzt, Projektkontext fällt beim Fokus weg, Kantentyp-Filter greift nicht); die fünfte (Wegfall der `!node.unresolved`-Bedingung beim Knotenklick) ist bewusst ungefangen — ein Knoten für ein unaufgelöstes Ziel wird allein aus `target_name` gebaut und hat deshalb nie einen Dateipfad, die Bedingung gibt für ihn also ohnehin nie allein den Ausschlag. `tsc --noEmit`, ESLint, volle Vitest-Suite (182 Tests) und Produktions-Build grün. Kein Container-Rebuild nötig — reine Testdatei. | Keine |
| O-059 | Frontend / Tests | `JobCenter.tsx` hat keinen Test — Job-Liste, Retry/Stop/Entfernen-Aktionen, Admin-only-Gating sind ungetestet. | **Erledigt 06.09.2026:** neue `JobCenter.test.tsx` (22 Fälle). Abgedeckt: (1) Job-Liste — Panel öffnet erst auf Knopfdruck und schließt bei einem Klick daneben, Label und übersetzter Status, Leer-Hinweis, Aktiv-Zähler am Knopf (und kein Zähler bei 0), Fortschrittsbalken nur für laufende Jobs mit Fortschritt, Auf-/Zuklappen der Fehlermeldung (und kein Aufklapper ohne Meldung), Projektfilter über `getJobs(projectId)`; (2) Rollen-Gating — die Regel ist nicht bloß „Admin sieht mehr": `Wiederaufnehmen` erscheint **ausschließlich** für Nicht-Admins, Admins bekommen stattdessen `Neu anstoßen`, `Stoppen` und `Entfernen`; ein fehlender Nutzer zählt als Nicht-Admin, und auch beim Admin entscheiden weiterhin die `can_start`/`can_stop`/`can_delete`-Flags des Jobs; (3) Aktionen — alle vier rufen den richtigen Endpunkt mit `kind`/`id`, Wiederaufnehmen/Neu anstoßen/Stoppen laden danach nach, Entfernen dagegen streicht die Zeile nur lokal, Server-Fehlertext bzw. allgemeiner Fallback landet in einem `role="alert"`, der Knopf ist während der laufenden Aktion gesperrt, ein fehlgeschlagener Poll wird verschluckt und ein 401 nicht einmal geloggt. Gegenprobe: sechs gezielte Mutationen am Bauteil (Wiederaufnehmen auch für Admins, Entfernen ohne Admin-Prüfung, Server-Fehlerdetail verworfen, 401 doch geloggt, Fortschrittsbalken auch für abgeschlossene Jobs, Entfernen lädt neu statt lokal zu streichen) lassen 6 der neuen Tests fehlschlagen. Randnotiz: jest-dom-Matcher (`toBeDisabled`, `toHaveTextContent`) sind im Typecheck nicht verfügbar, weil `vitest.setup.ts` in `tsconfig.json` ausgeschlossen ist — die Suite prüft deshalb durchgängig über einfache Eigenschaften. `tsc --noEmit`, ESLint, volle Vitest-Suite (204 Tests) und Produktions-Build grün. Kein Container-Rebuild nötig — reine Testdatei. | Keine |
| O-060 | Frontend / Tests | `LinkManagerView.tsx` hat keinen Test — manuelles Verknüpfen/Bewerten von Quellen (DOC-F-081) ist ungetestet. | **Erledigt 06.09.2026:** neue `LinkManagerView.test.tsx` (34 Fälle). Die Ansicht spricht ihr Backend direkt über `fetch` an (nicht über den axios-`api`-Wrapper), deshalb ein nach URL und Methode routender fetch-Stub statt eines Modul-Mocks; `TopicsPanel` ist als eigener, admin-exklusiver Bereich mit eigenem Datenabruf gestubbt. Abgedeckt: (1) Filterleiste — Tab-Zähler addieren Code- und Wissens-Links, Tabwechsel lädt mit neuem `status` nach, Score-Filter nur im Offen-Tab und als `min_score` in der Anfrage, Suche über beide Seiten **und** die Bildunterschrift, Artfilter (Alle/Code↔Doc/Doc↔Doc), Sortierung absteigend nach Wahrscheinlichkeit, Mindest-Wahrscheinlichkeit wird auf 0..100 geklemmt und im `localStorage` gemerkt, Auto-Link-Lauf schickt sie als `min_confidence` mit, Topics-Bereich nur für Admins, Leer-Zustände je Tab; dazu die Sammelbestätigung („Alle 100 % bestätigen": erscheint nur im Offen-Tab und nur mit perfekten Treffern, zählt nur diese, bestätigt sie gebündelt, meldet Teilfehlschläge); (2) manueller Picker — ohne Projekt gesperrt, Artfrage vorgeschaltet, Entity-Suche über Name und Pfad mit ausgeblendeten `ref_`-Einträgen, `ent_`-Präfix wird auf die numerische DB-ID zurückgeführt, Speichern erst mit beiden Seiten aktiv, Code↔Doc-Anlage wechselt danach in den Bestätigt-Tab, Doc↔Doc-Anlage geht als `link_type: manual`/`status: approved` raus, Serverfehler bleibt im offenen Formular stehen, Zurück-Pfeil behält die Auswahl — und das Zuklappen über „Manuell" räumt bewusst **nicht** auf, nur das Kreuz tut das; (3) Linkkarten — beide Seiten samt Score und richtungsabhängigem Pfeil, Bestätigen/Ablehnen mit PATCH-Status, **die Karte bleibt bei abgelehntem PATCH stehen** (Absicherung des früheren Fixes gegen stille Diskrepanz zum Serverstand), tabspezifische Aktionen (Offen: LLM-Prüfung/Bestätigen/Ablehnen, Bestätigt: Entfernen, Abgelehnt: Doch bestätigen/Löschen), Löschen per DELETE, LLM-Nachbewertung aktualisiert Score und Begründung in der Karte bzw. meldet den Serverfehler, und das nachträgliche Bearbeiten der Beschreibung (Enter speichert getrimmt, Escape verwirft, unverändert schickt nichts). Gegenprobe: sieben gezielte Mutationen am Bauteil (Karte verschwindet trotz fehlgeschlagenem PATCH, Suche ignoriert die rechte Seite, Artfilter greift nicht, `ent_`-Präfix bleibt stehen, Mindest-Wahrscheinlichkeit ungeklemmt, Sortierung entfällt, Sammelbestätigung nimmt alle statt nur der perfekten) lassen 10 der neuen Tests fehlschlagen. `tsc --noEmit`, ESLint, volle Vitest-Suite (238 Tests) und Produktions-Build grün. Kein Container-Rebuild nötig — reine Testdatei. | Keine |
| O-061 | Frontend / Tests | `PanelContentRenderer.tsx`/`SplitPaneWorkspace.tsx` (Routing, welcher Panel-Typ für Chat/Code/Dokument/Graph/Web/Callgraph/Link-Manager gerendert wird) hat keinen Test. | **Erledigt 06.09.2026:** zwei neue Dateien für die zwei Stufen der Panel-Weiche. `PanelContentRenderer.test.tsx` (16 Fälle) prüft die erste Stufe mit Stubs für alle vier Zielansichten, die ihre Props festhalten: `chat`/`callgraph`/`linkmanager` bekommen ihre eigene Ansicht, alles andere geht an `SplitPaneWorkspace` mit abgeleitetem `activeRightTab` (`doc`→`doc`, `graph`→`graph`, `webview`→`weborigin`, **unbekannte Typen fallen auf `code` zurück**); dazu die typabhängig durchgereichten Props (`layoutMode` nur fürs Graph-Panel, `onDocFocus` für alle außer Webview) und die Bindung der Rückrufe an den eigenen Panel-Index (Chat-Dateiauswahl, Call-Graph-Sprung mit `openIfMissing`/`preserveFrozenTarget`, Übersetzung der Dokumentauswahl in Name + Quellen-ID, Gutter-Klicks). `SplitPaneWorkspace.test.tsx` (17 Fälle) prüft die zweite Stufe mit gestubbtem Monaco, gestubbter Graph-Ansicht und gestubbtem `api`: der rechte Bereich bleibt ohne Datei und Dokument ganz leer (außer im Graph-Tab), und je Tab entsteht der richtige Inhalt — Wissensgraph statt Editor, Web-Original als `srcdoc`-iframe **ohne `allow-same-origin`**, Dokumente als PDF-iframe / `<img>` / Markdown / Klartext, Code im Editor mit aus der Endung erkannter Sprache (`.cbl` → COBOL, Unbekanntes → `text`); dazu die tabspezifische Ladeanzeige, die Weitergabe an die Graph-Ansicht und die Nebenabrufe (Graph-Tab holt weder Dateiinhalt noch Referenzen, Referenzen nur für Code- und Dokument-Panels). Gegenprobe: sieben gezielte Mutationen an beiden Bauteilen, alle sieben gefangen — der Graph-Tab-Fall erst, nachdem im Test ein `repo_id` am Projekt gesetzt wurde, ohne das gar kein Inhalt geladen worden wäre und der Test wirkungslos geblieben wäre. `tsc --noEmit`, ESLint, volle Vitest-Suite (271 Tests) und Produktions-Build grün. Kein Container-Rebuild nötig — reine Testdateien. | Keine |
| O-062 | Frontend / E2E | Nur 2 Playwright-Specs existieren (`login.spec.ts`, `accessibility.spec.ts`) — kein einziger End-to-End-Test für den eigentlichen Kernworkflow (Quelle anbinden → COBOL wird geparst → Chat-Frage stellen → Quellenverweis anklicken → Code-Ansicht öffnet an der richtigen Zeile). | **Erledigt 06.09.2026:** neue `e2e/golden-path.spec.ts`. Der Test legt sein Fixture-Repo selbst an — ein bares Git-Repository unter `repos/` mit einem kleinen COBOL-Programm; dasselbe Verzeichnis ist in `backend-api` und `parser-worker` als `/repos` eingehängt, deshalb genügt eine `file:///repos/...`-URL und es braucht weder Netzzugang noch ein fremdes Repo. Ablauf: anmelden → Projekt und Git-Wissensquelle anlegen → auf `sync_status: completed` warten (mit `expect.poll`, nicht mit festen Wartezeiten) → prüfen, dass der Parser die Objekte persistiert hat → in der Oberfläche das Projekt wählen, im Kopf-Suchschlitz nach dem COBOL-Paragraphen suchen, den Treffer anklicken → Code-Ansicht zeigt die richtige Datei **und** steht in der richtigen Zeile (geprüft über Monacos `.active-line-number`). Aufgeräumt wird in `afterAll` (Quelle, Projekt, Fixture-Repo). **Dieser Test hat sofort einen echten Fehler gefunden:** der Zeilensprung (`revealLineInCenter`/`setPosition` in `SplitPaneWorkspace.tsx`) hing an den Abhängigkeiten `[selectedLine, selectedFile]`. Beim **erstmaligen** Öffnen einer Datei — also genau bei einem Suchtreffer oder einem Quellenverweis aus dem Chat — ist Monaco zum Zeitpunkt des ersten Effektlaufs noch nicht montiert; der Effekt lief einmal ins Leere und wurde nie wiederholt, weil eine Ref-Zuweisung keine Neuauswertung auslöst. Der Editor blieb dadurch in Zeile 1 stehen statt zur Zielzeile zu springen. Behoben durch Aufnahme von `editorMountTick` (existierte schon, war aber nur am Entity-Dekorations-Effekt verdrahtet) und `contentToUse` in die Abhängigkeiten; zusätzlich als Unit-Regressionstest in `SplitPaneWorkspace.test.tsx` festgehalten (der Monaco-Stub meldet sich dort bewusst per `setTimeout` zurück, weil React Kind-Effekte VOR Eltern-Effekten ausführt und ein `onMount` im Effekt das echte, asynchrone Mount-Timing gerade nicht nachbilden würde). Für die Oberfläche kamen zwei stabile IDs dazu (`#global-search-input`, `#global-search-result-<typ>-<id>`), passend zur schon bestehenden ID-Konvention für E2E-Tests. **Zwei bewusste Grenzen:** (1) Die Chat-Frage aus der Aufgabenbeschreibung steht als zweiter Test in derselben Datei, überspringt sich aber, wenn die Installation kein nutzbares LLM hat — dieses Deployment fährt `LLM_MODEL=disabled` (CPU-only-Pilot, nur `bge-m3` gezogen), der Test ist deshalb **geschrieben, aber hier nicht grün beobachtet**; die Skip-Begründung zitiert den Serverfehler. Ein LLM allein zum Testen auf diesen 8-GB-Host zu ziehen wäre den Betriebsrisiken nicht wert. (2) Der Test ist **nicht** in den CI-`e2e`-Job aufgenommen: der fährt Backend und Frontend bare auf dem Runner, ohne `parser-worker` und ohne Celery — der Git-Sync würde dort nie starten. Er gehört an den Compose-Stack, wie im Punkt gefordert; fehlt das `repos/`-Bind-Mount, überspringt er sich mit Begründung statt dauerhaft rot zu stehen (dieselbe Regel wie beim `requires_ollama`-Marker der Parser-Tests). Verifiziert gegen den laufenden Stack: volle Playwright-Suite 4 passed, 1 skipped; Vitest 273 Tests, `tsc --noEmit`, ESLint und Produktions-Build grün; `frontend`-Image neu gebaut und ausgerollt. | Keine |

O-063 bis O-066 stammen aus einem Codequalitäts-Audit vom 05.09.2026 (Anlass:
Repo-Übergabe in ca. einem Monat). Kommentierung ist kein Problem — 0
TODO/FIXME/HACK-Marker im gesamten Code, Stichproben zeigen gezielte,
nicht-triviale Kommentare genau dort, wo CLAUDE.md-Regel 5 sie verlangt. Die
folgenden vier Punkte sind die tatsächlich gefundenen strukturellen Lücken.

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-063 | Frontend / Typsicherheit | Ausgangsbefund 05.09.2026: `strict: false` und zahlreiche explizite `any`-Typen in Chat, Graphen, Panel-Navigation und Einstellungen. | **Erledigt 06.09.2026:** vollständiger Strict-Modus bleibt aktiv; verbleibende explizite `any`-Typen im gesamten eigenen Frontend einschließlich Unit- und E2E-Tests ersetzt. Gemeinsame Domänen-/API-Verträge in `frontend/types/domain.ts` für Projekte, Quellen, Code-Entities, Dokumentauswahl, Workspace-Snapshots, Chat-Nachrichten/-Sitzungen/-Requests/-Stream-Ereignisse, Referenzen und Administration; bestehende LLM-Profiltypen wiederverwendet. Zentrale API-Antworten, Hook-State und Komponenten-Props typisiert, Monaco-/ForceGraph-Refs mit Bibliothekstypen, dynamische Metadaten und Tool-Argumente als `unknown` mit gezielter Eingrenzung. Stream-Grenze validiert Ereignisse vor State-Updates; numerische Quellen-IDs, fehlende Dokumentzeilen und alte Snapshot-Felder berücksichtigt. API-Fehlertexte werden vor der Anzeige geprüft. Nebenbefund behoben: Graph-Kameranachlauf verwendet die vorhandenen Graph-Daten statt des am React-Ref nicht verfügbaren `graphData()`-Aufrufs. Test-Fixtures und Axios-Mocks prüfen dieselben Verträge; nur gezielte Typassertionen für unvollständige Monaco-/DOM-Testadapter. ESLint verbietet `TSAnyKeyword` für alle eigenen TS-/TSX-Dateien ohne Testausnahme; Gegenprobe mit absichtlichem `any` wird abgewiesen. **Verifiziert:** Syntaxbaumprüfung über 111 eigene TS-/TSX-Dateien: **0 explizite `any`**; TypeScript und Produktionsbuild grün; volle Vitest-Suite **319 bestanden, 1 bestehender erwarteter Fehlschlag** (29 Dateien), einschließlich neuer Stream-/Fehlergrenzen- und Kamera-Regressionstests. ESLint ohne Fehler, zwei bekannte TanStack-Virtual-Warnungen. Keine neuen Abhängigkeiten; kein Backend-/Datenbankeingriff und kein Offline-Deployment-Test. | Keine weitere O-063-Arbeit offen. |
| O-064 | Backend+Parser / CI | **Erledigt 06.09.2026:** Ruff `0.16.6` als reine Entwicklungs-/CI-Abhängigkeit in `requirements-lint.txt` gepinnt; gemeinsame `ruff.toml` (Python 3.11, 100 Zeichen, Regeln `E4`/`E7`/`E9`/`F`). Neuer verbindlicher CI-Job `python-quality` führt Lint und Formatprüfung für `backend/` und `parser/` aus, einschließlich Tests, Migrationen und eigenem Unidecode-Shim. Ausgenommen sind generierter ANTLR-Code und der historische Spike; nur `backend/main.py` hat eine begründete `E402`-Ausnahme für Router-Imports nach der Logging-Initialisierung. Bestehende Lint-Befunde bereinigt: ungenutzte Imports/Variablen, missverständliche Variablennamen, überflüssige f-Strings; SQLAlchemy-NULL-Vergleiche verwenden `.is_(None)`, der von Job-Tests verwendete Celery-Import bleibt als expliziter Re-Export erhalten. 123 Dateien einmalig formatiert, Modellduplikate weiterhin byte-identisch. Lokale Befehle in README, MIT-Lizenz und Herkunft in `docs/OSS-CLEARING.md` dokumentiert. **Validierung:** Ruff-Lint und Formatprüfung für 159 Dateien grün, CI-YAML und `git diff --check` gültig, alle Migrationen auf temporärer DB erfolgreich. Backend: 179 bestanden, 1 übersprungen. Parser einschließlich COBOL-Golden-Tests: 198 bestanden, 4 übersprungen; 3 Fehlschläge in Folder-/WebDAV-Tests bei nicht verfügbarem Ollama, alle drei am unveränderten Git-Stand reproduziert (fehlende Mocks für den Modellaufruf, keine O-064-Regression). Tests liefen mit separaten PostgreSQL-/Valkey-Instanzen. | Keine weitere Ruff-Arbeit offen. `mypy` bleibt optional; Ruff prüft keine Python-Typverträge. Die drei vorbestehenden Ollama-abhängigen Testfälle benötigen separat vollständige Mocks. | Keine |
| O-065 | Repository-Hygiene | **Erledigt 06.09.2026:** historischen `parser/spikes/antlr_cobol/` entfernt: Vergleichsskript, Spike-Requirements und generierte Parser-Duplikate werden nicht mehr benötigt; Referenzprüfung ergab keine produktiven Imports. Die zwei weiterhin benötigten Grammatikquellen und der MIT-Lizenztext wurden byte-identisch nach `parser/cobol/grammar/` verschoben, ergänzt um Provenienz und ANTLR-4.13.2-Regenerierungsanleitung. Der aktive Parser unter `parser/cobol/_antlr/` bleibt unverändert. Analyse und Spike-Code sind über Commit `2d0a96706158f69a030c5b83c619c70cd108466b` in der Git-Historie abrufbar; kein separates Archiv-Repo nötig. Veraltete Docker-/Ruff-Ausnahmen entfernt und README, E-11, Lizenzbericht sowie Bridge-Kommentar aktualisiert. Die ursprünglich genannten 22 MB enthielten auch unversionierte lokale Entwicklungsartefakte; diese wurden außerhalb des Repos aufbewahrt, nicht gelöscht. **Verifiziert:** 123 COBOL-Tests einschließlich Golden Files bestanden, Ruff-Lint und Formatprüfung grün, `git diff --check` sauber, alle drei verschobenen Dateien byte-identisch. | Keine weitere Arbeit offen. | Keine |
| O-066 | Backend+Frontend / Struktur | **Erledigt 06.09.2026:** Alle sechs Dateien einzeln geprüft. `projects.py` enthielt die vollständig routenunabhängige, verschlüsselungsbewusste Text-Referenzsuche; diese liegt jetzt in `services/reference_search.py`, die zwei bisherigen privaten Namen bleiben als schmale Kompatibilitäts-Wrapper für die vorhandenen fokussierten Tests bestehen. `SplitPaneWorkspace.tsx` bündelt den einen Code-Panel-Lebenszyklus (Datei laden, Monaco-Mount/Decorations, Referenznavigation und Menüs) — diese Teile teilen Auswahl- und Editor-Refs so eng, dass eine Auslagerung nur einen großen Prop-Block erzeugen würde. `LinkManagerView.tsx` ist ebenfalls ein einzelner Review-Workflow; Ladezustand, Filter, Mutationen und Formulare arbeiten auf demselben Link-Snapshot und sind keine unabhängigen Komponenten. `knowledge_sources.py` hat zwar mehrere Endpoint-Gruppen, aber jede Route ist bereits kurz und nutzt denselben Sichtbarkeits-/Team-/Sync-Vertrag; die Atlassian-Vorschau ist nur der Implementierungszweig ihres `/resolve`-Endpoints. `app/page.tsx` enthält nach O-004 keine eigene Datenlogik mehr, sondern verdrahtet die vorhandenen State-Hooks mit Shell, Sidebar und Panel-Renderern; ein separates Wrapper-Element würde dieselbe Prop-Menge nur verlagern. **Echter Ausreißer:** `chat.py::chat()` ist keine Dateigrößenfrage, sondern eine 726-Zeilen-Route mit mehreren Geschäftsabläufen; sie wird gezielt und testbar in O-068 zerlegt, statt hier oberflächlich Dateien zu verschieben. | Keine weitere Aufteilung aus O-066. Künftige Änderungen folgen den festgestellten Grenzen; die Chat-Zerlegung ist O-068. | Keine |
| O-067 | Frontend / Session-Handling | **Erledigt 06.09.2026:** Der zentrale API-Client stellt jetzt `api.fetch()` für Fälle bereit, die bewusst die native `Response` brauchen (SSE und Binärdownloads). Der Wrapper setzt standardmäßig `credentials: include`, reicht die unveränderte Response weiter und feuert bei 401 dasselbe `doctus:unauthorized`-Event wie der Axios-Interceptor. Alle sieben betroffenen Komponenten (`ChatView`, Knowledge-/Call-Graph, Link-Manager, Split-Pane, Topics und Layout-Einstellungen) verwenden ihn; der Chat-SSE-Consumer in `useChatController` ebenso für seinen Same-Origin-Proxy `/api/chat`. Damit führt auch eine abgelaufene Session während Streaming, Graph-Laden, Exporten oder Link-/Themenaktionen zurück zur Login-Ansicht. Keine API-Endpunkte oder Datenformate geändert. **Verifiziert:** 116 gezielte Vitest-Tests grün, einschließlich neuem Test für Cookie-Default und 401-Event; TypeScript grün, ESLint ohne Fehler (zwei vorbestehende TanStack-Virtual-Warnungen), `git diff --check` sauber. | Keine |
| O-068 | Backend / Verständlichkeit | **Erledigt 06.09.2026:** `chat()` ist auf HTTP-/SSE-Orchestrierung, Autorisierung, Session-Lifecycle und Persistierung reduziert. Retrieval, Graph-/Sichtbarkeitsfilter, Pin-Kontext und Prompt-Aufbau liegen in `services/chat_service.py`; Standard-Provider-Streaming für Ollama/OpenAI, Gemini und Anthropic liefert dort normalisierte Chat-Events. Die bestehende Agent-Tool-Schleife bleibt als eigener Service-Schnitt erhalten. Neue Provider-Regression deckt Payload, Streaming-Chunks, Abschluss- und Antwort-Events ab; bestehende Hybrid-Search-, Prompt-Injection- und Provider-Guard-Tests bleiben kompatibel. | Keine weitere O-068-Arbeit offen. | Vollständiger Container-Testlauf nach Rebuild |
| O-069 | Abhängigkeiten / Sicherheit | Bisher nirgends erfasst: `npm audit` findet 9 bekannte Schwachstellen im Frontend (5 moderate, 4 high — `browserslist`, `dompurify`, `js-yaml`, `mermaid`, `monaco-editor`, `nanoid`, `next`, `postcss`, `undici`), `pip-audit` findet 10 in 4 Backend-/Parser-Paketen. Am relevantesten: **`pypdf` 6.14.2** (5 CVEs, alle DoS über präparierte PDFs — Speicher-/Laufzeit-Explosion, Endlosschleife bei der Textextraktion) sitzt genau im Upload-/OCR-Pfad, den O-031/O-044 heute noch ausgebaut haben; ein Nutzer könnte mit einer präparierten PDF den Parser-Worker lahmlegen. Die übrigen drei (`cryptography`, `diskcache`, `fastmcp`) betreffen Codepfade, die Doctus nicht aufruft (S/MIME-Entschlüsselung, Cache-Pickle-RCE ohne direkten Import, Windows-CLI-Installer/OAuthProxy von `fastmcp`, transitiv über `mcp-atlassian`) — real niedriges Risiko, aber billig mitzupatchen. | **Erledigt 06.09.2026:** Frontend: `npm audit` meldet jetzt **0 statt 9** Funde — das Lockfile zieht `dompurify` 3.4.15, `js-yaml` 4.3.2, `nanoid` 3.3.18, `postcss` 8.5.28, `undici` 7.29.1, `mermaid` 11.17.2, `monaco-editor` 0.55.1 und `browserslist` 4.28.9. `next` bleibt bewusst auf **16.2.12**: weder die npm-Advisory-Datenbank noch OSV kennen für diese Version einen Fund (der ursprüngliche `next`-Treffer kam über die mitgelieferten `postcss`/`undici`, die der `overrides`-Block abdeckt), der Sprung auf 16.3.4 wäre also ein reiner Minor-Upgrade ohne Sicherheitsgewinn und wird nicht ohne eigenen Anlass gezogen. Backend+Parser: `pypdf` 6.14.2→6.16.1 und `cryptography` 49.0.0→50.0.0 (`dbea2c0`). **Beim Nachaudit neu gefunden:** `gitpython` 3.1.52 stand in `parser/requirements.txt`, hatte allein 20 Advisories (Argument-Injection in weitergereichten git-Optionen, u. a. RCE über `core.sshCommand`) — und wurde von **keinem einzigen Modul importiert**: der Git-Konnektor spricht über `parser/git_utils.py` per Subprozess mit dem `git`-Binary. Ersatzlos entfernt; `parser/tests/test_dependency_hygiene.py` hält beide Richtungen fest (kein `import git` ohne Deklaration, keine Deklaration ohne Import). Nachweis: Parser-Suite 207 Tests, Backend-Suite 183 Tests, Frontend 321 Tests — alle grün gegen die neuen Stände. Verbleibende Funde (`fastmcp`, `diskcache`) sind als O-158 zurückgestellt. | Keine |
| O-070 | Betrieb / Onboarding | Beim Erststart erzeugte Superuser-Zugangsdaten waren nur im Backend-Log auffindbar, nicht im Installer-Output. | **Erledigt 06.09.2026:** Online- und Offline-Installer zeigen den vollständigen Bootstrap-Zugangsdatenblock nach dem Start direkt an, einschließlich Hinweis auf sichere Aufbewahrung und verpflichtenden Passwortwechsel. Gemeinsamer Shell-Helfer liest ausschließlich Backend-Logs seit Beginn des aktuellen Starts; keine temporäre Passwortdatei, kein erneutes Anzeigen historischer Zugangsdaten bei Updates, kein Ausgeben vorgegebener Passwörter oder beliebiger Logzeilen. Polling bis zu 120 Sekunden; bei bereits gesundem Backend ohne neuen Bootstrap sofortiger Hinweis auf bestehende/vorgegebene Zugangsdaten, bei Timeout oder Logfehler Diagnosehinweis. Readiness wird vor dem Logabruf geprüft, damit ein gerade abgeschlossener Bootstrap nicht übersehen wird. 11 Docker-unabhängige Regressionstests und eigener CI-Job; Shell-Syntax und TypeScript grün, Backend **180 passed**, Parser **205 passed** gegen isolierte Testdatenbanken. README aktualisiert; keine neuen Abhängigkeiten. | Keine |
| O-071 | Parser / Performance | Beim CardDemo-Testimport (05.09.2026) aufgefallen: `EMBED_CONCURRENCY=20` (Default) lässt bis zu 20 Dateien gleichzeitig Batch-Embedding-Anfragen an eine einzige CPU-only-Ollama-Instanz schicken, die intern aber sequentiell rechnet (~1,1 Chunks/s, siehe E-8). Die parallelen Anfragen stauen sich, reißen den 120s-Timeout (`EMBED_BATCH_TIMEOUT`), fallen dann in den noch langsameren Einzel-Chunk-Fallback (`reindex_chunks_preserving_links`) — vermutlich der Hauptgrund, warum ein vergleichsweise kleiner Bestand (329 Dateien) fast eine Stunde braucht. Kein Datenverlust (die zugehörige, bis dahin nichtssagende "Embedding-Fehler"-Meldung wurde am selben Tag direkt behoben, Commit `58d350a`), aber selbstverschuldete Verlangsamung. GPU-Installationen (`docker-compose.gpu.yml`) profitieren dagegen von echter Nebenläufigkeit — ein pauschal niedrigerer Default wäre dort ein Rückschritt. | **Erledigt 05.09.2026:** neue Funktion `ollama_client.py::is_gpu_accelerated(model)` lädt das Modell per winzigem Warmup-Embed und fragt danach Ollamas `GET /api/ps` ab (Feld `size_vram` — `0` heißt CPU-only, `>0` heißt (teil-)GPU-beschleunigt); `GitConnector.sync()` wertet das einmal pro Sync-Start aus — GPU aktiv → `EMBED_CONCURRENCY` (20) bleibt, CPU-only → gedrosselt auf neues `EMBED_CONCURRENCY_CPU_ONLY` (Default 2), inkl. Log-Zeile. Konservativer Fallback (CPU-only annehmen) falls `/api/ps` nicht antwortet oder das Feld fehlt. Neue Regressionstests in `test_ollama_client.py` (GPU/CPU/Modell-nicht-gelistet/Ollama-nicht-erreichbar) und bestehende `test_git_connector.py`-Sync-Tests patchen `is_gpu_accelerated` jetzt mit. Volle Parser-Suite im Container 192/193 grün, einziger Fehlschlag der bekannte, unabhängige O-076. Parser-Worker neu gebaut und ausgerollt (05.09.2026). | Keine |
| O-072 | Backend+Parser / Auditierbarkeit | Beim CardDemo-Testimport aufgefallen: `parser/worker.py::process_knowledge_source`/`process_local_document` (die Sync-Tasks, die auch die "Embedding-Fehler"-Zeilen aus O-071 erzeugen) akzeptieren **keine** `trace_id`, anders als `compute_entity_links`/`compute_knowledge_links`/`generate_diagnostics_bundle`, die das bestehende Request-Trace-ID-System (`core/tracing.py`, jede Log-Zeile trägt `[%(trace_id)s]`, wird über Prozessgrenzen an Celery-Tasks weitergereicht) konsequent nutzen. Meldet ein Kunde "meine Quelle hat nicht synchronisiert", lässt sich der ursprüngliche API-Request (der eine Trace-ID hatte) nicht mit den Sync-Log-Zeilen des Celery-Tasks verknüpfen — man ist auf `KnowledgeSource.sync_log` allein angewiesen (das seit heute immerhin brauchbare Fehlermeldungen enthält, Commit `58d350a`). Zusätzlich: `git.py`/`base.py` loggen nur "fertig" oder "Fehler" pro Datei, nie "Embedding gestartet für X" — bei bis zu `EMBED_CONCURRENCY` gleichzeitig laufenden Dateien lässt sich aus dem Log ohnehin nicht "die eine" Datei ablesen, an der gerade gearbeitet wird, nur die Menge der noch nicht abgeschlossenen. | **Erledigt 05.09.2026:** `process_knowledge_source`/`process_local_document` (`parser/worker.py`) nehmen jetzt `trace_id` entgegen und öffnen `trace_id_scope(trace_id)` wie die anderen drei Tasks. Alle Backend-Aufrufstellen (`backend/api/knowledge_sources.py`: create/sync/reindex/create_folder_watch/create_git/upload; `backend/api/jobs.py`: `_queue_source`) übergeben jetzt `{"trace_id": get_trace_id(), ...}`. **Nebenbefund, mitbehoben:** `jobs.py::resume_job` (kind="source") berechnete `trace` bereits am Funktionsanfang, übergab es beim Wiederaufnehmen eines fehlgeschlagenen Sync-Jobs aber nie an `send_tracked_task` — Kopiervorlage für die anderen zwei `resume_job`-Zweige (link_builder/diagnostics), die es richtig machten. Zusätzlich neue Logzeile "Embedding gestartet für X" beim Dispatch (`GitConnector._embed_document`, direkt nach Semaphore-Erwerb) für echte Live-Fortschrittssicht bei paralleler Verarbeitung. Neue Regressionstests: `parser/tests/test_worker_trace_id.py` (Trace-ID-Scope wird gesetzt/zurückgesetzt, Default `"-"` ohne trace_id für Beat-getriggerte Syncs), `test_git_connector.py::test_git_connector_logs_embedding_start_per_file`; bestehender Backend-Test `test_admin_can_queue_full_git_reindex` angepasst. Volle Parser-Suite im Container 196/197 grün (einziger Fehlschlag der bekannte O-076), Backend-Suite 168/180 grün (die 12 Fehlschläge vorbestehend/umgebungsbedingt, unverändert gegenüber dem Stand vor diesem Fix). Parser-Worker+Backend neu gebaut und ausgerollt (05.09.2026). **Verwandter Fund am selben Tag, direkt behoben (Commit `ab77f82`):** `GitConnector._save_document_chunks` übergab kein `on_embed_error` an `reindex_chunks_preserving_links` (anders als `base.py`) — scheiterte der Einzel-Chunk-Fallback für eine Datei komplett (live an `AWS.M2.CARDDEMO.ACCTDATA.PS`, einer EBCDIC-Binärdatei, beobachtet), wurde das **vollständig stillschweigend** übersprungen: keine Logzeile, nur "'X' indexiert (0 Chunks)" — nicht unterscheidbar von einer echten Leerdatei wie `.gitkeep`, obwohl der komplette Dateiinhalt verloren ging. Jetzt behoben. | Keine |
| O-075 | Parser / Fortschrittsanzeige | Live am CardDemo-Testimport entdeckt: `self.source.parsed_files`/`progress` werden ausschließlich innerhalb von `GitConnector.fetch_documents()` erhöht — also sobald eine Datei vom Datenträger gelesen und **in die Embedding-Pipeline eingereiht** wird, nicht wenn sie tatsächlich fertig embedded/gespeichert ist. Sobald `fetch_documents()` durchgelaufen ist (alle Kandidatendateien gelesen), gibt es **keine Codestelle mehr, die diese Felder aktualisiert** — der finale Sammel-Block in `sync()` (`if pending_tasks: await asyncio.wait(pending_tasks)`, wartet auf die letzten noch offenen Embedding-Tasks) zählt nur eine rein lokale Variable hoch, die nie in die Datenbank zurückgeschrieben wird. Ergebnis: Sobald das Einlesen fertig ist (typischerweise schnell, da reines Datei-Lesen), aber noch einzelne langsame Dateien (siehe O-071, O-074) im Hintergrund weiterlaufen, **friert die Fortschrittsanzeige ein** — bei CardDemo blieb sie bei 89 % (296 von 329) stehen, obwohl im Hintergrund über eine Stunde lang weiter Dateien fertig wurden (335 unterschiedliche Dateinamen im Log nachgezählt, mehr als `total_files`). Aus Nutzersicht nicht von einem echten Hänger zu unterscheiden — der Sync-Log selbst zeigt echten Fortschritt, aber die primäre Fortschritts-UI (Prozent/Zähler) nicht. | **Erledigt 05.09.2026:** `fetch_documents()` schreibt `parsed_files`/`progress` nicht mehr (reines Einreihen sagt nichts über tatsächlichen Fortschritt aus); stattdessen aktualisiert `sync()`s eigene Abschluss-Verarbeitung (`for t in done:`, an beiden Stellen — reguläre 50er-Batch-Schwelle und finaler Sammel-Block) diese Felder jetzt anhand tatsächlich fertiger Dokumente, gegen `self.source.total_files`. Neue Regressionstests: `test_git_connector_draining_fetch_documents_alone_does_not_move_progress` (Einlesen allein darf `parsed_files`/`progress` nicht bewegen) und `test_git_connector_progress_advances_only_as_files_actually_complete` (nach Sync-Abschluss `parsed_files == total_files`). Parser-Suite (asyncio) 9/9 bei `test_git_connector.py` grün; volle Suite 207/223 grün, alle 16 Fehlschläge vorbestehend/umgebungsbedingt (bekannte `[trio]`-Fälle plus ein neu isolierter, unabhängiger Fund O-076). Parser-Worker-Rebuild bewusst zurückgestellt, bis der laufende CardDemo-Import abgeschlossen ist. | Keine |
| O-074 | Parser / Ingestion-Qualität | Beim CardDemo-Testimport konkret geprüft: `GitConnector` hat — anders als `folder.py`/`webdav.py` mit ihrer `SUPPORTED_EXTENSIONS`-Allowlist — **keinerlei Dateityp-Filterung**. Jede Datei im Repo wird mit `open(path, "r", errors="ignore")` als Text gelesen, auch Bilder/Archive/Binärformate. Live nachvollzogen an `diagrams/Signon-Screen.png`: die PNG-Rohbytes werden zu Datenmüll "dekodiert" (Steuerzeichen, zufällige Fragmente, nur zufällig lesbare PNG-Formatmarker wie `IHDR`), in 251 Chunks zerlegt und durch das reine Text-Embedding-Modell `bge-m3` geschickt. Es gibt in der gesamten Pipeline **keine Bildverständnis-Komponente** (kein Vision-Modell, keine Bildbeschreibung) — der OCR-Fallback aus O-031 gilt ausschließlich für PDF-Seiten ohne Text-Layer, nicht für eigenständige Bilddateien. Ein LLM kann weder sagen, was auf einem so "indexierten" Bild zu sehen ist, noch ist eine Themenzuordnung/Relevanzprüfung über die semantische Suche möglich — der gespeicherte Vektor repräsentiert Byte-Rauschen, keinen Bildinhalt. Das verschwendet aktiv Ollama-Rechenzeit (Mitverursacher der langen Sync-Dauer, siehe O-071) **und verschmutzt den Vektorindex mit Rauschen**, das später als falscher Suchtreffer auftauchen kann. Gegenbeispiel zur Einordnung: `.drawio`-Dateien sind echtes, lesbares XML (Shape-/Tabellen-Labels als Klartext) und wären grundsätzlich sinnvoll embedd-bar — im Testlauf aber selbst an O-071 gescheitert, kein grundsätzliches Dateityp-Problem wie bei PNG. **Ergänzender Fund (05.09.2026):** Eine reine Dateiendungs-Sperrliste hätte `AWS.M2.CARDDEMO.ACCTDATA.PS` (EBCDIC-Text, siehe O-072) nicht abgefangen — `.PS` ist keine erkennbare Binärendung. Passt zu DOC-F-028 (EBCDIC explizit "nicht v1-Umfang"), wird aber aktuell nicht sauber abgelehnt, sondern still als Datenmüll verarbeitet. **Teilweise erledigt 05.09.2026 (Commit `96d3298`):** neue Sperrliste `_SKIPPED_BINARY_EXTENSIONS` in `GitConnector` — Bilder, Archive, ausführbare/kompilierte Dateien sowie PDF/DOCX/XLSX/PPTX (dafür fehlt in `GitConnector`, anders als bei `folder.py`/`webdav.py`/dem lokalen Upload, jede Extraktionsfunktion — ehrliches Überspringen statt Datenmüll). Übersprungene Dateien bekommen kein `SourceScanFile`-Eintrag (wie beim bestehenden `MAX_READ_BYTES`-Skip) und werden bei jedem künftigen Sync erneut geprüft/übersprungen. Regressionstest `test_git_connector_skips_known_binary_formats_instead_of_embedding_garbage`. **EBCDIC-Inhalts-Heuristik erledigt 05.09.2026:** neue Funktion `git.py::_looks_like_text(raw)` liest Dateien jetzt zuerst als Bytes, verlangt striktes UTF-8-Decode (EBCDIC/andere Nicht-UTF-8-Kodierungen fallen dort bereits durch) UND einen Anteil an Steuerzeichen nach dem Decode von höchstens 5 % (`_MAX_CONTROL_CHAR_RATIO`) — deckt sowohl `AWS.M2.CARDDEMO.ACCTDATA.PS` (EBCDIC, scheitert am Decode) als auch valides-aber-datenmüll-artiges UTF-8 ab, das eine Endungssperre nicht erfassen könnte. Übersprungene Dateien bekommen wie beim Endungs-Skip kein `SourceScanFile`-Eintrag. Fünf neue reine Funktionstests für `_looks_like_text` (ASCII, leer, ungültiges UTF-8, steuerzeichenlastiges UTF-8, vereinzelte Steuerzeichen in echtem Text) plus zwei Sync-Regressionstests (`test_git_connector_skips_non_utf8_content_despite_text_looking_extension`, `test_git_connector_skips_valid_utf8_dominated_by_control_characters`). Volle Parser-Suite im Container 203/204 grün, einziger Fehlschlag der bekannte O-076. Parser-Worker neu gebaut und ausgerollt (05.09.2026). **Noch offen (fachliche Entscheidung, kein Code):** ob Bilder künftig über ein Vision-Modell einen echten Mehrwert bieten sollen — bis dahin bleibt der Skip aus O-074 der Dauerzustand. Eine geteilte PDF/DOCX-Extraktion in `GitConnector` (analog zu O-031s `extract_pdf_pages`) wäre zudem der bessere Folgeschritt als dauerhaftes Überspringen. | Fachlich klären: Vision-Modell/Bildbeschreibung ja/nein; falls nein, reicht der jetzige Skip als Dauerzustand. | Fachliche Entscheidung zum Zielumfang (nur Skip vs. echtes Bildverständnis) |
| O-076 | Parser / Tests | Beim O-075-Regressionslauf gefunden und isoliert gegengeprüft (auch auf unverändertem Code reproduzierbar, siehe `git stash`): `test_ap4_persistence.py::test_entities_and_edges_persisted_and_globally_resolved[asyncio]` schlägt mit `RuntimeError: coroutine raised StopIteration` fehl — ein von `git.py`/O-075 komplett unabhängiger, bisher nicht dokumentierter Fehlschlag (anders als die bekannten `[trio]`-Umgebungsfälle betrifft das hier die sonst zuverlässige `[asyncio]`-Variante). | **Erledigt 06.09.2026:** Die PEP-479-Vermutung war richtig, aber sie war nur die Maskierung — die eigentliche Ursache lag in der Test-Fixture, nicht im Parser. `RuntimeError: coroutine raised StopIteration` kam aus dem blanken `next(e for e in by_type["USES"] if e.dst_name == "SHARED-FIELD")` in Zeile 168: es gab schlicht **keine** solche Kante (ein Diagnoselauf zeigte als einzige USES-Kante `WS-FLAG`), und die `StopIteration` des leeren Generators wird beim Verlassen der Test-Coroutine zu `RuntimeError` umgedeutet — die eigentliche Erwartung war damit unsichtbar. Grund für die fehlende Kante: `_MAIN_CBL` hatte `COPY FIELDS.` in der **PROCEDURE DIVISION** stehen. Nur ein COPY **innerhalb der DATA DIVISION** zieht Datenfelder ins Programm (`cobol/parse.py::data_copy_edges` filtert genau darauf); ein COPY in der PROCEDURE DIVISION kopiert Anweisungen, keine Deklarationen — `SHARED-FIELD` war in MAIN also nie bekannt und konnte keine USES-Kante erzeugen. Der Parser verhielt sich korrekt, das Testprogramm war kein gültiges COBOL für das, was es prüfen wollte. Fix: `COPY FIELDS.` in die WORKING-STORAGE SECTION verschoben (mit Kommentar, warum die Stelle entscheidend ist). Zusätzlich die Maskierung selbst beseitigt — in `test_ap4_persistence.py` und an den zwei gleichartigen Stellen in `test_chunk_reindex_collision.py` (ebenfalls async) steht jetzt `next(..., None)` plus eine sprechende Zusicherung; die Gegenprobe mit der alten, fehlerhaften Fixture meldet nun `AssertionError: Keine USES-Kante auf SHARED-FIELD, nur: ['WS-FLAG']` statt des nichtssagenden `RuntimeError`. Volle Parser-Suite im Container: **205 passed**, vorher 204 passed + 1 failed. Kein Produktionscode geändert, kein Container-Rebuild nötig. | Keine |
| O-073 | Frontend / Support-Fähigkeit | Das bestehende Request-Trace-ID-System (`X-Request-ID`-Response-Header, siehe O-072) wird dem Nutzer nirgends angezeigt — weder bei einem fehlgeschlagenen API-Aufruf noch im Frontend-Crash-Reporter (`app/error.tsx`, sendet nur `message`/`stack`/`digest`, keine Trace-ID). Ein Kunde, der einen Fehler meldet, hat keine ID, die er/sie mitgeben könnte, damit Support gezielt in den Logs suchen kann, statt über Zeitstempel und Zuruf grob einzugrenzen. | **Erledigt 06.09.2026:** Der Fehler-Toast zeigt jetzt „Fehler-ID: <id> — bitte bei Rückfragen angeben", und der Crash-Reporter schickt dieselbe ID als `trace_id` mit (neues Feld an `ClientErrorReport`, landet als `client_trace=` in der Logzeile). **Wichtigster Fund beim Umsetzen:** der Header war für das Frontend gar nicht lesbar. Frontend und Backend sind verschiedene Origins; ohne `Access-Control-Expose-Headers` gibt ein Browser dem JavaScript aus einer Cross-Origin-Antwort nur die sechs CORS-safelisted Header heraus — `X-Request-ID` war gesetzt, aber unsichtbar, ohne Fehlermeldung und ohne Logzeile. `expose_headers=["X-Request-ID"]` in `main.py` ergänzt und mit einem eigenen Test festgenagelt (gegengeprüft: ohne die Zeile ist er rot). Leseseite in `frontend/lib/traceId.ts`, bewusst mit **zwei getrennten Wegen**: `extractTraceId(cause)` liefert die ID genau des fehlgeschlagenen Aufrufs und ist der einzige Weg, über den ein Toast eine ID anzeigt — `showToast` hat dafür einen dritten Parameter (`cause`) bekommen, den alle 39 Fehler-Toasts in `catch`-Blöcken jetzt durchreichen; `getLastTraceId()` liefert nur die zuletzt gesehene ID und ist ausschließlich für den Crash-Reporter, der kein Fehlerobjekt hat. Ein rein clientseitiger Fehler (kein Panel-Platz, Zwischenablage) zeigt bewusst **keine** ID, statt eine fremde zu nennen. Der Chat-Stream wirft bei `!response.ok` seinen eigenen `Error` — der trägt die ID jetzt explizit mit, sonst wäre sie ausgerechnet im wichtigsten Pfad verloren. Das Toast-Markup ist dafür aus `page.tsx` nach `components/Toast.tsx` gewandert (vorher nicht testbar), samt gemeinsamem Typ `ShowToast`, der drei lokale Kopien und fünf Inline-Signaturen ablöst. Tests: 6 im Backend (`test_trace_id_visibility.py`), 18 im Frontend (`lib/traceId.test.ts`, `components/Toast.test.tsx`, `app/error.test.tsx`, `app/services/api.traceId.test.ts`); Frontend 345 Tests, Backend 189 Tests, Ruff, `tsc --noEmit` und ESLint grün. | Keine |
| O-077 | Parser / Architektur | 05.09.2026, im Rahmen einer Skalierbarkeits-Einschätzung ("können wir auf andere Sprachen skalieren?") geprüft: es gibt genau einen Struktur-Parser (COBOL, `parser/cobol/`), angebunden über eine **einzelne hartkodierte Weiche** in `GitConnector._embed_document`: `if lang in _COBOL_LANGS: parse_result = await asyncio.to_thread(self._parse_cobol, doc, lang)`. Eine zweite Sprache ließe sich hier nicht anbinden, ohne diesen Zweig direkt anzufassen. **Wichtige Vorgeschichte:** genau so eine pluggbare Mehrsprachen-Schicht gab es schon (`parser/languages/` mit `PARSER_REGISTRY`) — laut `docs/TECH_DEBT_CLEANUP_PLAN.md` §1a wurde sie komplett entfernt, weil sie **nie produktiv befüllt** war (`PARSER_REGISTRY` blieb leer, `extract_references()` gab immer `[]` zurück). Eine reine Vorrats-Abstraktion ohne zweiten Konsumenten ist also nachweislich schon einmal gescheitert und wieder rausgeflogen (CLAUDE.md Prinzip 3, "keine Importierung auf Vorrat"). | **Erledigt 06.09.2026 (`a9c69ad`):** Dispatch in eine kleine Registry umgebaut — `parser/cobol/registry.py::STRUCTURE_PARSERS` (`{"cobol": parse_program, "copybook": parse_copybook}`), `GitConnector._embed_document` schlägt die Sprache jetzt darin nach (`STRUCTURE_PARSERS.get(lang)`) statt der alten `if lang in _COBOL_LANGS`-Weiche; `_COBOL_LANGS` und `_parse_cobol()` entfernt. COBOL/Copybook bleiben die einzigen echten Einträge, **kein** neues `parser/languages/`-Paket. Dispatch-Mechanismus selbst mit einem Test-Double abgesichert: `test_git_connector_dispatches_via_structure_parser_registry` hängt einen Fake-"Sprache"-Eintrag in `STRUCTURE_PARSERS` ein und beweist so den generischen Registry-Lookup, ohne eine zweite echte Sprache vorzutäuschen; Gegenprobe `test_git_connector_falls_back_to_generic_chunking_for_unregistered_languages` sichert den generischen `CodeParser`-Fallback für nicht registrierte Sprachen ab. Volle Parser-Suite 203/203 grün (im Container mit installierten `requirements-dev.txt` verifiziert), Parser-Worker/-Beat neu gebaut und ausgerollt. | Keine |
| O-078 | Parser / Architektur | Direkt anschließend an O-077 geprüft: die Namensgebung suggeriert an zwei Stellen COBOL-Exklusivität, obwohl die Form dahinter sprachneutral ist. (1) `ParseResult`/`Entity`/`Edge`/`Chunk` (die generische Rückgabeform, die auch `cobol_persist.py::persist_parse_result` unverändert für jede Sprache verarbeiten könnte) sind in `parser/cobol/model.py` definiert — ein künftiger zweiter Sprach-Parser müsste `from cobol.model import ...` schreiben, was die falsche Kopplung nahelegt. (2) Der Endungs-Override pro Wissensquelle heißt `spaces["cobol_extensions"]` (`git.py`), obwohl `classify_extension()`/`_DEFAULT_EXTENSIONS` selbst nichts COBOL-Spezifisches tun (reine Endung-zu-Label-Zuordnung, aktuell mit `cobol`/`copybook`/`jcl` befüllt). | **Erledigt 06.09.2026 (`79994c3`):** (1) `ParseResult`/`Entity`/`ParsedEdge`/`Chunk` (plus die referenzierten Literal-Aliase `SourceFormat`/`EntityType`/`EdgeType`/`Resolution`) nach neuem `parser/core/model.py` verschoben; `cobol/model.py` re-exportiert sie unverändert, damit sämtliche bestehenden `from cobol.model import ...` (auch die paketinternen `from .model import ...`) unangetastet bleiben. `connectors/git.py` und `cobol/registry.py` — die beiden Stellen, die den Fehlanreiz ursprünglich zeigten — importieren `ParseResult` jetzt direkt aus `core.model`. COBOL-eigene Strukturen (`CobolProgram`, `Division`, `Section`, `Paragraph`, `DataItem`, `FileDescriptor`, `SqlBlock`, `Segment`, `LogicalLine`) bleiben unverändert in `cobol/model.py`. (2) `spaces`-Schlüssel in `_resolve_extension_config()` auf `language_extensions` umbenannt, alter Schlüssel `cobol_extensions` wird als Fallback weitergelesen. Drei neue Tests für `_resolve_extension_config` (neuer Schlüssel, alter Schlüssel weiterhin gültig, neuer Schlüssel hat Vorrang); die bestehende Testsuite (u. a. alle `from cobol.model import ...`-Importe sowie der Golden-File-Korpus) ist der Regressionsschutz für die reine Verschiebung selbst. Volle Parser-Suite 212/212 grün, Parser-Worker/-Beat neu gebaut und ausgerollt. | Keine |
| O-079 | Parser / Architektur | `GitConnector.sync()` ruft `_build_copybook_index()` **unconditional bei jedem Sync** auf — eine COBOL-spezifische Voranalyse (baut den quellenweiten COPY-Namensindex, den `parse_program`/`parse_copybook` für die COPY-Auflösung brauchen), fest verdrahtet mitten im ansonsten sprachunabhängigen `sync()`-Ablauf. Für ein reines Java-/Python-Repo liefe das leer durch (kein funktionaler Fehler, da `copybook_exts` dann leer wäre), ist aber konzeptionell eine COBOL-Annahme an einer Stelle, die sonst nichts von COBOL weiß. | **Erledigt 06.09.2026:** Jeder Registry-Eintrag ist jetzt ein `ParserEntry` (`cobol/registry.py`) mit `parse` und einem optionalen `prepare_source(wt, extensions) -> Any`-Hook. `_build_copybook_index()` ist als `_prepare_copybook_index()` von `connectors/git.py` nach `cobol/registry.py` gewandert und dort an die Einträge `"cobol"`/`"copybook"` gehängt (beide teilen sich denselben Hook, weil beide denselben Copybook-Index brauchen). `GitConnector` kennt den Hook-Namen nicht mehr: `fetch_documents()` ruft generisch `_run_prepare_hooks()` auf, das für jede Sprache mit Registry-Eintrag deren `prepare_source` (falls gesetzt) genau einmal ausführt — ein von mehreren Sprachen geteilter Hook läuft dabei per Funktions-Identität dedupliziert nur einmal, eine Sprache ohne Hook bekommt keinen Eintrag. Das Ergebnis reicht `_embed_document()` pro Datei generisch an `entry.parse()` weiter (`self._prepared_by_lang.get(lang)` statt des vormaligen COBOL-benannten `self._copybook_index`). `MAX_READ_BYTES` dafür nach `git_utils.py` verschoben (gemeinsame Obergrenze für Connector-Chunking wie Vorlauf-Hooks, ohne Zirkelimport zwischen `cobol/registry.py` und `connectors/git.py`). `connectors/git.py` importiert dadurch nichts COBOL-Spezifisches mehr direkt (kein `cobol.copybook`/`cobol.parse` mehr). Tests: neuer `test_run_prepare_hooks_dedupes_shared_hook_and_skips_languages_without_one` (Dedup + Skip-ohne-Hook direkt gegen `_run_prepare_hooks()`), bestehender Dispatch-Test (`test_git_connector_dispatches_via_structure_parser_registry`) auf `ParserEntry` umgestellt und um den Fall "kein Hook → `None` durchgereicht" ergänzt, `test_sourcewide_index_expands_nested_copybooks_and_replacing` ruft jetzt `registry._prepare_copybook_index()` statt des alten `git_connector._build_copybook_index()`. Volle Parser-Suite 213/213 grün (im Container verifiziert), Parser-Worker/-Beat neu gebaut und ausgerollt. | Keine |
| O-080 | Frontend / Struktur | `SplitPaneWorkspace.tsx`s `detectLanguage()` hat einen COBOL-Sonderfall (`isCobol`) für Klick-Decorations/Spalten-Zonenfärbung im Code-Editor — der einzige Ort im Frontend, an dem eine erkannte Struktur (Entities/Referenzen) tatsächlich interaktiv genutzt wird. Andere Sprachen (Java, Python, ... bereits in `detectLanguage()` für reines Monaco-Syntax-Highlighting gelistet) bekommen unabhängig vom Backend-Analysestand nie Entity-Decorations, weil die Weiche an der Sprach-Whitelist hängt, nicht an "hat diese Datei tatsächlich strukturierte Entities in der DB". | **Erledigt (als Fehlannahme geprüft und geschlossen) 06.09.2026:** Beim genauen Nachvollziehen stellte sich heraus, dass der Befund zwei verschiedene `isCobol`-Stellen in `SplitPaneWorkspace.tsx` zu einer vermischt hatte. (1) Die eigentlichen **Entity-Klick-Decorations** (Glyph-Margin, Hover, Klick-zu-Referenzen — der `entityDecorationsRef`-Effekt sowie der `onMouseDown`-Handler) filtern seit jeher ausschließlich über `projectEntities.filter(ent => ent.file_path === selectedFile)`, **ohne jede Sprachprüfung** — per `git log -p` auf die Datei bestätigt: eine `isCobol`/`detectLanguage()`-Weiche gab es an dieser Stelle nie. Jede Sprache mit `CodeEntity`-Zeilen für eine Datei bekäme also schon heute dieselben Decorations, ganz ohne O-077s Registry abzufragen. (2) Die einzige verbliebene `isCobol`-Verwendung im Code (das Spalten-Lineal über dem Editor: Sequence-Zone 1-6, Indicator 7, Area A 8-11, Area B 12-72, Programm-ID 73-80) ist keine Entity-Decoration, sondern COBOLs physisches Fixed-Format-Spaltenraster nach ANSI-Konvention — ein Konzept, das für Java/Python/C#/etc. schlicht nicht existiert. Eine Datenabfrage ("hat diese Datei geparste Entities?") wäre hier die falsche Ersetzung: eine Datei einer zweiten, künftigen Sprache könnte durchaus Entities haben, ohne COBOLs Spaltenkonvention zu folgen — das Lineal würde dann falsche Zonen anzeigen statt korrekt zu verallgemeinern. Es gibt also nichts zu ersetzen: der Whitelist-Fall, den O-080 beheben sollte, existiert im aktuellen Code nicht (mehr/nie), und die verbliebene COBOL-Sonderbehandlung ist korrekt COBOL-exklusiv. Keine Codeänderung nötig. | Keine |
| O-081 | Dokumentation | Damit "auf andere Sprachen skalierbar, wenn nötig" eine nachprüfbare Aussage ist und keine Behauptung bleibt: es fehlt eine konkrete, befolgbare Anleitung, was das tatsächlich bedeutet. | **Erledigt 06.09.2026:** neues `docs/ADDING_A_LANGUAGE.md`, Schritt für Schritt anhand des COBOL-Parsers als einziger existierender Referenzimplementierung, gegen den tatsächlichen Code verifiziert (nicht nur aus der Erinnerung beschrieben): (1) Dateiendungen in `connectors/git.py::_DEFAULT_EXTENSIONS` klassifizieren, (2) Struktur-Parser schreiben, der ein `ParseResult` liefert (`parser/core/model.py`-Vertrag aus O-078 im Detail erklärt, inkl. des Fallstricks, dass `cobol_persist.py` globale Kanten unabhängig vom Parser-Ergebnis auf `unresolved` zwingt), (3) in `STRUCTURE_PARSERS`/`ParserEntry` (O-077/O-079) eintragen, (4) Golden-File-Testkorpus nach Vorbild von `parser/tests/cobol_corpus/` + `scripts/regenerate_cobol_golden.py` anlegen, (5) Persistenz/Backend-API/Graph/Frontend-Entity-Decorations bleiben unverändert — mit Beleg (`grep -rl cobol backend/api backend/services` liefert nichts) statt bloßer Behauptung. Zwei Punkte ergänzt, die in der ursprünglichen Maßnahme fehlten: der Geltungsbereich (`STRUCTURE_PARSERS` wird ausschließlich von `GitConnector` abgefragt, andere Connectoren bleiben immer generisch) und dass der CI-Job `parser-golden` mit `-k cobol` bewusst COBOL-only gefiltert ist und für eine zweite Sprache absichtlich erweitert werden müsste. Bewusst als reine Anleitung ohne Code belassen (kein zweiter Struktur-Parser existiert, keine Importierung auf Vorrat, CLAUDE.md Prinzip 3) — inkl. eines expliziten Verweises auf O-080s Befund, dass die COBOL-Spaltenlineal-Sonderbehandlung korrekt sprachspezifisch bleiben soll statt verallgemeinert zu werden. | O-077, O-078 |
| O-082 | Parser / Ingestion-Qualität | 05.09.2026, bei der Beschreibung der Confluence-Sync-Pipeline (`docs/CONFLUENCE_SYNC_PIPELINE.md`) aufgefallen: Confluence-Chunks tragen **keinerlei Abschnitts-/Positionsinformation** innerhalb der Seite. `_ConfluenceHTMLParser` (`connectors/confluence.py`) behandelt `h1`–`h6` wie jedes andere Block-Element (nur ein `\n` aus dem `_BLOCK`-Set) — die Überschrift selbst geht als normale Zeile im Fließtext unter, keine Verankerung "dieser Text gehört zu Abschnitt X". `_build_document()`s `extra_meta` trägt für eine Seite ausschließlich `{"page_id": ...}` (`confluence.py:336`). Anders als bei COBOL, wo jeder Chunk `meta["section"]`/`meta["paragraph"]` trägt (`cobol/chunking.py`), landet in `DocumentChunk.metadata_json` für Confluence keine Positionsangabe innerhalb der Seite. Bei einer langen Seite (Runbook, FAQ, Architekturdoku) zeigt ein Chat-Zitat deshalb nur den Seitentitel, nie den Abschnitt, in dem die Antwort tatsächlich stand. | **Erledigt 06.09.2026:** `_ConfluenceHTMLParser` führt jetzt parallel zum Text eine `_line_sections`-Liste (1:1 pro Ausgabezeile die zuletzt gültige `h1`-`h6`-Überschrift, inklusive Inline-Markup wie `<strong>` im Heading-Text; dieselbe Trim-/Leerzeilen-Kollaps-Logik läuft auf beiden Listen zugleich, damit der Index synchron bleibt). `_html_to_text()` liefert entsprechend `(text, line_sections)`. Neues optionales `Document`-Feld `line_sections` (`connectors/base.py`, `NotRequired`) — nur von Confluence befüllt, alle anderen Connectoren unverändert. `_process_document()` (gemeinsamer Pfad für Confluence/Jira/WebDAV/FolderWatch) schlägt darüber nach dem Chunking per `chunk["start_line"]` die Section nach und übergibt sie als `chunk["meta"] = {"section": ...}` — exakt das schon in `git.py` etablierte `**(chunk.get("meta") or {})`-Merge-Muster, das `base.py` bisher fehlte, jetzt ergänzt. `CodeParser.chunk_file()` und die Chunk-Grenzen selbst bleiben unangetastet (das ist O-083). Tests: neues `tests/test_confluence_html_parser.py` (8 reine Unit-Tests ohne DB gegen `_html_to_text()` — Überschriftenwechsel, Inline-Markup, Text vor der ersten Überschrift, Leerzeilen-Kollaps, `<pre>`-Block zwischen Überschriften, leere Überschrift überschreibt nichts), plus drei neue Tests in `test_confluence_connector.py` (Ende-zu-Ende über `fetch_documents()`→`line_sections` sowie über `_process_document()`→`metadata_json["section"]`, inkl. Regressionstest, dass eine Seite ohne jede Überschrift weiterhin keinen `section`-Schlüssel bekommt). Volle Parser-Suite 224/224 grün, `ruff check`/`ruff format` sauber, Parser-Worker/-Beat neu gebaut und ausgerollt. | Keine |
| O-083 | Parser / Ingestion-Qualität | Direkt anschließend an O-082: Chunk-Grenzen schneiden Confluence-Text (und jeden anderen über `CodeParser.chunk_file()` generisch gechunkten Inhalt — Jira/WebDAV/FolderWatch/lokale Uploads) blind nach Zeichenzahl (`chunk_size`, Default 1000), unabhängig davon, ob die Grenze zufällig zwischen zwei unabhängigen Abschnitten oder mitten in einem zusammenhängenden Gedanken liegt. Der 150-Zeichen-Overlap mildert das nur ab, verhindert es nicht. Bei COBOL übernimmt die Paragraphengrenze aus der Strukturanalyse genau diese Rolle (`cobol/chunking.py::chunk()`, siehe `docs/GIT_SYNC_PIPELINE.md` Punkt 1) — bei generischem Text gibt es aktuell kein Äquivalent. | **Erledigt 06.09.2026:** `CodeParser.chunk_file()` bekommt einen neuen optionalen Parameter `boundary_lines` (1-basierte Zeilennummern) — ein neuer Chunk bricht ab, sobald eine solche Zeile erreicht wird, auch wenn `chunk_size` noch nicht ausgeschöpft ist; `chunk_size` bleibt aber die harte Obergrenze innerhalb einer Section. Kein Overlap über eine so erkannte Grenze hinweg (sonst würde der neue Chunk mit Text der alten Section beginnen und O-082s `meta["section"]`-Zuordnung über `chunk["start_line"]` verfälschen). Neuer Helfer `connectors/base.py::_section_boundaries()` leitet aus `line_sections` (O-082) genau die Zeilen ab, an denen sich die Section gegenüber der Vorzeile ändert, und übergibt sie in `_process_document()` an `chunk_file()` — ohne `line_sections` (Jira/WebDAV/FolderWatch/lokale Uploads/`git.py`-Fallback) bleibt das Verhalten unverändert zeichenzahl-basiert. Bewusst **nicht mitgelöst:** dichte Überschriftenfolgen können jetzt viele kleine Chunks erzeugen (z. B. zwei aufeinanderfolgende Überschriften ohne Fließtext dazwischen) — das ist explizit O-084s Aufgabe (Winzling-Merge), hier bewusst nicht vorgezogen, um die beiden Punkte nicht zu vermischen. Tests: neues `tests/test_code_parser_boundaries.py` (6 reine Unit-Tests gegen `chunk_file()` — kein Verhaltensunterschied ohne `boundary_lines`, Schnitt vor einer Grenze trotz Restplatz, kein Overlap über die Grenze, `chunk_size` greift weiterhin innerhalb einer Section, Grenze auf der ersten Zeile erzeugt keinen leeren Chunk, mehrere Grenzen ergeben einen Chunk pro Section), neues `tests/test_section_boundaries.py` (5 Unit-Tests gegen `_section_boundaries()`), plus ein neuer Ende-zu-Ende-Test in `test_confluence_connector.py` (zwei kurze Sections, die zusammen locker unter `CHUNK_SIZE` passen, landen jetzt trotzdem als zwei Chunks); der bestehende O-082-Metadaten-Test wurde auf eine einzelne, grenzenlose Section umgestellt, damit er weiterhin isoliert nur die Metadaten-Anreicherung prüft. Volle Parser-Suite 236/236 grün, `ruff check`/`ruff format` sauber, Parser-Worker/-Beat neu gebaut und ausgerollt. | O-082 |
| O-084 | Parser / Ingestion-Qualität | Direkt anschließend an O-082/O-083 geprüft: `cobol/chunking.py::chunk()` merged aufeinanderfolgende zu kleine Paragraphen (unter `min_chunk_size`, Default 200 Zeichen) zu einem gemeinsamen Chunk, damit kein fast bedeutungsloser Mini-Chunk einzeln in den Vektorindex kommt. Das generische `CodeParser.chunk_file()` — genutzt von Confluence, Jira, WebDAV, FolderWatch und lokalen Uploads — hat dieses Sicherheitsnetz **nicht**: eine kurze letzte Absatzzeile oder ein isolierter Listeneintrag kann als eigener 3-Wort-Chunk enden, der später als schwaches False-Positive in der semantischen Suche auftaucht (dieselbe Kategorie Problem wie O-074, nur milder, weil kein Datenmüll, sondern nur zu wenig Kontext pro Vektor). | **Erledigt 06.09.2026:** `chunk_file()` bekommt einen neuen Parameter `min_chunk_size` (Default 200, wie bei COBOL) und ruft am Ende die neue Funktion `_merge_small_chunks()` auf — legt aufeinanderfolgende Chunks unterhalb der Mindestgröße zu einem gemeinsamen Chunk zusammen, analog zu `cobol/chunking.py`s Pending-Merge-Logik. Da `chunk_file()` selbst kein Section-Konzept kennt, begrenzt stattdessen dasselbe `boundary_lines`-Set aus O-083 das Zusammenlegen (falls angegeben): zwei Chunks aus unterschiedlichen Confluence-Sections werden nie gemischt, auch wenn beide winzig sind — verhindert, dass O-084 die gerade erst mit O-083 gewonnene Section-Trennschärfe wieder verwischt. Gilt automatisch für **alle** Aufrufer von `chunk_file()` (Confluence, Jira, WebDAV, FolderWatch, lokale Uploads, `git.py`s Fallback für nicht-strukturierte Sprachen), ohne dass ein Aufrufer etwas ändern muss. Tests: neues `tests/test_code_parser_merge.py` (5 Unit-Tests — Default merged kurze Zeilen zu einem Chunk, laufende Summe erzeugt mehrere Gruppen statt eines Riesenchunks, `min_chunk_size=0` schaltet das Mergen ab, keine Vermischung über eine `boundary_lines`-Grenze hinweg, ein bereits großer Chunk wird nicht mit einem kleinen Nachbarn vermengt). Volle Parser-Suite grün (siehe O-085, gemeinsam verifiziert), `ruff check`/`ruff format` sauber, Parser-Worker/-Beat neu gebaut und ausgerollt. | Keine |
| O-085 | Parser / Ingestion-Qualität | Direkt anschließend an O-082 geprüft: `CodeParser.chunk_file()`s innere Schleife bricht vor dem Hinzufügen einer Zeile nur ab, wenn `current_len > 0` UND die neue Zeile das Limit sprengen würde — ist der aktuelle Chunk noch leer (`current_len == 0`), wird die Zeile immer aufgenommen, egal wie lang (verhindert eine Endlosschleife). Ergebnis: eine einzelne Zeile über `chunk_size` wird zu einem eigenen, übergroßen Chunk, ohne weitere Aufteilung. Bei Confluence kann das konkret passieren, wenn `_html_to_text()` einen langen `<p>`-Absatz ohne internes `<br/>` in eine einzige durchgehende Zeile umwandelt. `cobol/chunking.py::_split_paragraph()` löst den analogen Übergrößen-Fall bei COBOL, weil COBOL-Quellzeilen ohnehin durch Zeilenumbrüche strukturiert sind — bei einer einzelnen überlangen Zeile hilft dasselbe Muster nicht, es bräuchte eine Aufteilung innerhalb der Zeile selbst. | **Erledigt 06.09.2026:** neue Funktion `_split_oversized_line()` in `code_parser.py` — sobald `chunk_file()`s Hauptschleife eine einzelne Zeile über `chunk_size` erzwingt (der bestehende `current_len == 0`-Fall), wird diese eine Zeile an der letzten Satzgrenze (`. `/`! `/`? `) innerhalb des Limit-Fensters geteilt, sonst an der letzten Wortgrenze, sonst (kein Leerzeichen im gesamten Fenster, z. B. eine sehr lange URL) hart nach Zeichen geschnitten, damit die Schleife garantiert terminiert. Alle entstehenden Teile behalten `start_line`/`end_line` der ursprünglichen physischen Zeile, da sie alle aus ihr stammen. Gilt wie O-084 automatisch für alle `chunk_file()`-Aufrufer. Tests: neues `tests/test_code_parser_oversized_line.py` (7 Unit-Tests — Zeile unter `chunk_size` bleibt unverändert, mehrteiliger Split ohne Wortverlust, alle Teile tragen dieselbe Quellzeile, Satzgrenze hat Vorrang vor Wortgrenze, Fallback auf Wortgrenze, harter Schnitt bei einem einzelnen zusammenhängenden Token ohne Leerzeichen, exakt `chunk_size` lange Zeile bleibt unangetastet). Volle Parser-Suite 248/248 grün (12 neue Tests zwischen O-084/O-085), `ruff check`/`ruff format` sauber, Parser-Worker/-Beat neu gebaut und ausgerollt. | Keine |
| O-086 | Backend+Frontend / Chat-Auswertbarkeit | 05.09.2026 geprüft ("wie könnten wir Feedback zur Produktverbesserung nutzen"): `ChatMessage.feedback` (`up`/`down`) existiert als Spalte samt `PATCH /chat/messages/{id}/feedback`-Endpunkt und Button in der Chat-UI, wird aber **ausschließlich zurückgelesen**, um den Button-Zustand beim erneuten Laden anzuzeigen (`_serialize_messages()` in `chat.py`) — keine Aggregation, kein Dashboard, keine Auswertung irgendwo im Code. Live gegen die DB geprüft: **0 von 28 Assistant-Nachrichten haben je ein Feedback erhalten** — die Funktion ist bislang komplett unbenutzt, aber selbst wenn genutzt, ginge das Signal aktuell ins Leere. Jede Assistant-`ChatMessage` trägt bereits `sources_json` (tatsächlich zitierte Quellen) und `metadata_json` (Modell/Provider/`agent_steps`) — die Rohdaten für eine Auswertung liegen also schon vor. | Einfache Auswertungsansicht (Admin-Tab oder Export) über negativ bewertete Nachrichten samt `sources_json`/`metadata_json` je Nachricht ergänzen — reine Sichtbarkeit, kein ML. Grundlage für O-087/O-088, die auf demselben Signal aufbauen. | Keine |
| O-087 | Backend+Frontend / Verknüpfungsqualität | **Erledigt 06.09.2026:** Ein Downvote wird jetzt pro zitierter Link-ID als auditierbares Signal gespeichert, nicht anhand eines bloßen Dateinamens geraten: `sources_json` enthält für neu erzeugte Chat-Antworten zusätzlich die konkrete `DocumentChunk.id`; daraus werden nur `EntityDocLink`/`KnowledgeLink` mit genau diesem Chunk ermittelt. Erst zwei aktive Signale aus unterschiedlichen Chat-Sitzungen innerhalb von 30 Tagen setzen einen zuvor freigegebenen Link auf `pending`; mehrere Downvotes derselben Sitzung zählen nur einmal. Ein einzelnes Signal bleibt ein Hinweis in der Chat-UI. Wird ein Downvote zurückgenommen oder durch ein Upvote ersetzt, wird nur sein Signal deaktiviert; eine bereits ausgelöste Prüfung und manuelle Link-Entscheidungen bleiben bestehen. Migration `0008_chat_link_feedback_signals`; Regression deckt Entity- und Knowledge-Links, Zwei-Sitzungen-Schwelle und Rücknahme ab. | Keine |
| O-088 | Backend+Frontend / Antwortqualität | **Erledigt 06.09.2026:** Administratoren können in „Logs & Status" die lokale Sammlung minimierter Downvote-Diagnosefälle aktivieren (Default: aus). Ein Fall enthält nur Frage, Antwort, zitierte Quellen sowie Modell/Provider — keine vollständigen Verläufe und keine Agent-/Tool-Ergebnisse. Die Aufbewahrung ist auf 1–365 Tage konfigurierbar; abgelaufene Einträge werden bei Zugriff oder neuer Erfassung bereinigt und lassen sich vollständig löschen. Ein getrennter zweiter Opt-in erlaubt ausschließlich einen manuellen JSON-Download für den Support; die Anwendung überträgt niemals automatisch Daten nach außen. Wird ein Downvote zurückgenommen, verschwindet sein Diagnosefall wieder. Migration `0009_chat_feedback_diagnostics`, Admin-Gate und Regressionen für Capture, Export-Gate und Rücknahme ergänzt. | Keine |
| O-089 | Produkt / Entscheidung | Bewusst zurückgestellte Idee aus derselben Diskussion: (Frage, upgevotete Antwort, downgevotete Antwort)-Paare wären valide DPO-Präferenzpaare (die leichtgewichtige, moderne Alternative zu klassischem RLHF/PPO — kein separates Reward-Modell nötig). Aktuell aber weder Datenvolumen (0 Feedback-Einträge, siehe O-086) noch eine geklärte Datenhoheits-Frage (Architekturprinzip 1 „On-Premise per Default" spricht gegen zentrales Training über Kundengrenzen hinweg; pro Kunde einzeln trainieren bräuchte dessen ausdrückliche Zustimmung und eigene Trainings-Infrastruktur außerhalb von Ollama). Hier dokumentiert, damit die Idee nicht verloren geht, aber nicht als aktiver Umsetzungsauftrag missverstanden wird. | Zurückstellen, bis (a) bei einem Kunden genug Feedback-Volumen vorliegt UND (b) der Kunde einer Nutzung seiner Chat-Daten für ein eigenes, lokales Fine-Tuning ausdrücklich zustimmt. Kein Code vor dieser Klärung. | Kundenentscheidung + ausreichendes Feedback-Volumen (beides extern, siehe O-086) |
| O-090 | Chat / Antwortqualität | Nutzerbeobachtung 06.09.2026: Fragt man im Code-Editor nach einem bestimmten Wissensobjekt **oder** nach einer bestimmten Zeile, antwortet das LLM zwar auf genau diese Frage — erklärt aber zusätzlich alles andere mit, was zufällig im selben Chunk steht. **Ursache am 06.09.2026 verifiziert:** Der Gutter-Fokus übergibt zwar `pinned_context` mit exakt der angeklickten Zeile (`usePanelNavigation.tsx::handleGutterClick`), der Entity-Fokus wenigstens Startzeile und Objektlabel. `find_pinned_chunks()` lädt dann jedoch bis zu drei vollständige `DocumentChunk`s, die diese Zeile enthalten; `build_pinned_context()` hängt jeweils deren kompletten `chunk.content` in `<untrusted_pinned_code>` an. Nur der seltene Fallback ohne Index begrenzt den Ausschnitt auf ±15 Zeilen. `build_chat_prompt()` nennt den Pin lediglich „primary subject“, enthält aber keine harte Vorgabe, nur den Fokus zu erklären. Damit sieht das Modell regulär den gesamten Chunk und darf ihn auch regulär erläutern — keine Modellhalluzination, sondern fehlende Kontext-/Antwortbegrenzung. | **Offen für den nächsten Arbeitstag:** den fokussierten Bereich als primären Ausschnitt an den Prompt geben (bei Zeilenfokus kleines Fenster, bei Entity-Fokus deren tatsächlichen `start_line`–`end_line`-Bereich) und nur bei notwendiger Abhängigkeit auf den restlichen Chunk verweisen. Die direkte Index-Abfrage muss dafür Entity-Grenzen bzw. ein Fenster berücksichtigen; anschließend Regressionen für Zeilen- und Entity-Fokus ergänzen. | Keine |
| O-091 | Frontend / Panel-Navigation | Nutzerbeobachtung 06.09.2026: „In passender Ansicht öffnen" im Wissensgraph verhält sich bei `.cbl`-Knoten inkonsistent — der Code landet teilweise in der **Doku-Ansicht**, und zusätzlich geht ein **leerer Code-Editor** auf. **Ursache verifiziert am 06.09.2026 im Zuge von O-092 (die zuerst notierte Spur war falsch):** Betroffen ist der **Dokument-Zweig** der Aktion (`KnowledgeGraphView.tsx:877`), nicht der Entity-Zweig. Er feuert zwei voneinander unabhängige Navigationen: `onFileSelect(pathVal, null, sourceIdVal)` bestimmt den Zieltyp allein an der Dateiendung (`getSelectionViewType`) und öffnet für einen Dokument-Knoten namens `SRC/X.cbl` folgerichtig ein **Code**-Panel; unmittelbar danach öffnet `onDocFocus(..., true)` über `handleDocFocusRequest` **unbedingt** ein `doc`-Panel und setzt die globale Doc-Auswahl (`selectedFile: null`), die den eben geöffneten Code-Editor über die Render-Synchronisation wieder leert. Ergebnis: zwei Panels, die `.cbl` in der Doku-Ansicht, daneben ein leerer Editor. Die ursprüngliche Spur (Entity-Zweig, `handleEntitySelect` ruft erneut `handleFileSelect`) trägt nicht: im Panel-Kontext ist `onEntitySelect` an `handlePanelEntitySelect` verdrahtet (`PanelContentRenderer.tsx`), das gar keine Ansicht öffnet. | **Erledigt 06.09.2026:** Der Dokument-Zweig gibt pro Klick nur noch **einen** Navigationsaufruf ab: `onFileSelect` (→ `usePanelNavigation::handlePanelFileSelect`) bestimmt den Zieltyp genau einmal über `getSelectionViewType` und deckt dabei alle drei Fälle ab — `code`, `doc` und `webview`. Der zweite, parallele Aufruf `onDocFocus` ist im Wissensgraphen ersatzlos entfallen, samt Prop und Durchreichung in `SplitPaneWorkspace.tsx`; beide Zweige (Aktion „In passender Ansicht öffnen" und Einfachklick, `openIfMissing=false`) sind gleich behandelt. Für einen Dokument-Knoten `SRC/X.cbl` öffnet sich damit nur noch der Code-Editor — und der ist nicht mehr leer, weil `handlePanelFileSelect` die Entity über `api.resolveEntity` auflöst, bevor die globale Doc-Auswahl ihn überschreiben konnte. `onDocFocus` selbst bleibt als Mechanismus bestehen: der Verweis-Dropdown in `SplitPaneWorkspace.tsx` ist jetzt sein einziger Aufrufer (dort bewusst, weil ein gepinntes Call-Graph-Panel seinen Objektfokus behalten muss). Tests: `PS-15` in `panelSyncMatrix.test.tsx` von `it.fails` auf `it` umgestellt, `PS-16` neu ergänzt, dazu vier Fälle in `components/KnowledgeGraphView.test.tsx` (Einfachklick, Aktion, Confluence-Knoten über `url` statt `file_path` sowie ein struktureller Guard gegen einen wiederkehrenden zweiten Navigationsweg — gegengeprüft: er schlägt an, wenn man den alten Aufruf wieder einsetzt). Frontend-Suite 327 Tests grün, `tsc --noEmit` und ESLint sauber. | Keine |
| O-092 | Frontend / Panel-Synchronisation | Nutzerbeobachtung 06.09.2026: Auch abseits von [O-091] wird eine Aktion in einer Ansicht nicht in jeder Konstellation korrekt in die anderen Ansichten synchronisiert — das Verhalten wirkt fallabhängig, nicht durchgängig. **Ursache der Fallabhängigkeit (06.09.2026 belegt):** Es gibt **zwei** Navigationspfade mit unterschiedlichen Regeln. Pfad A (Sidebar, globale Suche) setzte die globale Auswahl und rief `ensurePanelType`, das nur öffnet, wenn der Typ noch **gar nicht** offen ist; Pfad B (Chat, Editor, Wissensgraph, Call-Graph) löst das Zielpanel über `resolvePanelNavigationTarget` auf. Bei einem nur **eingefrorenen** Zielpanel liefen beide auseinander: Pfad A tat sichtbar nichts, Pfad B überschrieb das eingefrorene Panel. | **Erledigt 06.09.2026:** Matrix als fachliche Festlegung in `docs/PANEL_SYNCHRONISATION.md` (30 Zellen `PS-01`…`PS-30` mit Ist, Soll und Status, Auslöser-Inventar, beide Pfade als Diagramm); Nebenbefund: der geplante dritte Faktor „Layoutmodus" ist keiner — `layoutMode` ist eine reine Funktion der Panel-Anzahl und wirkt nur über die 4-Panel-Grenze, wodurch die Matrix von 140 auf 30 Zellen schrumpft. Entscheidungen **D-1** (Einfrieren schützt in beiden Pfaden; neues `ensureLivePanelType`, `preserveFrozenTarget` als Sonderweg des Call-Graphs entfallen), **D-2** (Hinweis `page.toast.noPanelSpace` statt stillem Abbruch an der Panel-Obergrenze; `addPanel`/`ensurePanelType` melden den Fehlschlag jetzt zurück) und **D-3** (der Call-Graph behält seinen Objektfokus bei einer Auswahl ohne Entity) getroffen und umgesetzt. Automatisiert in `frontend/hooks/panelSyncMatrix.test.tsx` (30 Fälle: 29 grün, `PS-15` als `it.fails` = Reproduktion von [O-091]); die Render-Synchronisationsregel in `useWorkspaceLayout` war zuvor ungetestet. Beim Testschreiben korrigierte sich eine Matrixzeile selbst: PS-23 („Auftauen verliert die Zeile") ist kein Fehler. **D-4** am 06.09.2026 nachgezogen: die Historie eines Live-Panels nimmt alle anderen **Live**-Panels mit, eingefrorene nicht — das entspricht dem Ist-Zustand und ist jetzt als `PS-25` getestet. **Seit dem Fix von [O-091] am 06.09.2026 sind alle 30 Matrixzellen grün** (`PS-15` von `it.fails` auf `it`, `PS-16` ergänzt; 31 Fälle). **Offen:** nur noch D-5 (bleibt der Link-Manager eine Insel?) — als eigener Punkt [O-114] erfasst und damit nicht mehr Teil dieses Punktes. | O-091 (PS-15/PS-16 sind dieselbe Ursache) |

O-093 bis O-113 stammen aus der Nachfrage vom 06.09.2026 („sind damit alle Tests
nachgezogen?"). Statt die Doku zu bestätigen, wurde der tatsächliche Stand geprüft.
Ergebnis: die Reihe O-055–O-062 ist vollständig erledigt, aber sie hatte einen
bewussten Zuschnitt — die **Arbeitsflächen** (Chat, Sidebar, Workspace, Wissens-/
Call-Graph, Link-Manager, Job-Center). Nie auf der Liste standen zwei ganze
Bereiche: die **Einrichtungs- und Administrationsflächen**
(`frontend/components/settings/`, 5.259 Zeilen über elf Tabs, **kein einziger
Test**) und mehrere **Backend-API-Module ohne gezielten eigenen Test**. Dieselbe
strukturelle Ursache wie bei O-055 ff.: Tests entstehen seit 03.09.2026 bei jeder
Änderung, aber unberührter Bestandscode bleibt ungetestet, solange ihn niemand aus
anderem Anlass anfasst. Schnitt deshalb wie dort — ein Punkt pro Tab bzw. pro
API-Modul, damit die Lücke in Häppchen abtragbar bleibt statt als ein
unbeweglicher Brocken liegenzubleiben. Reihenfolge nach Produktkritikalität, nicht
nach ID.

Nicht als eigene Punkte geführt, weil beim Nachprüfen entgegen dem ersten Eindruck
doch abgedeckt: `backend/api/entities.py`, `callgraph.py` und `search.py` (alle
Routen über `tests/test_ap4_graph_api.py`), `audit.py` (`tests/test_mcp_audit.py`)
und `config_router.py` (`tests/test_chat_llm_provider_guard.py`).

**Frontend — Einrichtungs- und Administrationsflächen (`components/settings/`)**

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-093 | Frontend / Tests | `settings/tabs/SourcesTab.tsx` (605 Zeilen) hat keinen Test — die Quellenübersicht ist die Fläche, auf der ein Kunde seinen Wissensbestand tatsächlich verwaltet: `syncKnowledgeSource`, `reindexKnowledgeSource`, `deleteKnowledgeSource`, `updateKnowledgeSourceInterval`, `updateKnowledgeSourceContextNote`. Löschen und Reindizieren sind destruktiv bzw. teuer, sind aber nirgends gegen Fehlbedienung oder falsche Zuordnung abgesichert. | Component-Test analog O-055 ff.: Statusdarstellung je Konnektortyp, Sync-/Reindex-Auslösung (richtige Quelle, richtiger Endpunkt), Löschbestätigung, Intervall- und Kontextnotiz-Änderung inkl. Fehlerfall. | Keine |
| O-094 | Frontend / Tests | `settings/tabs/SourcesSetupTab.tsx` (514 Zeilen) hat keinen Test — der Anlege-Assistent für neue Quellen (`createKnowledgeSource`, `createFolderWatchSource`, `uploadLocalDocument`, `testConnector`). Ein Fehler hier verhindert die Inbetriebnahme beim Kunden, also genau den ersten Schritt nach der Installation ([O-051]). | Component-Test: Konnektorauswahl, Pflichtfeldvalidierung, „Verbindung testen" mit Erfolg und Fehlschlag, Upload-Pfad inkl. der serverseitigen Allowlist aus [O-044] (abgelehnte Endung wird als Fehler angezeigt, nicht verschluckt). | Keine |
| O-095 | Frontend / Tests | `settings/tabs/GitSetupTab.tsx` (869 Zeilen, größte Datei im Baum, gleichauf mit `ProjectsTab`) hat keinen Test — Git-Konnektor-Einrichtung mit `getConnectorRepos`, `getConnectorBranches` (drei Aufrufstellen), `testConnector`, `createGitSource`. Der Git-Import ist der meistgenutzte Ingestionsweg; die Punkte O-071/O-072/O-074 stammen alle aus genau dieser Kette. | Component-Test: Repo- und Branch-Auswahl (inkl. Nachladen bei Repo-Wechsel), Anmeldedaten-Eingabe, Verbindungstest, Anlegen; besonders die Nachlade-Reihenfolge, weil Branch-Listen an der zuvor gewählten Repo hängen. | Keine |
| O-096 | Frontend / Tests | `settings/tabs/ProjectsTab.tsx` (869 Zeilen) hat keinen Test — Projektverwaltung inklusive **Berechtigungen**: `addProjectMember`, `updateProjectMemberRole`, `removeProjectMember`, `getProjectAccessRequests`, `resolveProjectAccessRequest`, `requestProjectAccess`, dazu `updateProject`, `completeProject`, `deleteProject`. Die Backend-Seite dieser Funktionen ist über `tests/test_project_membership.py` abgedeckt, die Oberfläche darüber gar nicht — ein Zuordnungsfehler in der UI ist damit ein Sichtbarkeitsfehler auf fremde Projektinhalte. | Component-Test mit Schwerpunkt Berechtigungen: Rollenwechsel schickt die richtige Nutzer-/Projekt-Kombination, Zugriffsanfrage annehmen/ablehnen, Entfernen des letzten Admins, Projektlöschung mit Bestätigung. Nicht nur Rendering prüfen, sondern die abgesetzten Aufrufe. | Keine |
| O-097 | Frontend / Tests | `settings/tabs/UsersSettingsTab.tsx` (363 Zeilen) hat keinen Test — Nutzerverwaltung mit `createUser`, `updateUser`, `resetUserPassword`, `unlockUser`. Sicherheitsrelevante Aktionen (Passwort zurücksetzen, Konto entsperren nach Login-Drosselung) sind backendseitig getestet (`test_users_admin.py`, `test_login_throttle.py`), an der Oberfläche nicht. | Component-Test: Admin-Gating (Nicht-Admin sieht die Aktionen nicht), Anlegen mit Initialpasswort-Anzeige, Rücksetzen, Entsperren, Deaktivieren — jeweils gegen den richtigen Nutzer. | Keine |
| O-098 | Frontend / Tests | `settings/tabs/TeamsSettingsTab.tsx` (339 Zeilen) hat keinen Test — Teamverwaltung (`createTeam`, `updateTeam`, `deleteTeam`, `addTeamMember`, `removeTeamMember`). Teams sind die Grundlage der Projektsichtbarkeit, ein Fehler wirkt sich wie ein Berechtigungsfehler aus. | Component-Test: Anlegen/Umbenennen/Löschen, Mitglied hinzufügen und entfernen, Verhalten bei bereits zugeordnetem Nutzer, Fehleranzeige bei abgelehntem Aufruf. | Keine |
| O-099 | Frontend / Tests | `settings/tabs/AiSettingsTab.tsx` (478 Zeilen) hat keinen Test — LLM-/Profilkonfiguration, setzt `updateModelInfo` ab. Berührt direkt [O-035] (`POST /model-info` verändert die Modellwahl global im laufenden Prozess statt request-/profilgebunden); ohne Test ist beim Fix von O-035 nicht abgesichert, dass die Oberfläche danach noch das Richtige schickt. | Component-Test: Profilauswahl, Modellwechsel, Speichern (richtige Nutzlast), Fehlerfall bei nicht erreichbarem LLM. Beim Umsetzen von [O-035] als Regressionsnetz voranstellen. | Sinnvollerweise vor O-035 |
| O-100 | Frontend / Tests | `settings/tabs/LogsSettingsTab.tsx` (497 Zeilen) hat keinen Test — Diagnose-/Auditfläche (`getDiagnosticsRuns`, `generateDiagnosticsBundle`, `getMcpToolAuditLogs`, `syncKnowledgeSource`). Genau die Fläche, die im Supportfall benutzt wird; ein Fehler fällt also erst auf, wenn ohnehin schon etwas anderes kaputt ist. | Component-Test: Bundle-Erzeugung inkl. Wartezustand und Download-Link, Auditlog-Filterung, Admin-Gating. Berührt [O-073] (Trace-ID sichtbar machen) — beim Umsetzen mitziehen. | Keine |
| O-101 | Frontend / Tests | `settings/tabs/LayoutSettingsTab.tsx` (326 Zeilen) hat keinen Test — Layout-/Darstellungseinstellungen. Reine Anzeigeeinstellungen, aber sie steuern denselben Layoutzustand, um den es in [O-092] geht; ein stiller Widerspruch zwischen gespeicherter Einstellung und tatsächlichem Panelzustand wäre hier zu finden. | Component-Test: Auswahl wird persistiert und beim Neuaufbau wieder angewandt, Zusammenspiel mit `useWorkspaceLayout` (dort bereits getestet) an der Nahtstelle prüfen. | O-092 (gemeinsame Layout-Semantik) |
| O-102 | Frontend / Tests | Reste des Settings-Baums ohne Test: `settings/tabs/ProjectSetupTab.tsx` (172), `settings/tabs/SourceNetworkGraph.tsx` (227) und `settings/SettingsContext.tsx` (69). Der Context trägt den gemeinsamen Zustand aller Tabs — er ist die Voraussetzung dafür, dass die Tests aus O-093 ff. überhaupt sauber isolierbar sind. | Als **erster** Punkt der Settings-Reihe umsetzen, nicht als letzter: eine Test-Hilfsfunktion, die `SettingsContext` mit definiertem Zustand bereitstellt, spart in O-093 bis O-101 jeweils die halbe Testeinrichtung. Danach die beiden kleinen Komponenten direkt mit abdecken. | Keine |

**Frontend — weitere ungetestete Bestandskomponenten**

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-103 | Frontend / Tests | `components/MarkdownContent.tsx` (537 Zeilen) hat keinen Test — der **selbstgeschriebene** Markdown-Renderer, durch den jede Chat-Antwort läuft: `parseText`, Tabellen-Parsing (`splitTableRow`, `parseTableAlignment`), Überschriften, Codeblöcke, und die Erkennung von Dateiverweisen gegen `knownSources`, die per `onFileClick` die Panel-Navigation auslöst. Damit hängt er direkt an [O-091]/[O-092]. Ein handgeschriebener Parser ohne Test ist die klassische Stelle für stille Fehldarstellungen (zerschossene Tabelle, nicht erkannter Verweis). | Dankbarster Punkt der ganzen Reihe: `parseText` und die Tabellenhilfen sind reine Funktionen und lassen sich ohne Rendering als Unit-Test abdecken. Danach der Component-Test für die Verweis-Erkennung: bekannter Dateiname wird klickbar und meldet die **richtige** Quelle an `onFileClick`. | Keine |
| O-104 | Frontend / Tests | `components/TopicsPanel.tsx` (717 Zeilen) hat keinen Test — Themenverwaltung (Anlegen, Umbenennen, Löschen, Knoten zuordnen/entfernen, Knotensuche) gegen die ebenfalls ungetesteten Endpunkte aus [O-111]. | Component-Test analog O-055 ff.; sinnvollerweise zusammen mit O-111 umsetzen, dann deckt ein Durchgang beide Seiten derselben Funktion ab. | Sinnvoll gemeinsam mit O-111 |
| O-105 | Frontend / Tests | Drei Daten-Hooks ohne Test: `hooks/useProjects.ts` (189), `hooks/useKnowledgeSources.ts` (120), `hooks/useChatSessions.ts` (74). Die Schwestern-Hooks `useChatController`, `usePanelNavigation`, `useWorkspaceLayout`, `useAiSettings` und `useDisplaySettings` sind getestet — diese drei sind bei O-004 entstanden und nie nachgezogen worden. Sie halten den Projekt-, Quellen- und Sitzungszustand der gesamten Oberfläche. | Hook-Tests analog `useChatController.test.tsx`: Laden, Fehlerfall, Aktualisierung nach Änderung, Projektwechsel (kein Nachhallen alter Daten). | Keine |
| O-106 | Frontend / Tests | `app/page.tsx` (959 Zeilen) hat keinen Test — die verbliebene Verdrahtung der Hauptseite nach dem O-004-Umbau. Der Fall ist derselbe wie bei `Sidebar.tsx` in [O-056]: die Bestandteile sind getestet, die Orchestrierung darüber nicht. | Zuletzt angehen, nach O-105 — ein Integrationstest der Seite ist erst dann aussagekräftig, wenn die darunterliegenden Hooks eigene Tests haben. Schwerpunkt: Zusammenspiel von Projektwechsel, Sitzungswechsel und Panelzustand. | O-105 |

**Backend — API-Module ohne gezielten Test**

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-107 | Backend / Tests | `backend/api/projects.py` (977 Zeilen) hat keinen eigenen Test. Der Mitglieder-/Zugriffsanfragen-Teil ist über `tests/test_project_membership.py` abgedeckt, die **inhaltliche Hälfte** dagegen gar nicht: `PATCH /{id}`, `DELETE /{id}`, `POST /{id}/complete`, `GET /{id}/files`, `/{id}/entities`, `/{id}/stats`, `POST /{id}/sync`, `GET /{id}/references`. Andere Tests rufen diese Routen nur beiläufig als Fixture-Aufbau auf, prüfen sie aber nicht. | Eigene `test_projects.py`: Projekt-CRUD inkl. Löschung mit abhängigen Daten, Abschluss (`complete`), Datei-/Entitätslisten mit Projektkontext, `sync`-Auslösung, `references`. Schwerpunkt Projektkontext-Isolation — dieselbe Klasse von Fehlern, die [O-032] sicherheitsrelevant gemacht hat. | Keine |
| O-108 | Backend / Tests | `backend/api/entity_links.py` (388 Zeilen, 9 Routen) hat **keinen einzigen Testaufruf**: Link-Empfehlungen lesen/anlegen/berechnen, Link-Builder-Läufe, `PATCH`/`DELETE` einzelner `EntityDocLink`s, `POST .../llm-review`, Entitäts-Links und `doc-chunks/search`. Das ist die Datengrundlage für die Verknüpfung Code↔Dokumentation (DOC-F-081) und für [O-087]. | Eigene `test_entity_links.py`: Empfehlung anlegen, bewerten, löschen; Projektkontext-Prüfung je Route; `llm-review` mit deaktiviertem LLM (kein 500, sondern definierte Antwort). Die Oberfläche darüber (`LinkManagerView`) ist seit [O-060] getestet — hier fehlt die Gegenseite. | Keine |
| O-109 | Backend / Tests | `backend/api/knowledge_links.py` (306 Zeilen, 7 Routen) hat keinen Testaufruf: Verknüpfungen zwischen Wissensquellen anlegen/lesen/ändern/löschen, `POST /compute`, `GET /runs`, `POST /{link_id}/llm-review`. | Eigene `test_knowledge_links.py`, Zuschnitt wie O-108. Beide Module teilen sich Bewertungs- und Review-Logik — beim Schreiben auf gemeinsame Testhilfen achten statt zu duplizieren. | Sinnvoll gemeinsam mit O-108 |
| O-110 | Backend / Tests | `backend/api/link_chat.py` (305 Zeilen, 4 Routen) hat keinen Testaufruf: `GET /sources`, Nachrichten lesen/senden, `POST /sessions/{id}/confirm`. Ein LLM-gestützter Dialog, der am Ende **Verknüpfungen bestätigt** — also schreibend auf den Wissensgraphen wirkt, ohne jede Absicherung. | Eigene `test_link_chat.py`: Sitzungszugriff nur für Berechtigte, Nachrichtenverlauf, `confirm` legt genau die erwarteten Links an (und nicht mehr), Verhalten bei `LLM_MODEL=disabled` analog `test_chat_llm_provider_guard.py`. | Keine |
| O-111 | Backend / Tests | `backend/api/topics.py` (176 Zeilen, 8 Routen, admin-only) hat keinen Testaufruf: Themen-CRUD, Knotenzuordnung, `GET /search-nodes`. Das Admin-Gating (`require_admin` am Router) ist damit für dieses Modul nie geprüft worden. | Eigene `test_topics.py`: Admin-Gating zuerst (Nicht-Admin bekommt 403 auf jeder Route), dann CRUD und Knotenzuordnung. Gemeinsam mit [O-104] umsetzen. | Sinnvoll gemeinsam mit O-104 |
| O-112 | Backend / Tests | `backend/api/diagnostics.py` (125 Zeilen) hat keinen Testaufruf: `POST /generate`, `GET /runs`, `GET /runs/{id}/download` — plus ein zweiter, **öffentlicher** Router (`public_router`), der ohne `require_admin` eingebunden ist (`main.py`). Ein Diagnose-Bundle enthält Betriebsdaten; welche Routen davon wirklich öffentlich sein sollen, ist nirgends per Test festgehalten. | Eigene `test_diagnostics.py`: zuerst festhalten, was `public_router` bewusst offenlegen darf und was nicht (das ist die eigentliche Klärung), dann Bundle-Erzeugung und Download inkl. Admin-Gating. | Keine |
| O-113 | Backend / Tests | `backend/api/system.py` (118 Zeilen) ist nur über `/health` und einen einzelnen `/model-info`-Aufruf berührt; `GET /version`, `GET /models` und vor allem `POST /model-info` haben keinen gezielten Test — genau die Route, die in [O-035] als prozessglobaler Nebeneffekt beanstandet ist. | Kleiner, schnell zu schreibender `test_system.py`. Beim Umsetzen von [O-035] vorher schreiben, damit die Verhaltensänderung (global → request-/profilgebunden) belegt statt behauptet ist. | Sinnvollerweise vor O-035 |

O-114 bis O-116 stammen aus der Frage vom 06.09.2026, was mit dem Link-Manager
perspektivisch geschehen soll (Entscheidung D-5 aus
[docs/PANEL_SYNCHRONISATION.md](PANEL_SYNCHRONISATION.md)). Für die Antwort wurde
geprüft, was die Ansicht heute tatsächlich tut — dabei sind zwei Befunde
aufgetaucht, die vorher in keinem Punkt standen.

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-114 | Frontend / Link-Manager | Der Link-Manager ist das einzige Werkzeug, mit dem ein Mensch die Qualität des Wissensgraphen kuratiert (Posteingang `pending`/`approved`/`rejected`, Konfidenzschwelle, LLM-Review je Link, manuelles Verknüpfen). Genau die Frage, die man dort beantworten muss — „stimmt diese Verknüpfung?" — lässt sich dort aber nicht beantworten: sichtbar sind Titel, Typ und Konfidenzwert, **nicht** der Code und **nicht** der Doku-Abschnitt. Als einziges Panel hat `LinkManagerView` keinerlei Navigations-Rückrufe (`PanelContentRenderer.tsx`, `linkmanager`-Zweig), man kann also nicht in die Code-/Doku-Ansicht springen (PS-30). | Beide Seiten eines Links öffenbar machen: „Code-Seite öffnen" / „Doku-Seite öffnen" über die vorhandenen `handlePanelFileSelect`/`handleDocFocusRequest`-Rückrufe, die jedes andere Panel schon bekommt. Der Aufwand ist klein, seit die Zielauflösung samt Einfrier-Regel zentral in `usePanelNavigation` liegt (O-092/D-1). Damit wäre D-5 zu „nein, keine Insel" entschieden. | Keine (O-092/D-1 erledigt die Vorarbeit) |
| O-115 | Backend / Toter Code | Bei O-114 entdeckt: `backend/api/link_chat.py` (305 Zeilen, 4 Routen, LLM-gestützter Dialog zum Finden von Verknüpfungen) wird **von keinem Client aufgerufen** — kein `link-chat` im gesamten Frontend, in keinem Skript, in keiner Doku. `POST /link-chat/sessions/{id}/confirm` legt dabei `KnowledgeLink`-Zeilen direkt mit `status="approved"` an, also **am pending/approve-Workflow des Link-Managers vorbei**. Zugriffsschutz ist vorhanden (Sitzungseigentümer, Sichtbarkeitsprüfung je Seite), Tests gibt es keine ([O-110]). | Entscheiden statt liegen lassen: entweder an den Link-Manager anschließen (er wäre dessen natürliches zweites Standbein: „hilf mir, Verknüpfungen zu finden") oder entfernen. Eine tote, ungetestete, schreibende LLM-Route ist für die Repo-Übergabe die schlechteste der drei Möglichkeiten. Bei „entfernen" wird [O-110] gegenstandslos. | Produktentscheidung |
| O-116 | Frontend / Konsistenz | Bei O-114 entdeckt: `LinkManagerView.tsx` spricht das Backend mit rund 20 direkten `fetch(${API_URL}/…)`-Aufrufen an, statt über die zentrale `api`-Schicht (`app/services/api.ts`). Damit greift für diese Ansicht der globale 401-Interceptor aus [O-067] **nicht** — bei abgelaufener Sitzung sieht der Nutzer im Link-Manager Fehlermeldungen statt der Anmeldemaske. Dasselbe gilt für jedes künftige Querschnittsthema (Trace-ID aus [O-073], einheitliches Fehlerformat, Wiederholungslogik). | Die `fetch`-Aufrufe auf `api.*` umstellen und die fehlenden Methoden dort ergänzen. Sinnvoll gemeinsam mit O-114, weil dieselbe Datei ohnehin angefasst wird. | Keine |

### Mainframe-/COBOL-Kompatibilität (O-117–O-156)

Anlass: Kunden sollen ihre bestehende Mainframe-Umgebung anbinden können, ohne
dass ein erfolgreicher Import fälschlich vollständige Dialektunterstützung
suggeriert. Details und belegte Ist-Befunde:
[Mainframe-Kompatibilität](MAINFRAME_KOMPATIBILITAET.md).
P0 = Korrektheit/Grundlage, P1 = allgemeine Flexibilität, P2 = kundenspezifischer
Ausbau. Alle Punkte sind **offen**; Beispiele sind keine Support-Zusagen.
Erweiterungen über den bisherigen v1-Umfang sind ausdrücklich markiert.

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-117 | P0 / Kundenprofil | Zielumgebung systematisch erfassen statt „COBOL“ als ausreichende Spezifikation zu behandeln. | Aufnahmebogen für OS, Compiler/Version, Buildoptionen, Formate/CCSID, Bibliotheken, SCM, Batch/Online, Subsysteme und Transferweg. **Abnahme:** freigegebene Beispielkonfiguration mit unbekannten Feldern ausdrücklich markiert und priorisierten Kundenszenarien. | Kundenangaben für reale Abnahme; Vorlage sofort möglich |
| O-118 | P0 / Testmatrix | Keine verifizierte Kompatibilitätsmatrix je Compilerfamilie, Version und Merkmal. | Synthetische sowie freigegebene reale Fixtures, erwartete Entities/Kanten/Zeilen und Negativfälle erfassen; die zwei reproduzierten Direktivenfehler zuerst aufnehmen. **Abnahme:** CI-Matrix mit Status unterstützt/teilweise/nicht geprüft; optionale Herstellercompiler-Referenzläufe getrennt von normalen Unit-Tests. GnuCOBOL ist kein Ersatz für Herstellerverifikation. | O-117; vorhandene Golden-Tests |
| O-119 | P0 / Parserdiagnosen | ANTLR-Syntaxfehler werden derzeit nicht strukturiert an `ParseResult` weitergegeben. | Lexer-/Parser-/Präprozessor-Diagnosen mit Code, Schwere, Originaldatei/-zeile, betroffener Phase und Profil ausgeben; Zahl/Textmenge begrenzen und Wiederholungen bündeln. **Abnahme:** unbekannte Syntax erzeugt reproduzierbare Diagnose, ohne den gesamten Import abzubrechen; SLL-Fehler bei anschließend erfolgreichem LL-Lauf nicht als endgültigen Fehler melden. | Keine; O-118 liefert Regressionen |
| O-120 | P0 / Analysequalität | `errors` leer wird als `ok` interpretiert; Nutzer sehen nicht zuverlässig, ob der Graph vollständig ist. | Status vollständig/teilweise/Textfallback/übersprungen samt Gründen von Parser über Persistenz/API bis Editor, Graph, Chat und Diagnosebericht durchreichen. **Abnahme:** inaktiver/ungeklärter Code oder fehlende Struktur erscheint nie als uneingeschränkt analysiert; vorhandene Quellen kompatibel migrieren. | O-119, O-150; O-063/O-078 beachten |
| O-121 | P0 / Buildprofile | Dialekt und Buildkontext sind keine expliziten Parser-Eingaben. | Versioniertes Profil für Compilerfamilie/-version, Format, Encoding, Defines, Bibliotheksfolge und unterstützte Analyseoptionen; nachvollziehbare Vererbung Quelle → Pfad/Member → Buildvariante. Quelltextdirektiven nach den Regeln des jeweiligen Profils auswerten. **Abnahme:** effektives Profil reproduzierbar, Konflikte sichtbar, unbekannte Optionen diagnostiziert; keine automatische Dialekterkennung als Gewissheit ausgeben. | O-117, O-119 |
| O-122 | P1 / Reindexierung | Derselbe Quelltext kann nach Änderung von Defines, Codepage oder Suchpfaden andere Bedeutung haben. | Analyse-Fingerprint aus Quellrevision, Profil, Parser-/Grammatikversion und relevanten Bibliotheken bilden. **Abnahme:** Profilwechsel aktualisiert betroffene Entities/Kanten auch ohne Inhaltsänderung; unveränderte Profile bleiben inkrementell; alte und neue Varianten werden nicht vermischt. Embeddings nur bei geändertem zu indexierendem Inhalt neu erzeugen. | O-121, O-137, O-150 |
| O-123 | P0 / Quellformat | Reproduziert: eingerücktes `>>SOURCE FORMAT FREE` wird als Fixed erkannt; Struktur geht verloren. | Explizite Vorgaben und unterstützte Formatdirektiven auswerten, Wechsel innerhalb einer Datei und konfigurierbare Spaltengrenzen berücksichtigen; Heuristik nur als gekennzeichneten Fallback verwenden. **Abnahme:** Fixed/Free und bestätigtes Variable-/Extended-Format samt Fortsetzungen haben korrekte Originalzeilen, auch bei Text jenseits Spalte 72. | O-118, O-121 |
| O-124 | P0 / Bedingte Kompilierung | Reproduziert: `>>IF 1 = 1`/`>>ELSE` erzeugt CALLs aus beiden Zweigen bei leerer Fehlerliste. | Unterstützte DEFINE/IF/EVALUATE-Varianten, Verschachtelung und externe Defines profilabhängig auswerten. **Abnahme:** bekannte inaktive Zweige erzeugen keine aktiven Kanten; unbekannte Bedingungen bleiben als Bedingungen erhalten; Ausdrucksauswertung führt keinen Quellcode aus; Originalzeilen bleiben stabil. | O-118, O-119, O-121, O-150 |
| O-125 | P1 / Lexikalische Regeln | Kommentare, Debug-Zeilen, Compilerzeilen und Continuation werden bisher vereinfacht behandelt. | Regeln je bestätigtem Format/Dialekt prüfen: Debug-Modus, `$SET`/CBL/PROCESS soweit benötigt, Tabulatoren, Wort-/Literalfortsetzung und Kommentarzeichen innerhalb von Literalen. **Abnahme:** Paartests „Kommentar vs. Literal“ und „Debug an/aus“ verändern nur die erwarteten Tokens/Kanten. | O-118, O-121, O-123 |
| O-126 | P1 / Bezeichner und Literale | ASCII-Faltung für ANTLR und eigener Tokenizer müssen dieselben Namen zuverlässig abbilden. | Erlaubte Sonderzeichen, nationale/Unicode-/Hex-Literale sowie APOST/QUOTE-Varianten anhand Kundenprofil prüfen. Originalschreibweise und kanonische Vergleichsform getrennt halten. **Abnahme:** keine Kollisionen durch Normalisierung, korrekte Tokenpositionen, nicht unterstützte Zeichen sichtbar; nicht mit Byte-Dekodierung verwechseln. | O-118, O-121, O-127 |
| O-127 | P1 / Quelltext-Encoding | O-074 lehnt Nicht-UTF-8 ab; Copybook-Vorlauf dekodiert dagegen mit `errors="ignore"`. | Gemeinsamen Decoder für Import und Copybook-Vorlauf schaffen. Explizite CCSID/Codepage pro Quelle/Member, z. B. bestätigte EBCDIC-Varianten; bereits konvertierte Exporte erkennen bzw. deklarieren lassen. **Abnahme:** kein stiller Zeichenverlust oder doppeltes Transcoding, identische Decode-Regeln in allen Pfaden, Binärfilter bleibt wirksam. **Erweiterung gegenüber F-028/v1.** | O-121, O-128; O-074 und fachlicher Zielumfang |
| O-128 | P1 / Rekordformate | Eine exportierte Mainframe-Datei muss nicht aus LF-getrennten Textzeilen bestehen. | Importvertrag für bereits normalisierten Text oder deklarierte Rekorddaten (RECFM/LRECL bzw. Quellformatspezifikation); benötigte feste/variable Rekorde, RDW/BDW nur bei entsprechendem Exportformat. **Abnahme:** Rekordnummer ↔ Originalzeile/Bytebereich nachvollziehbar, keine abgeschnittenen oder erfundenen Zeilen; unsupported Formate ablehnen. | O-117, O-129; bestätigte Exportformate |
| O-129 | P1 / Importmanifest | Pfad allein beschreibt Dataset-Member und Bibliotheksversion nicht eindeutig. | Versioniertes Manifest für Plattform, Bibliothek/Dataset, Member/Element, Version/Typ, Exportzeit, Revision, Encoding, Rekordformat, Prüfsumme und Profilreferenz. **Abnahme:** gleichnamige Member kollidieren nicht, Quelle bleibt bis zum Original nachvollziehbar; unvollständiger Export führt nicht zu automatischer Löschung vermeintlicher Orphans. | O-117 |
| O-130 | P1 / Quellanbindung | O-042 nennt Zugangswege, aber es fehlt ein gemeinsamer Vertrag für lesende Mainframe-Exporte. | Unter O-042 festlegen: Inventar/Paginierung, Inhalt, Metadaten, Versionen, Vollständigkeit, Tombstones, Fortsetzung und Berechtigungsfehler. **Abnahme:** zwei Test-Adapter liefern denselben Manifestvertrag; Authentifizierung/Transport bleiben je Adapter austauschbar, keine zusätzliche ungenutzte Service-Schicht. | O-042, O-129 |
| O-131 | P2 / z/OS-Zugang | PDS/PDSE/USS-Bestände ohne geeignete Git-Bridge benötigen einen bestätigten Leseweg. | Nach O-042 bevorzugt freigegebenen Export oder vorhandene z/OSMF-REST-Schnittstelle anbinden. **Abnahme:** Memberliste/-inhalt/-revision, Zeichensatzmetadaten, Pagination, Resume und Teilberechtigungen an Testsystem geprüft; lesende Operationen, kein Jobsubmit. Kein Zowe-/SDK-Paket ohne eigene Lizenzprüfung. | O-130, O-152; Kundenendpunkt und Rechte |
| O-132 | P2 / BS2000-Zugang | BS2000-Dateien und Bibliothekselemente müssen mit ihrem eigenen Namens-/Versionskontext aufgenommen werden. | Freigegebenen Exportweg für Dateien bzw. PLAM/LMS-Elemente, Elementtyp/-version und Compilerprofil definieren; nativen Adapter erst bei belegter Schnittstelle. **Abnahme:** gleichnamige Elemente verschiedener Versionen/Bibliotheken getrennt, COPY-Auflösung aus konfigurierten Bibliotheken reproduzierbar. | O-130, O-135; Kundenexport/-zugang |
| O-133 | P2 / SCM und Stufen | Git-Bridge bzw. Endevor-/ChangeMan-Export kann Umgebung, Stage und Version verdecken. | O-042 konkretisieren: vorhandene Bridge nutzen, Mapping von Element/Typ/Stage/Revision auf Manifest nachweisen; native Anbindung nur bei verbleibender Lücke. **Abnahme:** DEV/TEST/PROD nicht unbemerkt zusammenführen, Transfers/Löschungen inkrementell korrekt, kein produktives Promotion-/Checkout-Schreiben. | O-042, O-129, O-130; Kundensystem |
| O-134 | P1 / Artefakterkennung | Endungsbasierte Klassifikation reicht für Member ohne Suffix oder kundeneigene Typkennungen nicht. | Manifesttyp und konfigurierbare Bibliotheks-/Pfadregeln mit dokumentierter Priorität verwenden; Inhaltsheuristik nur als Hinweis. **Abnahme:** COBOL, Copybook, JCL/PROC und Daten/Binärdateien korrekt getrennt; benutzerdefinierte Copybook-Endungen auch bei Namensauflösung konsistent berücksichtigen. | O-129; O-078/O-074 abstimmen |
| O-135 | P1 / COPY-Suchreihenfolge | Vorhandene quellenweite COPY-Auflösung kennt keine compilergetreue Bibliotheksverkettung. | Konfigurierte Suchreihenfolge, OF/IN-Qualifikation, Membernormalisierung und freigegebene gemeinsame Bibliotheken berücksichtigen. **Abnahme:** zwei gleichnamige Copybooks werden je Buildprofil deterministisch aufgelöst; Ambiguität und fehlende Berechtigung führen nie zum geratenen Treffer; vollständiger Suchpfad als Diagnose. | O-121, O-129, O-134; E-2 |
| O-136 | P1 / Textsubstitution | COPY REPLACING ist vorhanden; globale REPLACE-/weitere Compiler-Substitutionsregeln brauchen separate Prüfung. | Unterstützten Umfang von REPLACE/OFF, Pseudotext, LEADING/TRAILING und geschachtelter Substitution pro Profil festlegen und implementieren. **Abnahme:** Definitionen und USES-Ziele nach Substitution korrekt; Ursprungsdatei/-zeile erhalten; keine Expansion im angezeigten Originaltext. | O-118, O-121, O-135; E-2 |
| O-137 | P1 / Copybook-Kontext | Derselbe Copybook-Inhalt kann in verschiedenen Builds unterschiedliche Defines, Substitutionen oder Abhängigkeiten haben. | Kontextabhängige Analyse/Cache-Schlüssel und transitive Abhängigkeitsinvalidierung für Copybooks einführen. DATA-/PROCEDURE-/ENVIRONMENT-Verwendung sowie Zyklen prüfen. **Abnahme:** Änderung eines verwendeten Copybooks aktualisiert betroffene Aufrufer; Varianten bleiben getrennt; kein kompletter Bestands-Neuimport bei jeder Änderung. | O-124, O-135, O-136 |
| O-138 | P1 / Programmgrenzen | Mehrere bzw. verschachtelte Programme und ENTRY-Zugänge benötigen nachgewiesene Scope-Behandlung. | Compilation Units mit mehreren PROGRAM-IDs, verschachtelten Programmen, END PROGRAM und ENTRY gegen Herstellerfixtures prüfen/ergänzen; CLASS-ID/METHOD-ID/INVOKE nur bei bestätigtem OO-COBOL-Bedarf als eigenen Teilumfang aufnehmen. **Abnahme:** gleiche Paragraph-/Feldnamen bleiben programmlokal; ENTRY ist dem korrekten Programm zugeordnet; Chunks und Aufrufziele springen nicht über Programm-/Methodengrenzen. | O-118, O-150; E-1 |
| O-139 | P2 / Sprachmerkmale | Datenbeschreibungen und Anweisungen können über den getesteten COBOL85-Umfang hinausgehen. | Benötigte USAGE/PIC-/NATIONAL-/BINARY-/POINTER-/TYPEDEF-/OCCURS-Varianten sowie XML/JSON, intrinsische Funktionen oder alte Kontrollflussformen einzeln inventarisieren und in kleine Folge-Tickets schneiden; Grammatik, Visitor und eigene Scanner gemeinsam prüfen. **Abnahme:** Hierarchie, Attribute und Referenzen korrekt; ungesicherte Bytegrößen/Layouts oder Kontrollflussziele nicht behaupten, relevante Compileroptionen berücksichtigen. | O-118, O-121, O-156 |
| O-140 | P1 / Aufrufauflösung | PROGRAM-ID, ENTRY, Loadmodulname und Alias müssen nicht derselbe Name sein; dynamische CALLs sind nicht sicher auflösbar. | Namenskonventionen und bereitgestellte Binder-/Buildzuordnungen einbeziehen; statische Literale, konfigurationsbelegte Aliase und begrenzte Datenflusskandidaten unterscheiden. **Abnahme:** kein geratener eindeutiger Treffer für dynamische Aufrufe; Beleg und Buildvariante je Ziel sichtbar. | O-121, O-138, O-146, O-150 |
| O-141 | P2 / Db2 und SQL | Bestehende SQL-Analyse deckt nicht automatisch Präcompiler-/DBMS-Varianten ab. | Db2-/andere tatsächlich genutzte SQL-Dialekte, EXEC SQL INCLUDE, SQLCA/DCLGEN, Host-/Indikatorvariablen, Cursor und dynamisches SQL gegen Fixtures prüfen. **Abnahme:** Include-Herkunft und sichere Tabellen-/Feldbezüge korrekt, dynamische SQL-Ziele als unbekannt/bedingt ausgewiesen; keine Datenbankausführung. | O-118, O-135, O-146, O-150 |
| O-142 | P2 / CICS | EXEC-CICS-Maskierung bewahrt Text, liefert aber keine vollständigen Online-Abhängigkeiten. | Benötigte LINK/XCTL/START/RETURN-Transfers sowie READ/WRITE-Dateien, BMS-Maps und COMMAREA-/Channel-Bezüge mit explizitem Umfang ergänzen; bereitgestellte Ressourcenexporte separat auswerten. **Abnahme:** feste und dynamische PROGRAM-Ziele unterschieden, Ressourcenauflösung mit Herkunft, keine CICS-Laufzeitaktionen. | O-118, O-140, O-150; Ressourcenexport optional |
| O-143 | P2 / IMS | DL/I-Schnittstellenaufruf und fachliches Programm-/Datenbankziel sind unterschiedliche Beziehungen. | Bestätigte CBLTDLI/CEETDLI-/EXEC-DLI-Formen sowie bereitgestellte PSB/PCB-/DBD-Metadaten abbilden. **Abnahme:** Funktionscode, Schnittstellenaufruf und belegte Segment-/Datenbankbezüge getrennt; ohne Metadaten ehrliche Teilanalyse, keine erfundenen Anwendungs-CALLs. | O-118, O-150; Kundenfixtures/Metadaten |
| O-144 | P2 / Weitere Subsysteme | Kunden können openUTM/KDCS, UDS/SQL, SESAM, Adabas, IDMS, MQ oder eigene Präprozessoren verwenden. | Je bestätigtem System ein eigenes Folge-Ticket mit Versionsumfang, Syntax-/Metadatenbeispiel und erwarteten Beziehungen schneiden; vorhandene Calls zunächst als externe Schnittstellen erhalten. **Abnahme:** erste benötigte Integration mit Goldens; keine Universalunterstützung aus einer Keyword-Liste ableiten. | O-117, O-118, O-150; Produktumfang je System |
| O-145 | P2 / JCL-Grundstruktur | JCL/PROC werden derzeit ausschließlich als Text indexiert (F-026). | Bei bestätigter Umfangserweiterung JOB/EXEC/PGM/PROC/DD, INCLUDE, Fortsetzungen, In-stream-Daten und Kommentare strukturieren. **Abnahme:** Job → Step → Programm/Prozedur mit Originalzeilen; Datenzeilen nicht als JCL interpretieren; keine Jobausführung. | O-118, O-134, O-150; Erweiterung F-026/v1 |
| O-146 | P1 / Buildbelege | Reale Compileroptionen, Präprozessoren und Modulzuordnungen stehen häufig außerhalb der COBOL-Datei. | Versionierte Buildmanifeste und benötigte Compiler-/Binder-Listings importieren, Analyseprofil/-revision zuordnen; generierte/präprozessierte Zeilen auf Originalquellen abbilden. **Abnahme:** belegte Optionen und Aliase nachvollziehbar, fremde/veraltete Listings nicht still anwenden; unbekannte Listingformate diagnostizieren. | O-121, O-129; freigegebene Beispielexporte |
| O-147 | P2 / Batch-Datenfluss | Programm-Dateinamen, DD-Namen und physische Datasets brauchen einen gemeinsamen Ausführungskontext. | Auf O-145 aufbauend PROCs, SET-Symbole, Overrides, Bibliotheksfolge und Bedingungen auswerten; FILE-CONTROL/SELECT/ASSIGN mit passenden DDs verbinden. **Abnahme:** Dataset-/GDG-Ausdrücke und bedingte Beziehungen erhalten; unbekannte Laufzeitsymbole nicht durch Vermutungen ersetzen. | O-121, O-145, O-150 |
| O-148 | P2 / Betriebssteuerung | Scheduler, BS2000-SDF-Prozeduren und Utility-Steuerkarten können relevante Abhängigkeiten außerhalb von COBOL/JCL enthalten. | Nach O-117 konkrete Exportformate priorisieren, etwa einen Scheduler oder eine SORT-/IDCAMS-/SQL-Steuerkarte; pro Format separates Folge-Ticket. **Abnahme:** statische Steuerung, deklarierte Abhängigkeit und tatsächlicher Laufnachweis getrennt; nur bereitgestellte Artefakte auswerten. | O-129, O-145, O-150; Kundennutzen/Exports |
| O-149 | P2 / Gemischte Sprachen | COBOL ruft gegebenenfalls Assembler, PL/I, C oder andere Programme auf. | Externe Sprachgrenzen im Graphen erhalten und freigegebene Symbol-/Build-Mappings nutzen. Erst bei Bedarf genau einen weiteren Strukturparser integrieren. **Abnahme:** unbeobachtete Innensemantik bleibt unbekannt, kein Verlust externer CALLs; echte Erweiterung folgt O-077–O-081 statt eines leeren Frameworks. | O-140, O-150; O-077–O-081 bei Strukturparser |
| O-150 | P0 / Evidenz und Varianten | Zusätzliche Dialekt-/Subsystemdaten dürfen nicht als gleich sichere Kanten zusammenfallen. | Gemeinsamen Vertrag für ursprüngliche Quellstelle, Buildvariante, Bedingung, Auflösungsstatus und Belegart definieren; vorhandene Entities/Kanten kompatibel erweitern. **Abnahme:** gleiche Namen in verschiedenen Varianten kollidieren nicht, Herkunft/Unsicherheit bleiben über Persistenz, API, Graph, Export und Chat erhalten. | O-119; bestehende Modell-/API-Verträge, E-1/E-2 |
| O-151 | P1 / Einrichtung und Diagnose | Kunden brauchen vor dem Vollimport eine verständliche Einschätzung ihrer Konfiguration. | Stichproben-Preflight für Format/Encoding/Profile/Copybooks anbieten, mit manuellen Overrides und Konflikthinweisen; Profilimport/-export ohne Secrets. **Abnahme:** Meldung nennt Datei, Ursache und mögliche Korrektur, zeigt wirksames Profil und ungetestete Merkmale; Änderungen nutzen O-122. | O-120, O-121, O-122, O-127, O-135 |
| O-152 | P1 / Betrieb der Anbindung | Kundennetz, Authentifizierung und Bibliotheksrechte müssen zum gewählten Importweg passen. | Offline-Export als vollwertigen Weg erhalten; für bestätigte Adapter Proxy, private CA, benötigte Authverfahren, Credential-Rotation, Rate-Limits und Teilberechtigungen prüfen. **Abnahme:** keine Secrets im Profil/Diagnoseexport, keine unberechtigten Quellen in Suche/Chat, Abbruch oder Rechteverlust löscht nicht den Bestand. | O-130; bestehende Auth-/Projekt-/Diagnosemechanismen |
| O-153 | P1 / Skalierung und Wiederaufnahme | Varianten, Copybook-Abhängigkeiten und Mainframe-Inventare dürfen Laufzeit/Speicher nicht unkontrolliert vervielfachen. | Streaming, Checkpoints, stabile Inventarsnapshots und gezielte Reanalyse messen; begrenzte Parallelität/Timeouts auch für ANTLR und Präprozessoren. **Abnahme:** Abbruch/Resume ohne Doppelobjekte, konfigurierbare Ressourcenlimits und nachvollziehbarer Fortschritt; unvollständige Scans markieren. | O-122, O-129, O-137; O-001/O-075 |
| O-154 | P1 / Kundenabnahme | Eine synthetische Suite allein belegt keinen konkreten Kundencompilerbestand. | Repräsentative Stichprobe nach Compiler/Version/Format/Subsystem auswählen und mit bestätigten Referenzergebnissen vergleichen; Ziele für Strukturtreue, Fehlkanten, Unresolved-Anteil, Originalzeilen und Durchsatz vorab vereinbaren. **Abnahme:** versionierter Bericht je Profil, dokumentierte Restlücken; keine pauschale Prozentzusage ohne Messbasis. | O-117, O-118, O-120, O-153; O-001/Kundenbestand |
| O-155 | P1 / Erweiterungsleitfaden | Weitere Dialekte sollen gezielt ergänzt werden können, ohne Parser-Forks je Kunde. | Nach erstem implementiertem Profil dokumentieren: Profiloption, Testfixture, Normalisierung/Grammatik/Visitor/Scanner, Diagnose, Migration und CI. **Abnahme:** Entwickler ergänzt einen belegten Dialektfall anhand Anleitung; kein Kundenname als fest verdrahteter Codepfad und keine ungenutzte Plugin-Abstraktion. | O-118, O-121, O-156; O-081 für echte weitere Sprachen |
| O-156 | P1 / Reproduzierbarkeit und OSS | Dialektanpassungen betreffen generierte Grammatik und gegebenenfalls fremde Testkorpora. | Generator-/Runtime-/Grammatikstände pinnen, lokale Änderungen dokumentieren und Regenerierungsvergleich automatisieren; Provenienz/Rechte pro Fixture und Erweiterung prüfen. **Abnahme:** Golden-Änderungen ausdrücklich geprüft, generierter Diff reproduzierbar, bestehende MIT-/BSD-/Apache-Regel eingehalten; proprietäre Compiler nur kundenseitig bereitgestellte optionale Testwerkzeuge. | O-064/O-065 vorhanden; E-11 und OSS-Clearing |

### Offline-Deployment erneut verifizieren (O-157)

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-157 | Betrieb / Offline-Deployment | Vollständigen Offline-Installationslauf mit aktuellem Versionsstand einschließlich O-070 wiederholen. Historischer Nachweis: 31.07.2026, Commit `c1b1c23`, Bundle-Build, Installation, gesunde Dienste und Login; eine technisch erzwungene Internetsperre ist dort nicht belegt. Die elf O-070-Tests simulieren Docker-Ausgaben und ersetzen diesen Durchlauf nicht. | **Offen, 06.09.2026 — auf Nutzerwunsch für später vorgemerkt, jetzt nicht ausführen.** Bundle auf einem Online-Buildhost erstellen; auf einer separaten frischen Testinstanz mit gesperrtem externem Netzwerk und ohne vorab geladene Anwendungsimages/-modelle installieren. Prüfsummen, Image-/Modellimport, Migrationen, alle Dienste, Health-/Versions-Endpunkte, Frontend, Erststart-Zugangsdaten im Installer, Login und verpflichtenden Passwortwechsel prüfen; lokale Verarbeitung und Embeddings mit `bge-m3` testen. Erneuten Installerlauf und Update mit vorhandenen Testdaten prüfen: Daten und geändertes Passwort bleiben erhalten, alte Zugangsdaten werden nicht erneut ausgegeben. Vorgegebenes Bootstrap-Passwort separat prüfen: keine Passwortausgabe. Version, Netzsperre, Befehle und Ergebnisse ohne Klartextpasswörter dokumentieren; Modellumfang explizit festhalten. Produktiven Stack und produktive Daten unangetastet lassen. | O-070 umgesetzt; separate Testinstanz und aktuelles Offline-Bundle |

### Deployment-Doku gegen den OIDC-Code (O-159)

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-159 | Betrieb / Doku vs. Code | Am 06.09.2026 auf Nachfrage („ist ein OIDC-Login bereits implementiert?") gefunden: `docs/DEPLOYMENT.md` und `docs/deployment-customer.md` beschrieben einen Login, den es so nie gab. Sie nannten `OIDC_ISSUER_URL` (der Code liest `OIDC_ISSUER`), eine frei konfigurierbare `OIDC_REDIRECT_URI` (existiert nicht — der Wert wird in `core/oidc.py::redirect_uri()` aus `API_URL` abgeleitet), den Callback-Pfad `/auth/callback` statt `/auth/oidc/callback` und ein `ADMIN_EMAILS`, das im Backend nirgends vorkommt. Beide Dokumente verwiesen außerdem für den Wegwerf-Keycloak auf `.github/keycloak/doctus-realm.json` samt Login-Theme — dieses Verzeichnis hat es im Repo **nie** gegeben (`git log --all` auf den Pfad ist leer). Wer sich an die Anleitung hielt, bekam vom IdP „Invalid parameter: redirect_uri" und von Doctus gar keine Fehlermeldung. Der O-041-Eintrag hielt fest, die Kundendoku beschreibe den Ablauf „bereits korrekt" — das stimmte auf der Ebene des Konzepts, nicht auf der der Variablennamen. | **Erledigt 06.09.2026:** Beide Dokumente auf den tatsächlichen Code gezogen (richtige Variablennamen, abgeleitete Redirect-URI statt eigener Variable, echter Callback-Pfad, Bootstrap-Superuser statt `ADMIN_EMAILS`, SSO durchgängig als *optionaler zweiter* Anmeldeweg statt als Voraussetzung). `.github/keycloak/doctus-realm.json` neu angelegt: Realm `doctus`, Client `doctus-backend`, Nutzer `testuser`/`testpass`; das in der Doku beschriebene Login-Theme ist entfallen statt erfunden zu werden. **Erstmals ohne synthetische Tokens nachgewiesen:** Wegwerf-Keycloak 25 mit dieser Realm-Datei gestartet und den vollständigen Authorization-Code-Flow gegen ein Backend aus dem aktuellen Image gefahren — `ssoEnabled`, Redirect zum IdP, Keycloak-Loginformular, Rücksprung mit `code`/`state`, Session-Cookie, `GET /auth/me` liefert den frisch angelegten `testuser` (Rolle `user`, ohne lokales Passwort); ein zweiter Durchlauf findet dasselbe Konto wieder, statt ein zweites anzulegen. Zwei Fallstricke fielen dabei erst im echten Lauf auf und stehen jetzt in der Doku: Keycloak akzeptiert **keinen Platzhalter im Hostnamen** einer Redirect-URI, und `CLIENT.DESCRIPTION` ist auf 255 Zeichen begrenzt — ein längerer Text lässt den Realm-Import mit einem SQL-Fehler abbrechen. Neuer Regressionstest `backend/tests/test_oidc_docs_match_code.py` (5 Fälle) nagelt fest, dass die Doku nur Variablen nennt, die `core/config.py` auch liest, den echten Callback-Pfad verwendet, kein `ADMIN_EMAILS` verspricht und die Realm-Datei zur abgeleiteten Redirect-URI passt — gegengeprüft gegen den alten Stand: vier der fünf Fälle schlagen dort fehl. | Keine |


### SSO-Betrieb beim Kunden (O-160 bis O-164)

Der Anmeldeweg selbst funktioniert ohne weitere Arbeit — am 06.09.2026 unter
[O-159] gegen einen echten Keycloak 25 durchgespielt, vom Redirect bis zum
angelegten Nutzer. Die folgenden Punkte sind das, was zwischen „der Login geht"
und „das kann ein Kunde produktiv betreiben" liegt. Sie sind bewusst nur
erfasst, nicht umgesetzt.

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-160 | SSO / Betriebsrisiko | `core/oidc.py::_jwks()` und `_discover()` füllen ihren Cache einmal pro Prozess und verwerfen ihn nie. Rotiert der IdP seine Signaturschlüssel — Keycloak tut das im Normalbetrieb —, findet `KeySet.import_key_set(_jwks())` den `kid` des neuen Tokens nicht mehr, und **jeder** SSO-Login scheitert an der Signaturprüfung, bis jemand `backend-api` neu startet. Der Modul-Docstring nennt den Neustart als bewusste Annahme („Deployment, das den IdP wechselt oder dessen Signaturschlüssel rotiert, braucht ohnehin einen Neustart"); für den Kunden ist es ein Login-Ausfall ohne erkennbaren Anlass, und die Fehlermeldung („Anmeldung fehlgeschlagen") zeigt nicht auf die Ursache. Betrifft nur SSO, der Passwort-Login läuft weiter. | Beim Verifizieren eines ID-Tokens einmalig neu laden, wenn der `kid` unbekannt ist (JWKS erneut holen, Cache ersetzen, genau einen zweiten Versuch), statt sofort zu scheitern. Begrenzen, damit ein kaputter IdP nicht bei jedem Loginversuch einen Abruf auslöst. Test: Token mit rotiertem Schlüssel gegen einen Cache mit dem alten JWKS. | Keine |
| O-161 | SSO / Abmeldung | `POST /auth/logout` (`api/auth.py`) löscht nur die eigene Session-Cookie; das `end_session_endpoint` des IdP wird nirgends aufgerufen (im gesamten `core/oidc.py` kein Treffer). Nach „Abmelden" lebt die IdP-Sitzung weiter — ein Klick auf den SSO-Knopf meldet dieselbe Person ohne jede Eingabe sofort wieder an. Auf einem geteilten Arbeitsplatz sieht der Nutzer eine Abmeldung, die faktisch keine ist. | RP-initiated Logout ergänzen: nach dem lokalen Abmelden zum `end_session_endpoint` weiterleiten (mit `id_token_hint` und `post_logout_redirect_uri`). Setzt voraus, dass das ID-Token (oder wenigstens dessen `sid`) für die Dauer der Sitzung verfügbar bleibt — heute wird es nach der Prüfung verworfen, das ist der eigentliche Umbau. Fachlich zu entscheiden: soll „Abmelden" auch die IdP-Sitzung beenden (und damit andere Anwendungen mit abmelden) oder nur Doctus? | Fachliche Entscheidung zum Umfang der Abmeldung |
| O-162 | Sicherheit / Sitzungen | **Sicherheitsrelevant, betrifft beide Anmeldewege:** `core/auth_dependency.py::get_current_user` lädt den Nutzer über die `user_id` aus der signierten Cookie und prüft **nicht**, ob `is_active` noch gesetzt ist. `is_active` wird nur beim Login geprüft (`api/auth.py`, `core/oidc.py::provision_or_link_user`). Ein deaktiviertes Konto behält damit vollen Zugriff, bis seine Cookie abläuft — bis zu 14 Tage (`SESSION_MAX_AGE_SECONDS`). Die Deaktivierung in der Nutzerverwaltung wirkt also erst beim nächsten Anmeldeversuch, nicht sofort; genau das erwartet aber jeder, der ein Konto sperrt. | `get_current_user` um die `is_active`-Prüfung ergänzen (401 bei deaktiviertem Konto) — kleiner Eingriff, aber an der zentralen Stelle jedes Requests, deshalb mit Test für beide Richtungen. Beim Umsetzen mitentscheiden, ob eine Rollenänderung (superuser → user) ebenso sofort greifen soll. | Keine |
| O-163 | SSO / Sitzungsdauer | Doctus stellt nach dem SSO-Login seine **eigene** Sitzung über 14 Tage aus und hält danach keinerlei Verbindung zum IdP: weder Refresh-Token noch Ablauf der IdP-Sitzung spielen eine Rolle. Sperrt der Kunde jemanden im IdP, kommt die Person mit einer bestehenden Doctus-Sitzung bis zu zwei Wochen weiter herein (O-162 behebt nur den Fall, dass das Konto **in Doctus** deaktiviert wird). Für die compliance-getriebene Zielgruppe ist das eine Frage, die im Kundengespräch kommt. | Zuerst fachlich festlegen, welche Zusage gelten soll: kürzere Sitzung für SSO-Konten, regelmäßige Nachprüfung beim IdP (Refresh-Token bzw. Userinfo-Abruf) oder bewusst beibehalten und im Kundendokument benennen. Erst danach umsetzen — die drei Varianten unterscheiden sich stark im Aufwand. | Fachliche Entscheidung; sinnvoll gemeinsam mit O-162 |
| O-164 | SSO / Rechtevergabe | `core/users.py::create_oidc_user` legt jedes SSO-Konto mit Rolle `user` und **ohne** Teamzugehörigkeit an; IdP-Gruppen oder `roles`-Claims werden nicht ausgewertet (bewusst so entschieden, E-12: „Rechte kommen weiterhin aus Doctus selbst"). Folge im Betrieb: Wer sich neu per SSO anmeldet, sieht zunächst nichts und muss von einem Administrator einem Team zugeordnet werden. Bei einer Handvoll Nutzern unauffällig, bei einer Konzerneinführung mit dutzenden Nutzern ist es genau so oft Handarbeit. | Bedarf beim Zielkunden klären. Falls gewünscht: konfigurierbare Abbildung von IdP-Gruppen/-Claims auf Doctus-Teams und die Admin-Rolle, mit einem Standard, der ohne Konfiguration dem heutigen Verhalten entspricht. Die E-12-Entscheidung dabei bewusst neu bewerten statt sie zu übergehen. | Fachliche Priorisierung / Kundengröße |


### Link-Manager bei großen Vorschlagsmengen (O-165)

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-165 | Frontend/Backend / Link-Manager | Nutzerbeobachtung 11.09.2026: Sobald der Link-Manager geöffnet wird und die Vorschlagsliste groß ist (Messung am Bestand: 1072 Knowledge-Links `pending` plus 518 Entity-Links), bricht die Bedienbarkeit der Ansicht spürbar ein. **Ursachen am 11.09.2026 gemessen, drei Stück:** (1) `LinkManagerView.tsx` rendert die vereinigte Liste ungefenstert — bei diesem Bestand ~1590 Karten gleichzeitig im DOM, und jeder Tastendruck im Suchfeld baut sie neu auf; (2) `fetchKnowledgeLinks` holte die Tab-Zähler, indem es **alle drei Statuslisten vollständig** lud und nur ihre Länge nahm — im Offen-Tab also dieselben 1072 Vorschläge (572 KB JSON) gleich zweimal; (3) `list_knowledge_links` prüfte die Sichtbarkeit über `is_side_visible` einzeln pro Link-Seite: für einen Nicht-Admin gemessen **5361 SQL-Queries und 1,41 s** allein im Backend (für einen Superuser greift die Kurzschluss-Abfrage, dort war es 1 Query). | **Erledigt 11.09.2026:** (1) Die Liste rendert gefenstert über `@tanstack/react-virtual`, gleiches Vorgehen wie `VirtualizedSessionList`/`FileTreeList` aus [O-036]; die `ScrollArea` weicht dafür einem direkt referenzierbaren Scroll-Container, `measureElement` gleicht die variablen Kartenhöhen an. (2) Neuer Endpunkt `GET /knowledge-links/counts` liefert die drei Tab-Zähler (mit `min_score`/`source_type` wie die Liste, gemeinsame Filterfunktion, damit Liste und Zähler nicht auseinanderlaufen); das Frontend macht statt vier Abfragen noch zwei. (3) Neue `LinkVisibilityIndex` in `api/knowledge_links.py` lädt Team-/Projekt-Scope und alle referenzierten Entities/Chunks samt Projekten/Wissensquellen gebündelt: **5361 → 9 Queries, 1,41 s → 0,02 s**. `is_side_visible` bleibt für Einzel-Links (PATCH/DELETE/`llm-review`) unverändert bestehen; die Gleichwertigkeit beider Wege ist gegen den echten Bestand über alle acht Team-/Projekt-Kombinationen ohne Abweichung geprüft. Tests: `backend/tests/test_knowledge_links_listing.py` (Zähler je Status, `min_score`, Anmeldepflicht, Sichtbarkeit über Projektgrenzen in beide Richtungen, und eine konstante Query-Obergrenze als Regressionsschutz gegen das N+1); zwei Fälle in `LinkManagerView.test.tsx` (nur der sichtbare Ausschnitt im DOM, Zähler über `/counts` statt über die Statuslisten). **Bewusst nicht mitgemacht:** echte serverseitige Pagination — als [O-166] unter „Zurückgestellte Entwicklungspunkte" erfasst, samt Auslöser und dem, was dann in einem Zug mit dazugehört. | Keine |

### Laufzeit-Images geben den Quellcode preis (O-167)

| ID | Bereich | Punkt | Status / nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-167 | Backend/Parser / Deployment-Hygiene | Geprüft 11.09.2026 auf Nutzeranfrage: `backend/Dockerfile` und `parser/Dockerfile` sind Single-Stage und kopieren mit `COPY . .` den kompletten Quellbaum (`api/`, `core/`, `services/`, `models/`, `agent.py`, `mcp_client.py`, `tests/` …) unverändert ins Laufzeit-Image; es gibt kein `.dockerignore` im jeweiligen Build-Kontext (`parser/.dockerignore` schließt nur `__pycache__`/`*.pyc` aus). Jeder mit `docker exec` in den laufenden Container oder mit Zugriff auf die Image-Datei (`docker save` + entpacken) liest damit denselben Code wie im Repo. Keine `.env`/Secrets betroffen (in keinem der beiden Build-Kontexte vorhanden), aber der komplette Python-Quellcode. Das `frontend/Dockerfile` ist bereits sauber (Multi-Stage, Runtime-Stage kopiert nur `.next`/`public`/`package.json`/`node_modules`, keine Sourcemaps aktiviert). | **Erledigt 11.09.2026:** Beide Dockerfiles auf Multi-Stage umgestellt. Builder-Stage installiert `build-essential`/`libpq-dev` + `pip install --user` (Backend zusätzlich `vendor/unidecode_shim`, landet als installiertes Paket in `/root/.local`, der Quellordner selbst geht nicht in die Runtime-Stage). Runtime-Stage kopiert nur `/root/.local` plus die tatsächlich für den Betrieb nötigen Dateien/Ordner (Backend: `api/`, `core/`, `models/`, `services/`, `alembic/`, `alembic.ini`, `main.py`, `agent.py`, `mcp_client.py`, `docker-entrypoint.sh`; Parser: `cobol/`, `connectors/`, `core/`, `models/`, `tasks/` + die Top-Level-`.py`-Dateien) — kein `tests/`, kein `requirements-dev.txt`, kein Build-Werkzeug, kein `vendor/`-Quellbaum. Parser behält `git`/`tesseract-ocr`/`tesseract-ocr-deu`/`poppler-utils` in der Runtime-Stage (werden dort tatsächlich aufgerufen), `build-essential`/`libpq-dev` bleiben auf die Builder-Stage beschränkt (psycopg2-binary bringt `libpq` bereits mit). Neu: `backend/.dockerignore` (vorher keins) und `parser/.dockerignore` um `tests/`/`.pytest_cache/`/`requirements-dev.txt` erweitert, damit diese den Build-Kontext gar nicht erst erreichen. **Verifiziert:** beide Images frisch gebaut; `docker run` gegen beide Images bestätigt `tests/` und `requirements-dev.txt` sind nicht mehr im Image; `docker compose up` mit `backend-api`+`parser-worker`+`parser-beat` gegen echte `db`/`redis`/`ollama` — Alembic-Migration lief, alle drei Container wurden `healthy`, Backend beantwortet `/health`/`/config/features`, Parser-Worker meldet `ready`/ANTLR-COBOL-Warmup, Beat `healthy`. Keine Anwendungslogik geändert, daher keine neuen Pytest-Fälle nötig (CI installiert Test-Dependencies ohnehin direkt auf dem Runner, nie im Laufzeit-Image — unverändert). **Bewusst nicht behoben, weil laut ursprünglicher Einschätzung technisch nicht vollständig lösbar:** Python bleibt interpretiert und muss als Quelltext vorliegen, um zu laufen — `docker exec`/`docker save` zeigen weiterhin die eigentliche Anwendungslogik (`api/`, `core/`, `services/`, `models/`, COBOL-Parser etc.), nur nicht mehr Tests/Dev-Tooling/Build-Kette obendrein. Echter Schutz (kein Code-Einblick über den API-/URL-Zugang hinaus) bräuchte weiterhin ein anderes Betriebsmodell (Nutzer selbst hostet, gibt nur API-Zugang heraus, keinen Server-/Container-Zugriff) — das ist eine Entscheidung, keine Codeänderung. **Nutzerentscheidung 11.09.2026:** Restrisiko als vertretbar eingestuft (On-Premise-Produkt — wer `docker exec`/Image-Export gegen `doctus-backend`/`doctus-parser`/`doctus-beat` fahren kann, hat als Betreiber ohnehin schon vollen Infrastrukturzugriff; keine Secrets im Image; kein klassisches Sicherheitsloch, nur IP-Sichtbarkeit gegenüber dem eigenen Betreiber). Betriebsmodell-Wechsel bewusst nicht verfolgt. | Erledigt — Restrisiko vom Nutzer akzeptiert, keine weitere Aktion offen |

| O-168 | Backend+Parser / Agent-Kontextfenster | Nutzerfrage 11.09.2026 (angestoßen durch eine Kollegenfrage zu Claude Codes Auto-Compact): "was passiert, wenn der Chat zu lang wird?" Befund: Die Chat-Historie pro Anfrage ist bereits gedeckelt (`backend/api/chat.py` lädt nur die letzten 6 Nachrichten). Zwei echte Lücken gefunden: (1) Für Ollama (Default-Pfad, On-Premise) wurde nirgends `num_ctx` gesetzt — `backend/services/chat_service.py`, `backend/agent.py` und `parser/ollama_client.py::get_chat_json` liefen alle auf Ollamas eingebautem Default-Kontextfenster (deutlich kleiner als das, was `mistral-nemo` könnte), das bei Überlauf **stillschweigend von vorne kürzt** (System-Prompt, abgerufenes Wissen, frühe Agent-Werkzeugergebnisse können unbemerkt rausfallen). (2) Die Agent-Werkzeugschleife (`backend/agent.py`, `max_turns = 8`) hängt bei jedem Werkzeugaufruf das volle Ergebnis unbegrenzt an `messages`/`contents` an; lokale Repo-Werkzeuge sind zwar schon vernünftig gedeckelt (`view_repo_file` 300 Zeilen, `list_repo_files` 250 Dateien, `search_repo_code` 80 Treffer), aber MCP-Werkzeuge (Jira/Confluence etc.) waren komplett ungedeckelt — ein einzelnes großes Confluence-Dokument oder Jira-Ticket hätte unbegrenzt viel Kontext verbraucht. | **Erledigt 11.09.2026:** (1) Neue Einstellung `OLLAMA_NUM_CTX` (`backend/core/config.py`, Default 8192, env-konfigurierbar) an allen drei Ollama-Aufrufstellen verdrahtet — `chat_service.py`/`agent.py` als Top-Level-Feld `num_ctx` im OpenAI-kompatiblen `/v1/chat/completions`-Request (per PR ollama/ollama#6137 unterstützt), `parser/ollama_client.py` als `options.num_ctx` im nativen `/api/chat` (eigene Env-Variable, da das Modul nicht von `core.config` importiert). In `docker-compose.yml`/`docker-compose.offline.yml` für `backend-api` und `parser-worker` durchgereicht, in `.env.example` dokumentiert. (2) Neue `_cap_tool_result()` in `agent.py` deckelt MCP-Werkzeugergebnisse auf `MAX_MCP_TOOL_RESULT_CHARS` (8000 Zeichen) mit sichtbarer Kürzungsnotiz ("[… gekürzt: N weitere Zeichen entfernt …]"), angewendet an der einen Stelle (`execute_tool()`), die alle drei Provider-Zweige (OpenAI/Ollama, Anthropic, Gemini) gemeinsam durchlaufen — lokale Repo-Werkzeuge bleiben unverändert (schon sinnvoll gedeckelt, eine zusätzliche Deckelung hätte dem Agenten echten COBOL-Lesekontext genommen). Die Kürzungsnotiz landet unverändert im `tool_result`-Schritt, den das Frontend (`AgentSteps.tsx`) ohnehin je Werkzeugaufruf anzeigt. **Nachbesserung noch am 11.09.2026, auf Nutzernachfrage ("bekommt der Nutzer davon überhaupt etwas mit?"):** die Notiz allein wäre erst nach zwei manuellen Klicks sichtbar gewesen (Zwischenschritte-Panel ausklappen, dann das einzelne Werkzeugergebnis ausklappen) — für die meisten Chats de facto unsichtbar. Jetzt trägt jedes `tool_result`-Event/-Schritt ein `truncated`-Flag (neues `_tool_result_was_truncated()`, an allen drei Provider-Zweigen gesetzt, per SSE und in `metadata_json` gespeichert), das in `AgentSteps.tsx` ein **immer sichtbares** Amber-Badge im Header auslöst ("N× Werkzeugergebnis gekürzt (Kontextfenster)", mit Tooltip) — unabhängig davon, ob das Zwischenschritte-Panel ein- oder ausgeklappt ist. Neue Tests: `test_ollama_agent_loop_flags_truncated_mcp_result` (Backend, prüft SSE-Event und `agent_steps` auf `truncated: true`), `chatStream.test.ts` (zwei neue Fälle für das Flag inkl. Default `false`). `npx tsc --noEmit`, ESLint und volle Vitest-Suite (351 Tests) grün; Backend/Frontend-Images neu gebaut und ausgerollt. **Bewusst nicht umgesetzt:** eine Claude-Code-artige echte Kompaktierung (ältere Turns nachträglich zusammenfassen/verdichten statt nur neue MCP-Ergebnisse zu deckeln) — die drei Provider-Formate (flache OpenAI/Ollama-`messages`-Liste, Anthropic-Content-Blocks, Gemini-`parts`) sind strukturell verschieden genug, dass ein generischer Trim-Mechanismus über alle drei hinweg ohne Bruch der `tool_call_id`-Paarung ein separates, größeres Vorhaben wäre; die jetzige Kombination (num_ctx angehoben + einzige unbegrenzte Quelle gedeckelt) schließt die konkret gefundene Lücke. **Verifiziert:** neue Tests `backend/tests/test_agent.py` (Cap-Verhalten + `num_ctx` im Ollama-Payload der Agentenschleife), `backend/tests/test_chat_service.py::test_ollama_stream_sets_explicit_num_ctx`, `parser/tests/test_ollama_client.py::test_get_chat_json_sets_explicit_num_ctx` — alle grün. Volle Suiten sauber: Backend 211/211 (zusätzlich 4 Fehlschläge durch fremdkontaminierte Testdatenbank identifiziert und auf frischer DB als Fehlalarm bestätigt), Parser 249/249 auf frischer DB inkl. erreichbarem Ollama (die 3 sonst bekannten Ollama-Konnektivitäts-Fehlschläge aus O-064 reproduziert und als Umgebungsfrage bestätigt, nicht als Regression). Backend-/Parser-Images neu gebaut und ausgerollt, `OLLAMA_NUM_CTX=8192` im laufenden Container bestätigt. | Keine |


## Noch zu evaluieren

- O-006 (Git-Performance) — bewusst aus den aktuell offenen
  Implementierungsaufträgen herausgenommen. Nach der providerübergreifenden
  Kompatibilitätsabsicherung für GitHub und Bitbucket soll anhand eines großen,
  repräsentativen Bestands gemessen werden, ob `git repack -a -d` und
  `git fetch --refetch` nach der Ersteinindexierung sinnvoll sind. Zu erfassen
  sind Erst-/Folgesync, Mirror-/Worktree-Größe, Netzwerktraffic sowie CPU-/IO-
  Aufwand. Kein aktueller Release-Blocker.

## Umsetzungs- und Übergabestatus

Die Arbeitspakete AP-0 bis AP-9 sind technisch als abgeschlossen dokumentiert.
Drei Punkte sind Übergaben an Fujitsu/DRV: die formale Lasttest-Abnahme am
echten Bestand, die formale BITV-Abnahme sowie eine mögliche
Farbkontrast-Nachbesserung nach Markenfreigabe. Der synthetische Lasttest und
der automatisierte axe-core-Basischeck sind bereits vorhanden.

Die technischen Abschlussarbeiten (npm-CVE-Fix und Behebung des
Embedding-Timeout-Problems) sind erledigt; die zugehörigen Entscheidungen
stehen im [Entscheidungslog](ENTSCHEIDUNGEN.md).

## Bewusst nicht als offene Codeänderung geführt

Diese Punkte sind derzeit keine ungeklärten Implementierungsaufträge:

- Der Embedding-CPU-Timeout wurde durch Sub-Batches und einen konfigurierbaren
  Timeout behoben (E-8, 08.08.2026).
- Der frühere GPL-Fund `Unidecode` wurde durch das MIT-Shim ersetzt (E-7,
  08.08.2026); er ist kein Release-Blocker mehr.
- Die Refaktorierung des `SettingsModal` bis zur Shell und die Aufteilung der
  Tabs ist erledigt. Die verbliebenen `page.tsx`-Nacharbeiten aus O-004/O-005
  und O-027 sind ebenfalls abgeschlossen.
- Die frühere Testlücke bei Orphan-Schutz und Chunked-Downloads wurde in AP-8
  geschlossen.

## Erledigt, zuletzt verschoben

- O-009 (quellenweiter Copybook-Feldindex / XREF) — 02.09.2026 umgesetzt;
  Copybooks werden als eigene Entities geparst, verschachtelte COPYs transitiv
  vererbt, `REPLACING` vollständig angewendet und USES-Kanten pfadgenau auf
  Copybook-Felder nachaufgelöst. COPYs aus der PROCEDURE DIVISION erzeugen
  keine Datenfeldvererbung. Mit Parser-, Persistenz- und Connector-Regressionen
  verifiziert.

- O-022 (MCP-Audit) — 02.09.2026 umgesetzt; MCP-Tool-Aufrufe werden pro Chat-Turn
  mit Nutzer, Session, Projekt/Quelle, Tool, Status und Dauer protokolliert.
  Argumente und Fehlermeldungen werden vor der Speicherung datensparsam bereinigt;
  eine konfigurierbare Aufbewahrung von standardmäßig 90 Tagen sowie eine
  admin-only Einsicht im Logs-Tab sind umgesetzt. Fünf neue Backend-Audit-Tests,
  32 Frontend-Tests, Typecheck, ESLint und Produktions-Build grün.
- O-020 (Link Manager / Layout) — 02.09.2026 umgesetzt; Link-Manager-Header,
  Filterleiste, manuelle Picker und Linkkarten reagieren jetzt auf die tatsächliche
  Panelbreite. In schmalen Zellen werden Inhalte container-basiert gestapelt oder
  gekürzt, sodass der Link Manager auch im Vier-Panel-Layout ohne horizontales
  Überlaufen nutzbar bleibt. 30 Frontend-Tests, Typecheck, ESLint und
  Produktions-Build grün.
- O-021 (View-Layout) — 02.09.2026 umgesetzt; im Vier-Panel-Layout steuert ein
  gemeinsamer Kreuzgriff das horizontale und vertikale Verhältnis per Pointer-Drag.
  Die Mindestgrößen werden bei Maus- und Touch-Eingaben eingehalten, die Werte im
  Workspace-Snapshot gespeichert und beim Wiederherstellen validiert. 32 Frontend-
  Tests, Typecheck, ESLint und Produktions-Build grün.
- O-004 (Frontend-Domänen-Hooks) — 02.09.2026 umgesetzt; Projekt-, Quellen-,
  Chat-Session- und Workspace-Layout-Zustände sind aus `app/page.tsx` in
  englisch dokumentierte Hooks ausgelagert. Typecheck, Lint, 18 Frontend-Tests
  und Produktions-Build grün.
- O-005 (gezielte Frontend-Performance) — 02.09.2026 umgesetzt; weitergereichte
  Handler verwenden stabile Callback-Identitäten, der Settings-Context erhält
  ein memoisiertes Value-Objekt und die unabhängig renderbaren Header-/Sidebar-
  Bereiche sind memoisiert. Typecheck, Lint, 18 Frontend-Tests und
  Produktions-Build grün.
- O-024 (Chat-Orchestrierung) — 02.09.2026 umgesetzt; Chat-Streaming, Senden,
  Retry/Regenerate, Session-Auswahl/-Löschung und Teilen in
  `useChatController` ausgelagert. URL-Synchronisierung und Workspace-Restore
  bleiben als App-weite Brücken erhalten. Mit drei gezielten Controller-
  Regressionstests sowie insgesamt 21 Frontend-Tests, Typecheck, ESLint und
  Produktions-Build verifiziert (Commit `bd0f3b9`).
- O-025 (Panel-/Datei-/Entity-Navigation) — 02.09.2026 umgesetzt; Routing,
  Fokusweitergabe, History-Navigation sowie Fixierung, Live-Panels und mobile
  Navigation in `usePanelNavigation` ausgelagert. Die gemeinsame Referenzauflösung
  liegt in `referenceTarget`. Mit sechs gezielten Regressionstests sowie insgesamt
  27 Frontend-Tests, Typecheck, ESLint und Produktions-Build verifiziert.
- O-026 (Workspace-Rendering) — 02.09.2026 umgesetzt; Panel-Chrome und Fokusleiste
  liegen in `PanelRenderer`, Mobile-Tabs sowie Desktop-Split/Grid-Layout in
  `WorkspaceShell`. Das Mapping über stabile Panel-Indizes, Panel-History und
  Layoutwechsel bleiben erhalten. 27 Frontend-Tests, Typecheck, ESLint und
  Produktions-Build grün.
- O-027 (AI-Settings) — 02.09.2026 umgesetzt; `useAiSettings` kapselt
  Profilmigration, aktives Profil, Profilparameter, Modellinformationen und
  verfügbare Modelle inklusive localStorage-/API-Lifecycle. `useDisplaySettings`
  kapselt Theme- und Editorpräferenzen inklusive Hydration-sicherer
  Wiederherstellung. Chat, Link Manager und Settings verwenden nun denselben
  Profilwechselpfad; 30 Frontend-Tests, Typecheck, ESLint und Produktions-Build
  grün.
- O-028 (Panel-Content-Rendering) — 02.09.2026 umgesetzt; Chat, Code-,
  Dokument-, Graph-, Web-, Call-Graph- und Link-Manager-Inhalte liegen in
  `PanelContentRenderer`. `page.tsx` ist dadurch von 1.096 auf 947 Zeilen
  reduziert; Panel-State und bereichsübergreifende Navigation bleiben im
  Orchestrator. Auf ausdrückliche Vorgabe wurde für diesen Entwicklungsschritt
  kein Test- oder Buildlauf gestartet.
- O-019 (Chat / LLM-Fokus) — 02.09.2026 umgesetzt; Projekt, Quelle und Pin
  werden pro Chat-Turn als kanonischer Fokus-Snapshot in Request und
  Nachrichtenmetadaten übernommen. Retry/Regenerate verwendet dadurch den
  ursprünglichen Turn-Fokus statt einer späteren UI-Auswahl; Legacy-Metadaten
  bleiben lesbar. Mit sechs gezielten Hook-Tests, Typecheck, ESLint und
  Produktions-Build verifiziert.
- O-018 (Job Center) — 02.09.2026 umgesetzt; abgeschlossene und fehlgeschlagene
  Vorgänge bleiben in der Job-Übersicht sichtbar. Admins können fehlgeschlagene
  Jobs aus der UI neu anstoßen; erfolgreiche Jobs bleiben schreibgeschützt und
  laufende Jobs werden gegen Doppelstarts geschützt. Erfolgreiche Einträge
  können per X aus dem Job-Center entfernt werden, ohne die zugrunde liegende
  Wissensquelle zu löschen; Run-basierte Wiederholungen erzeugen einen neuen
  Lauf und erhalten die Historie. Backend-Berechtigungs-, Queue- und
  Entfernen-Tests sind ergänzt; Frontend-Tests, Typecheck, ESLint,
  Produktions-Build und Service-Smokechecks sind grün.

- O-017 (Fixierte Views / Historie) — 02.09.2026 umgesetzt; die History-Transitionen
  sind jetzt unabhängig von der Fixierung gekapselt. Zurück-/Vorwärtsnavigation
  arbeitet in fixierten und live Panels gleich; beim Aufheben einer Fixierung wird
  die bisherige feste Auswahl als Verlaufseintrag erhalten. 14 Frontend-Tests,
  Typecheck und Produktions-Build grün.
- O-016 (Knoten-Icons) — 02.09.2026 umgesetzt; zentrale Icon-Zuordnung für
  Dokumente, Code-Entitäten und Webquellen in Graph-, Such-, Topic-, Link- und
  Referenzansichten ergänzt, einschließlich Canvas-Darstellung im Knowledge- und
  Call-Graph. Unit-Tests für die Typzuordnung sowie Frontend-Test, Typecheck und
  gezielter Lintlauf grün.
- O-015 (Call Graph / Code View) — 01.09.2026 umgesetzt; Call-Graph-Klicks navigieren bevorzugt in ein live/unfixiertes Code-Panel oder öffnen bei verfügbarem Platz ein neues Code-Panel. Fixierte Code-Views bleiben dabei unverändert; der Routingpfad ist durch Tests für live, fixiert und volle Panelbelegung abgesichert.
- O-011 (Teilen-Funktion) — 01.09.2026 umgesetzt; die Chat-Teilen-Aktion liegt jetzt in der globalen Header-Bar und verwendet weiterhin den bestehenden Link-/Clipboard-Mechanismus inklusive Rückmeldung bei fehlendem Chat.
- O-023 (Chat/Darstellung) — 01.09.2026 umgesetzt; die leere `refs`-Liste wurde nicht mehr als sichtbare `0` gerendert, sodass jede Nutzernachricht ohne Kontext sauber beginnt. Das „ME“-Label wurde durch ein lokales, originales Demo-Mitarbeiter-Avatarbild ersetzt; die Bildbeschreibung ist in Deutsch und Englisch hinterlegt.
- O-014 (Chat/Fokus) — 01.09.2026 umgesetzt; das Projekt-Badge kann über einen zugänglichen X-Button geschlossen werden, nutzt dabei den zentralen Projektwechselpfad und zeigt im allgemeinen Kontext ein „Allgemein“-Badge.
- O-013 (Berechtigungen) — 01.09.2026 umgesetzt; Projekt-, Quellen- und Graph-Zugriffe prüfen jetzt Team- und Projektmitgliedschaft konsistent, globale Modellumschaltung ist auf globale Admins begrenzt, Projektmitglieder können nur aus dem eigenen Team hinzugefügt werden und das Frontend lädt dafür eine projektbezogene Kandidatenliste. Regressionstests für Quellenzugriff, Modellumschaltung und Projektmitgliedschaft ergänzt.
- O-012 (UI-Standarddarstellung) — 01.09.2026 umgesetzt; zentrale Desktop-Skalierung auf 110 %, mobile Basis auf 100 % gesetzt und in den Design Guidelines dokumentiert.
- O-010 (Chat-View schließen und erneut öffnen) — 01.09.2026 umgesetzt; der Chat verwendet nun denselben Schließen-Button wie alle anderen Views und wird über „Ansicht hinzufügen“ mit frischem Panelzustand wieder geöffnet.
- E-7 (`Unidecode`/GPL) — 08.08.2026 durch das MIT-Shim gelöst.
- E-8 (Embedding-Batchgröße und CPU-only-Timeout) — 08.08.2026 umgesetzt.
- AP-8-Testlücken (Orphan-Schutz, Chunked-Download) — in AP-8 geschlossen.
- `SettingsModal`-Tab-Aufteilung — Schritt 2 abgeschlossen; die verbleibenden
  `page.tsx`-Arbeiten sind als O-004 und O-005 weitergeführt.

- O-086 (Chat-Auswertbarkeit) — 06.09.2026 umgesetzt; der ausschließlich für
  Administratoren zugängliche Endpunkt `GET /admin/chat-feedback` liefert die
  letzten 100 negativ bewerteten Antworten mitsamt unmittelbar vorhergehender
  Nutzerfrage, Quellen und Modellmetadaten. Im Bereich „Logs & Status“ macht
  eine neue Auswertung diese Fälle lesbar; Quellen und Metadaten sind bewusst
  einklappbar. Kein Training und keine automatische Folgewirkung. Backend-
  Regressionen für Downvote-Filter, Turn-Kontext und Admin-Gate ergänzt;
  TypeScript und ESLint grün.

## Referenzen

- [Mainframe-Kompatibilität](MAINFRAME_KOMPATIBILITAET.md) — Befunde, Kundenprofil, Quellen und Abnahmeregeln zu O-117–O-156
- [Tech-Debt-Cleanup-Plan](TECH_DEBT_CLEANUP_PLAN.md) — Detailplanung für O-004/O-005
- [Entscheidungslog](ENTSCHEIDUNGEN.md) — fachliche und technische Entscheidungen
- [FAQ Konnektor-Modularität](FAQ_KONNEKTOR_MODULARITAET.md) — Frage-Antwort-Referenz zu O-042 für Vertriebs-/Kundengespräche
- [Panel-Synchronisation](PANEL_SYNCHRONISATION.md) — Soll-Matrix zu O-092, inklusive der offenen Festlegungen D-1 bis D-5

## Zurückgestellte Entwicklungspunkte

| ID | Bereich | Punkt | Nächste Aktion | Abhängigkeit |
|---|---|---|---|---|
| O-158 | Abhängigkeiten / Sicherheit | Nach O-069 verbleiben zwei transitive Python-Auditfunde: `fastmcp` 2.14.7 hat zwei advisories, deren Fix erst `fastmcp` 3.2.0 (Major-Upgrade) ist und über `mcp-atlassian` 0.22.1 eingebracht wird; `diskcache` 5.6.3 hat derzeit keinen veröffentlichten Fix. Die direkt patchbaren Funde (`pypdf`, `cryptography`) sind bereits aktualisiert. | Erst prüfen, ob eine getestete `mcp-atlassian`-Version FastMCP 3 unterstützt; dann Connector- und MCP-Regressionen gegen den Major-Upgrade ausführen. `diskcache` auf Upstream-Fix überwachen und bis dahin die tatsächliche Erreichbarkeit des betroffenen Cache-Pfads dokumentieren. | Upstream-Kompatibilität bzw. veröffentlichter `diskcache`-Fix |
| O-166 | Frontend/Backend / Link-Manager | Bewusst zurückgestellter Rest aus [O-165]: Die Vorschlagsliste des Link-Managers wird weiterhin **komplett** geladen (gemessen 572 KB JSON für 1072 Knowledge-Links plus 211 KB für 518 Entity-Links), nur eben nicht mehr komplett gerendert. Echte serverseitige Pagination ist nicht trivial, weil die Ansicht **zwei** Tabellen (`knowledge_links` und `entity_doc_links`) zu einer nach Konfidenz absteigend sortierten Liste vereint: seitenweises Nachladen würde entweder die globale Sortierung brechen (später geladene Treffer mit höherem Score müssten oberhalb bereits angezeigter einsortiert werden — sichtbares Springen) oder Suche, Art-Filter und „Alle 100 % bestätigen" auf die jeweils geladene Seite einschränken. Bei der heutigen Größenordnung fällt das vollständige Laden nach der Fensterung nicht mehr auf; die Frage ist verschoben, nicht beantwortet. | Anfassen, sobald ein Bestand in den **fünfstelligen** Bereich kommt oder das erste Laden der Ansicht wieder spürbar wird. Dann in einem Zug: `limit`/`offset` **und** Suche/Art-Filter/Sortierung serverseitig (sonst filtert die UI über einen Ausschnitt), `POST /knowledge-links/approve-perfect` o. ä. als serverseitige Sammelbestätigung statt der heutigen Schleife über die geladenen Links, und ein Nachlade-Verhalten für die vereinte Liste (beide Quellen nach Score absteigend, k-Wege-Merge mit Puffer je Quelle). | O-165 (Fensterung und Zähler-Endpunkt liegen bereits) |

## Pflege

Beim Bearbeiten eines Punktes den Status, die nächste Aktion und das Datum direkt
in dieser Tabelle aktualisieren. Erledigte Punkte nicht löschen, sondern in den
Abschnitt „Erledigt“ verschieben und auf Commit, Testlauf oder Abnahme verweisen.
