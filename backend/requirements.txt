fastapi==0.139.2
uvicorn==0.51.0
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
celery==5.6.3
redis==8.0.1
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
pgvector==0.5.0
pypdf==6.14.2
python-docx==1.2.0
mammoth==1.12.0
cryptography==49.0.0
alembic==1.18.5
itsdangerous==2.2.0
argon2-cffi==25.1.0
# OIDC-ID-Token-Prüfung (core/oidc.py, E-12): JOSE/JWKS, kein OAuth-Framework
# drumherum. BSD-3-Clause, reines Python auf Basis von `cryptography`, das
# ohnehin schon Abhängigkeit ist. (Nicht `Authlib` selbst: dessen `authlib.jose`-
# Submodul ist zugunsten von `joserfc`, demselben Autor/Projekt, als deprecated
# markiert.)
joserfc==1.7.5
Pillow==12.3.0
markdown==3.10.2
# MIT-Shim fuer die GPL-2.0-or-later "Unidecode"-Transitivabhaengigkeit von
# mcp-atlassian (docs/ENTSCHEIDUNGEN.md E-7, backend/vendor/unidecode_shim/README.md).
# Deklariert sich selbst als Paket "unidecode" >=1.3.0 -> pip loest mcp-atlassians
# Anforderung dagegen auf statt das echte PyPI-Paket zu laden.
./vendor/unidecode_shim
mcp-atlassian==0.22.1


