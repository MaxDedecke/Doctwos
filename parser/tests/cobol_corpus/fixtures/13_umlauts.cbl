       IDENTIFICATION DIVISION.
       PROGRAM-ID. KONTOSTATUSPRÜFUNG.

       ENVIRONMENT DIVISION.
       INPUT-OUTPUT SECTION.
       FILE-CONTROL.
           SELECT PRÜF-DATEI ASSIGN TO "pruef.dat".

       DATA DIVISION.
       FILE SECTION.
       FD  PRÜF-DATEI.
       01  PRÜF-SATZ           PIC X(20).

       WORKING-STORAGE SECTION.
       01  WS-ANZAHL-PRÜFUNGEN  PIC 9(03).
       01  WS-STATUS-TABELLE.
           05  WS-STATUS-EINTRAG PIC X(01)
               OCCURS 1 TO 10 TIMES DEPENDING ON WS-ANZAHL-PRÜFUNGEN.
       01  WS-STATUS-FLAG      PIC X(01).
           88  STATUS-GEPRÜFT      VALUE 'J'.

       PROCEDURE DIVISION.
       PRÜFUNGS-LOGIK SECTION.
       200-BUCHUNGSPOSITIONEN-PRÜFEN.
           DISPLAY WS-STATUS-FLAG.
           GOBACK.
